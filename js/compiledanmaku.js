// ==========================================
// PRE-COMPILED DANMAKU GENERATORS
// Auto-generated by tools/compile.js
// ==========================================

window.compiledDanmaku = window.compiledDanmaku || {};
window.DanmakuCompilerRuntime = window.DanmakuCompilerRuntime || {};
window.compiledDanmakuList = [
  {
    "difficulty": "NORMAL",
    "name": "【サンプル】スパイラルレイン",
    "desc": "全方位に螺旋を描く弾を発射します（サンプル）",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    aimAtTarget()\n    spawnRing(\"normal\", \"#33ffff\", 180, angle, 8, 0, 0, 6, \"none\", \"relative\")\n    angle += 5\n    wait(0.1)\n}\n        ",
    "bulletScript": "\nspeed = 180\nbounce()\nif (isBounced) {\n    aimAtTarget()\n    color = \"#ff33ff\"\n}\n        ",
    "magicCircleScript": ""
  },
  {
    "difficulty": "EASY",
    "name": "華符「大輪舞転」",
    "desc": "とにかく綺麗に作った。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 2,
    "emitterScript": "\nangle2 = 0\nxspa2 = x\nyspa2 = y\nwhile (true) {\n    spawnWay(\"normal\", \"#ff3333\", 1000, angle + 180, 5, 72, 0, 0, 16, \"ohuda\", \"relative\", \"2\")\n    spawnWay(\"normal\", \"#ff3333\", 1000, angle, 5, 72, 0, 0, 16, \"ohuda\", \"relative\", \"2\")\n    if (second >= 10) {\n        angle2 -= 43.5\n    }\n    angle += 0.2\n    wait(0.0167)\n}\nwhile (true) {\n    tween(\"xspa\", x, 384, \"step\", 1)\n    tween(\"yspa\", y, 448, \"step\", 1)\n    xspa = 384\n    yspa = 448\n    wait(3)\n    while (true) {\n        tween(\"xspa\", xspa, 384, \"step\", 0.1)\n        tween(\"yspa\", yspa, 448, \"step\", 0.1)\n        wait(3)\n    }\n}\nwhile (true) {\n    spawnRing(\"normal\", \"#ffffff\", 200, 0, 18, 0, 0, 9, \"poihuru\", \"relative\", \"7\")\n    wait(0.6)\n}\n        ",
    "bulletScript": "\nif (color != #ffffff) {\n    if (frame == 1) {\n        if (color == \"#ff3333\") {\n            x = e_xspa\n            y = e_yspa\n            prev_x = e_xspa\n            prev_y = e_yspa\n        }\n        if (color == \"#3366ff\") {\n            x = e_xspa2\n            y = e_yspa2\n            prev_x = e_xspa2\n            prev_y = e_yspa2\n        }\n    }\n    if (color == \"#ff3333\") {\n        dx = e_xspa - prev_x\n        dy = e_yspa - prev_y\n        prev_x = e_xspa\n        prev_y = e_yspa\n    }\n    if (color == \"#3366ff\") {\n        dx = e_xspa2 - prev_x\n        dy = e_yspa2 - prev_y\n        prev_x = e_xspa2\n        prev_y = e_yspa2\n    }\n    x += dx\n    y += dy\n    if (frame == 5..150) {\n        angle += 2\n    }\n    if (frame == 170..250) {\n        angle += 0\n    }\n    if (frame == 110..2510) {\n        y += 800000\n        angle -= 0\n    }\n}\nif (color == #ffffff) {\n    once {\n        x = 384\n        y = 448\n    }\n}\n        ",
    "magicCircleScript": ""
  },
  {
    "difficulty": "HARD",
    "name": "サンライトインパクト~Normal~",
    "desc": "ルナティックインパクトのパク...オマージュ。",
    "duration": 50,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    while (true) {\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(6)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n}\n    ",
    "bulletScript": "\n    once {\n    m = 1\n}\nif (isTouchEdge) {\n    aimAtTarget()\n    spriteAngle = angle\n    speed = 0\n    m = 0\n}\nif (m===0) {\n    for (let i = 0; i < 2; i++) {\n        rang = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, rang, 36, 0, 0, 9, \"star\", \"relative\", \"3\")\n        wait(0.2)\n    }\n    m = 1\n}\nif (m === 1) {\n    speed += 2.5\n}\n    ",
    "magicCircleScript": "\n    spriteAngle += 3\n    "
  },
  {
    "difficulty": "LUNATIC",
    "name": "サンライトインパクト",
    "desc": "ルナティックインパクトのパク...オマージュ。",
    "duration": 50,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    while (true) {\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(6)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n    m = 0\n    aimAtTarget()\n    spawnBulletResist(\"normal\", \"#ffdd33\", 0, angle, 0, 0, 70, \"ootama\", \"relative\", \"70\")\n    wait(10)\n}\n    ",
    "bulletScript": "\n    once {\n    m = 1\n}\nif (isTouchEdge) {\n    aimAtTarget()\n    spriteAngle = angle\n    speed = 0\n    m = 0\n}\nif (m===0) {\n    for (let i = 0; i < 5; i++) {\n        rang = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, rang, 36, 0, 0, 9, \"star\", \"relative\", \"6\")\n        wait(0.05)\n    }\n    m = 1\n}\nif (m === 1) {\n    speed += 2.5\n}\n    ",
    "magicCircleScript": "\n    spriteAngle += 3\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "弾幕の檻",
    "desc": "自機狙いと自機外しの弾が発射され、壁に当たるとレーザーを放つ。もちろん殺意の百合のオマージュ",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    while (true) {\n    aimAtTarget()\n    spawnWay(\"normal\", \"#ff3333\", 500, angle, 5, 20, 0, 0, 30, \"ootama\", \"relative\", \"15\")\n    wait(0.1)\n    aimAtTarget()\n    spawnWay(\"normal\", \"#ff3333\", 500, angle + 50, 5, 20, 0, 0, 30, \"ootama\", \"relative\", \"15\")\n    wait(0.1)\n    aimAtTarget()\n    spawnWay(\"normal\", \"#ff3333\", 500, angle - 50, 5, 20, 0, 0, 30, \"ootama\", \"relative\", \"15\")\n    wait(2)\n    aimAtTarget()\n    for (let i = 0; i < 36; i++) {\n        spawnWay(\"normal\", \"#3333ff\", 500, angle, 1, 10, 0, 0, 20, \"ohuda\", \"relative\", \"6\")\n        angle += 25\n        wait(0.01)\n    }\n    wait(3)\n}\n    ",
    "bulletScript": "\n    if (color != #ffffff) {\n    if (x > 758) {\n        angle = 180\n        warningTime = 1\n        activeTime = 1.5\n        laserWidth = 20\n    }\n    if (y < 10) {\n        angle = 90\n        warningTime = 1\n        activeTime = 1.5\n        laserWidth = 20\n    }\n    if (x < 10) {\n        angle = 0\n        warningTime = 1\n        activeTime = 1.5\n        laserWidth = 20\n    }\n    if (y > 886) {\n        angle = 270\n        warningTime = 1\n        activeTime = 1.5\n        laserWidth = 20\n    }\n}\n    ",
    "magicCircleScript": "\n    "
  },
  {
    "difficulty": "EASY",
    "name": "超絶気合符「インフィニットスパイラル」",
    "desc": "楽しい！好き！",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    for (let i = 0; i < 1200000000; i++) {\n    spawnRing(\"normal\", \"#ff3333\", 200, angle, 36 + way, 0, 0, 20, \"ohuda\", \"relative\", \"6\")\n    way += 2\n    angle += 16\n    wait(0.26)\n}\nwhile (true) {\n    moveTo(\"center\")\n}\n    ",
    "bulletScript": "\n    if (isBounced) {\n    y = -80000\n}\nif (y > 886) {\n    y = -80000\n}\n    ",
    "magicCircleScript": "\n    "
  },
  {
    "difficulty": "HARD",
    "name": "技符「陰陽掃除機」",
    "desc": "完全パターンスペル。そこそこ作るのに苦労しました",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    while (true) {\n    spawnBulletResist(\"normal\", \"#ff3333\", 150, 45, 0, 0, 100, \"b_knife\", \"relative\", \"40\")\n    spawnBulletResist(\"normal\", \"#ff3333\", 150, 45 + 90 + 90, 0, 0, 100, \"b_knife\", \"relative\", \"40\")\n    wait(200)\n}\n    ",
    "bulletScript": "\n    once {\n    shotTimer = 0\n}\nspeed += 2\nspriteAngle = angle\nif (y > 886) {\n    angle = -angle\n    speed = 150\n    y = 886\n}\nif (y < 10) {\n    angle = -angle\n    speed = 150\n    y = 10\n}\nif (x < 10) {\n    angle = 180 - angle\n    speed = 150\n    x = 10\n}\nif (x > 758) {\n    angle = 180 - angle\n    speed = 150\n    x = 758\n}\nshotTimer = shotTimer + 1\nif (shotTimer >= 12) {\n    spawnWay(\"normal\", \"#3388ff\", 200, angle + 180, 5, 72, 0, 0, 6, \"onmyoutama\", \"relative\", \"6\")\n    shotTimer = 0\n}\n    ",
    "magicCircleScript": "\n    spriteAngle += 5\n    "
  },
  {
    "difficulty": "HARD",
    "name": "上は洪水下は大火事",
    "desc": "普通にお気に入りのスペル！楽しい！",
    "duration": 32,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n    while (true) {\n    angle = -90 + random(-20,20)\n    if (cardSecond == 15..30) {\n        angle += random(-30,30)\n    }\n    if (cardSecond == 30..35) {\n        angle += random(-50,50)\n    }\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle, 0, 886, 20, \"poihuru\", \"relative\", \"10\")\n    if (cardSecond == 0..15) {\n        wait(0.05)\n    }\n    if (cardSecond == 15..30) {\n        wait(0.02)\n    }\n    if (cardSecond == 30..35) {\n        wait(0.0002)\n    }\n}\n    ",
    "bulletScript": "\n    if (color == #ff3333) {\n    if (y < 0) {\n        angle = 90 + random(-5,5)\n        speed = 5\n        color = #33ffff\n        y = 5\n    }\n    speed += 0.4\n}\nif (color == #33ffff) {\n    speed += 2\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "フラッシュレートレイン",
    "desc": "雨と巨大レーザー！",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 1200000; i++) {\n    spawn = random(0,768)\n    for (let i = 0; i < 3; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 300, 90, 0, -200, 7, \"uroko\", \"relative\", \"4\")\n        wait(0.03)\n    }\n}\nfor (let i = 0; i < 1200000; i++) {\n    spawn2 = random(0,768)\n    for (let i = 0; i < 5; i++) {\n        spawnBullet(\"normal\", \"#ff3323\", 300, 90, 0, -200, 7, \"uroko\", \"relative\", \"4\")\n        wait(0.03)\n    }\n}\nfor (let i = 0; i < 1200000; i++) {\n    spawn3 = random(0,768)\n    for (let i = 0; i < 7; i++) {\n        spawnBullet(\"normal\", \"#ff3332\", 300, 90, 0, -200, 7, \"uroko\", \"relative\", \"4\")\n        wait(0.03)\n    }\n}\nwhile (true) {\n    wait(4)\n    spawnBullet(\"normal\", \"#33ffff\", 200, 90, 0, -150, 6, \"none\", \"relative\", \"\")\n}\n    ",
    "bulletScript": "\nonce {\n    if (color == #ff3333) {\n        x = spawn\n    }\n    if (color == #ff3332) {\n        x = spawn2\n    }\n    if (color == #ff3323) {\n        x = spawn3\n    }\n    if (color == #33ffff) {\n        x = tx\n        warningTime = 3\n        activeTime = 1.5\n        laserWidth = 900\n    }\n    wait(0.1)\n}\nif (y > 896) {\n    y = -580000\n}\n    ",
    "magicCircleScript": "\n    "
  },
  {
    "difficulty": "Lunatic",
    "name": "札と刃の境界",
    "desc": "うおっ、となるスペルです。",
    "duration": 65,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 10,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 20; i++) {\n        ys = 0\n        for (let i = 0; i < 10; i++) {\n            spawnBullet(\"normal\", \"#33ffff\", 120, 0, -100, ys + 15, 23, \"ohuda\", \"absolute\", \"12\")\n            spawnBullet(\"normal\", \"#33ffff\", 120, 0, -100, ys - 15, 23, \"ohuda\", \"absolute\", \"12\")\n            ys += 100\n        }\n        ys = 0\n        for (let i = 0; i < 10; i++) {\n            spawnBullet(\"normal\", \"#33ff88\", 120, 180, 868, ys + 65, 23, \"ohuda\", \"absolute\", \"12\")\n            spawnBullet(\"normal\", \"#33ff88\", 120, 180, 868, ys + 35, 23, \"ohuda\", \"absolute\", \"12\")\n            ys += 100\n        }\n        wait(0.7)\n    }\n    wait(1)\n    for (let i = 0; i < 20; i++) {\n        xs = 0\n        for (let i = 0; i < 10; i++) {\n            spawnBullet(\"normal\", \"#33ffff\", 120, 90, xs + 15, 0, 23, \"ohuda\", \"absolute\", \"12\")\n            spawnBullet(\"normal\", \"#33ffff\", 120, 90, xs - 15, 0, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 100\n        }\n        xs = 0\n        for (let i = 0; i < 10; i++) {\n            spawnBullet(\"normal\", \"#33ff88\", 120, -90, xs + 15 + 50, 996, 23, \"ohuda\", \"absolute\", \"12\")\n            spawnBullet(\"normal\", \"#33ff88\", 120, -90, xs - 15 + 50, 996, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 100\n        }\n        wait(0.7)\n    }\n    wait(1)\n    for (let i = 0; i < 20; i++) {\n        xs = -650\n        ys = 550\n        for (let i = 0; i < 25; i++) {\n            xs -= 11\n            ys += 11\n            spawnBullet(\"normal\", \"#33ffff\", 120, 45, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 22\n            ys -= 22\n            spawnBullet(\"normal\", \"#33ffff\", 120, 45, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs -= 11\n            ys += 11\n            xs += 71\n            ys -= 71\n        }\n        xs = 1500\n        ys = 357\n        for (let i = 0; i < 25; i++) {\n            xs += 11\n            ys -= 11\n            spawnBullet(\"normal\", \"#33ff88\", 120, 225, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs -= 22\n            ys += 22\n            spawnBullet(\"normal\", \"#33ff88\", 120, 225, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 11\n            ys -= 11\n            xs -= 71\n            ys += 71\n        }\n        wait(0.7)\n    }\n    wait(2)\n    for (let i = 0; i < 20; i++) {\n        xs = 1418\n        ys = 550\n        for (let i = 0; i < 25; i++) {\n            xs -= 11\n            ys -= 11\n            spawnBullet(\"normal\", \"#33ffff\", 120, 135, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 22\n            ys += 22\n            spawnBullet(\"normal\", \"#33ffff\", 120, 135, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs -= 11\n            ys -= 11\n            xs -= 71\n            ys -= 71\n        }\n        xs = -732\n        ys = 357\n        for (let i = 0; i < 25; i++) {\n            xs -= 11\n            ys -= 11\n            spawnBullet(\"normal\", \"#33ff88\", 120, 315, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs += 22\n            ys += 22\n            spawnBullet(\"normal\", \"#33ff88\", 120, 315, xs, ys, 23, \"ohuda\", \"absolute\", \"12\")\n            xs -= 11\n            ys -= 11\n            xs += 71\n            ys += 71\n        }\n        wait(0.7)\n    }\n    wait(2)\n}\nwhile (true) {\n    xdao = tx\n    spawnWay(\"normal\", \"#ff3333\", 1, angle, 1, 20, xdao, 0, 30, \"knife\", \"absolute\", \"6\")\n    wait(1)\n}\n    ",
    "bulletScript": "\nif (color==#ff3333) {\n    once {\n        x = tx\n        angle = 90\n    }\n    speed += 1\n}\nif (speed == 50..10000) {\n    speed = speed / 1.0005\n}\nif (speed == 200..202) {\n    angle += random(0,0)\n}\nonce {\n    speed += 40\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "双毒「絡みつく赤大蛇」",
    "desc": "クリアチェックはしました。",
    "duration": 60,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    if (cardSecond == 0..15) {\n        xs = random(-600,600)\n        spawnBullet(\"normal\", \"#33ffff\", 200, 90, 0, -200, 10, \"onmyoutama\", \"relative\", \"10\")\n        wait(0.1)\n    }\n}\nwhile (true) {\n    if (cardSecond == 15..30) {\n        xs = random(-600,600)\n        spawnBullet(\"normal\", \"#33ffff\", 200, 90, 0, -200, 10, \"onmyoutama\", \"relative\", \"10\")\n        wait(0.06)\n    }\n}\nwhile (true) {\n    if (cardSecond == 30..60) {\n        xs = random(-600,600)\n        spawnBullet(\"normal\", \"#33ffff\", 200, 90, 0, -200, 10, \"onmyoutama\", \"relative\", \"10\")\n        wait(0.04)\n    }\n}\nwhile (true) {\n    if (cardSecond == 15..60) {\n        xs = random(-600,600)\n        spawnBullet(\"normal\", \"#ffaa33\", 200, 90, 0, -200, 30, \"onmyoutama\", \"relative\", \"35\")\n        wait(0.4)\n    }\n}\nwhile (true) {\n    if (cardSecond == 35..60) {\n        xs = random(-600,600)\n        spawnBullet(\"normal\", \"#ffaa33\", 200, 90, 0, -200, 30, \"onmyoutama\", \"relative\", \"35\")\n        wait(0.4)\n    }\n}\nwhile (true) {\n    if (cardSecond == 0..15) {\n        for (let i = 0; i < 8; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            spawnBullet(\"normal\", \"#ff3333\", 300, -angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            angle += 10\n            wait(0.05)\n        }\n    }\n    if (cardSecond == 15..30) {\n        for (let i = 0; i < 16; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            spawnBullet(\"normal\", \"#ff3333\", 300, -angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            angle += 10\n            wait(0.05)\n        }\n    }\n    if (cardSecond == 30..45) {\n        for (let i = 0; i < 24; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            spawnBullet(\"normal\", \"#ff3333\", 300, -angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            angle += 10\n            wait(0.05)\n        }\n    }\n    if (cardSecond == 45..60) {\n        for (let i = 0; i < 32; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            spawnBullet(\"normal\", \"#ff3333\", 300, -angle, 0, 0, 30, \"b_uroko\", \"relative\", \"20\")\n            angle += 10\n            wait(0.05)\n        }\n    }\n    wait(0.8)\n    angle += 20\n}\nwhile (true) {\n    if (cardSecond == 45..60) {\n        aimAtTarget()\n        spawnWay(\"normal\", \"#ffdd33\", 400, angle, 3, 30, 0, 0, 30, \"onmyoutama\", \"relative\", \"35\")\n        wait(1)\n    }\n}\n    ",
    "bulletScript": "\nif (color == #ff3333) {\n    if (timer == 1..1.4) {\n        speed += -7\n    }\n    if (timer == 1.4..2) {\n        speed = 300\n        once {\n            aimAtTarget()\n            if (cardSecond == 30..60) {\n                angle += random(-3,3)\n            }\n        }\n    }\n}\nif (color == #33ffff) {\n    speed = 150\n    m += 5\n    once {\n        y = 0\n        xs = random(-600,600)\n        x += xs\n    }\n}\nif (color == #ffaa33) {\n    speed = 100\n    m += 5\n    once {\n        y = 0\n        xs = random(-600,600)\n        x += xs\n    }\n}\nif (color == #ffdd33) {\n    m += 5\n}\nif (cardSecond == 15) {\n    y = -8000\n}\nif (cardSecond == 30) {\n    y = -8000\n}\nif (cardSecond == 45) {\n    y = -8000\n}\nspriteAngle = angle + m\nif (dist < 50) {\n    y = y\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "LUNATIC",
    "name": "旋風「ビッグトルネード」",
    "desc": "圧倒的弾速、圧倒的気合避け",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    angle = 0\n    for (let i = 0; i < 3; i++) {\n        spawnWay(\"normal\", \"#ff3333\", 1000, angle + 45, 20, 18, 0, 0, 30, \"ootama\", \"relative\", \"15\")\n        wait(0.08)\n    }\n    angle = 9\n    for (let i = 0; i < 3; i++) {\n        spawnWay(\"normal\", \"#ff3333\", 1000, angle + 45, 20, 18, 0, 0, 30, \"ootama\", \"relative\", \"15\")\n        wait(0.08)\n    }\n}\n    ",
    "bulletScript": "\nonce {\n    angle += random(-2,2)\n    speed += random(0,0)\n}\nif (speed == 400..100000) {\n    speed += -5\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "偽符「ダブルスパイル」",
    "desc": "攻略法が分かると簡単なタイプ。",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 7,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 10; i++) {\n        spawnWay(\"normal\", \"#ff3333\", 35000, angle, 10, 36, 384, 448, 10, \"marutama\", \"absolute\", \"4\")\n        wait(0.05)\n        angle += 2\n    }\n    for (let i = 0; i < 10; i++) {\n        spawnWay(\"normal\", \"#ff3333\", 35000, angle, 10, 36, 384, 448, 10, \"marutama\", \"absolute\", \"4\")\n        wait(0.05)\n        angle -= 9\n    }\n}\n    ",
    "bulletScript": "\nif (frame = 30..30000) {\n    if (x == 374..394) {\n        if (y == 438..458) {\n            color = #3333ff\n        }\n    }\n}\nif (frame = 2..3) {\n    angle += 180\n    speed = 200\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "EASY",
    "name": "秘儀「十三頭の龍」",
    "desc": "結構いい感じに作れました。下からの反射を追加したことでそこそこの難易度になったかも...",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 1200; i++) {\n    aimAtTarget()\n    for (let i = 0; i < 12; i++) {\n        spawnp = spawnp + 1\n        for (let i = 0; i < 4; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300 + spd, angle, 0, 0, 10, \"uroko\", \"relative\", \"5\")\n            spawnBullet(\"normal\", \"#ff3332\", 300 + spd, angle, 0, 0, 10, \"uroko\", \"relative\", \"5\")\n            spd += 20\n        }\n        spd = 0\n        wait(0.02)\n    }\n    for (let i = 0; i < 12; i++) {\n        spawnp = spawnp - 1\n        for (let i = 0; i < 4; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 300 + spd, angle, 0, 0, 10, \"uroko\", \"relative\", \"5\")\n            spawnBullet(\"normal\", \"#ff3332\", 300 + spd, angle, 0, 0, 10, \"uroko\", \"relative\", \"5\")\n            spd += 20\n        }\n        spd = 0\n        wait(0.02)\n    }\n    spawnp = -10\n    wait(0.15)\n}\n    ",
    "bulletScript": "\nif (color == #ff3333) {\n    once {\n        x = 379 + spawnp * 60\n    }\n}\nif (color == #ff3332) {\n    once {\n        x = 379 - spawnp * 60\n    }\n}\nif (y > 890) {\n    angle = -90\n}\nif (y < 10) {\n    y = -50000\n}\nif (x > 758) {\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "EASY",
    "name": "波符「白銀のタイダルウェーブ」",
    "desc": "気づいたら星蓮船四面ボスのアレみたいになってた。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    tween(\"ex\", ex, tx, \"vecstep\", 0.1)\n    tween(\"ey\", ey, ty, \"vecstep\", 0.1)\n    wait(1 - t)\n    wait(1 - t)\n    wait(0.5)\n    spawnRing(\"normal\", \"#ffffff\", 200, 0, 72, 0, 0, 10, \"b_uroko\", \"relative\", \"5\")\n    wait(0.2)\n    spawnRing(\"normal\", \"#ffffff\", 200, 0, 72, 0, 0, 10, \"b_uroko\", \"relative\", \"5\")\n    wait(0.2)\n    spawnRing(\"normal\", \"#ffffff\", 200, 0, 72, 0, 0, 10, \"b_uroko\", \"relative\", \"5\")\n    wait(0.2)\n    t += 0.05\n    t2 += 0.0175\n}\n    ",
    "bulletScript": "\n    speed += 1\n    ",
    "magicCircleScript": "\n    "
  },
  {
    "difficulty": "EASY",
    "name": "白銀のタルタルソース",
    "desc": "",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 5,
    "emitterScript": "\nfor (let i = 0; i < 1200000000; i++) {\n    spawnBullet(\"normal\", \"#ffffff\", 200, -90 + angle, 0, 0, 30, \"b_marutama\", \"relative\", \"20\")\n    angle = random(-50,50)\n    wait(0.02)\n}\nwhile (true) {\n    r = random(-100,100)\n    r2 = random(-100,100)\n    tween(\"ex\", ex, 384 + r, \"seconds\", 0.4)\n    tween(\"ey\", ey, 370 + r2, \"seconds\", 0.4)\n    wait(5)\n}\n    ",
    "bulletScript": "\nonce {\n    yp = -40\n}\ny += yp / 10\nyp += 0.4\nif (yp = -5..5) {\n    SpriteAngle = 90\n}\nwait(0.00001)\nif (yp = -5..5) {\n    SpriteAngle = 90\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "熱符「真紅たる太陽風」",
    "desc": "交差する弾って...楽しいですよね。",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 1200; i++) {\n    spawnRingResist(\"normal\", \"#ff3333\", 300, angle, 36, 0, 0, 10, \"b_marutama\", \"relative\", \"6\")\n    spawnRingResist(\"normal\", \"#ff3332\", 300, angle, 36, 0, 0, 10, \"b_marutama\", \"relative\", \"6\")\n    angle += random(-5,5)\n    wait(0.3)\n}\n    ",
    "bulletScript": "\nif (color == #ff3333) {\n    angle += 0.2 - m\n}\nif (color == #ff3332) {\n    angle -= 0.2 - m\n}\nm += 0.0003\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "忌符「フライングスター」",
    "desc": "全方位反射って難しいですよねぇ...",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\ncx = 384\ncy = 350\nR = 200\nbx = 0\nby = 0\nbx2 = 0\nby2 = 0\nangle = 0\nangle2 = 0\nwhile (true) {\n    // 1ループごとに、星全体の傾きをランダム（0〜360度）で決める\n    base_angle = random(0, 360)\n    // 決まった傾きを足して、星の頂点座標を毎回計算し直す\n    x0 = cx + R * cos(-90 + base_angle)\n    y0 = cy + R * sin(-90 + base_angle)\n    x1 = cx + R * cos(-18 + base_angle)\n    y1 = cy + R * sin(-18 + base_angle)\n    x2 = cx + R * cos(54 + base_angle)\n    y2 = cy + R * sin(54 + base_angle)\n    x3 = cx + R * cos(126 + base_angle)\n    y3 = cy + R * sin(126 + base_angle)\n    x4 = cx + R * cos(198 + base_angle)\n    y4 = cy + R * sin(198 + base_angle)\n    rx0 = cx + R * cos(90 + base_angle)\n    ry0 = cy + R * sin(90 + base_angle)\n    rx1 = cx + R * cos(162 + base_angle)\n    ry1 = cy + R * sin(162 + base_angle)\n    rx2 = cx + R * cos(234 + base_angle)\n    ry2 = cy + R * sin(234 + base_angle)\n    rx3 = cx + R * cos(306 + base_angle)\n    ry3 = cy + R * sin(306 + base_angle)\n    rx4 = cx + R * cos(18 + base_angle)\n    ry4 = cy + R * sin(18 + base_angle)\n    bx = x0\n    by = y0\n    bx2 = rx0\n    by2 = ry0\n    wait(0.3)\n    star = 1\n    t = 0\n    // ナイフの向き（進行方向）にも傾きを足して、綺麗に星の辺に沿わせる\n    angle = 72 + base_angle\n    angle2 = -108 + base_angle\n    tween(\"bx\", x0, x2, \"seconds\", 0.3)\n    tween(\"by\", y0, y2, \"seconds\", 0.3)\n    tween(\"bx2\", rx0, rx2, \"seconds\", 0.3)\n    tween(\"by2\", ry0, ry2, \"seconds\", 0.3)\n    wait(0.3)\n    angle = -144 + base_angle\n    angle2 = 36 + base_angle\n    tween(\"bx\", x2, x4, \"seconds\", 0.3)\n    tween(\"by\", y2, y4, \"seconds\", 0.3)\n    tween(\"bx2\", rx2, rx4, \"seconds\", 0.3)\n    tween(\"by2\", ry2, ry4, \"seconds\", 0.3)\n    wait(0.3)\n    angle = 0 + base_angle\n    angle2 = 180 + base_angle\n    tween(\"bx\", x4, x1, \"seconds\", 0.3)\n    tween(\"by\", y4, y1, \"seconds\", 0.3)\n    tween(\"bx2\", rx4, rx1, \"seconds\", 0.3)\n    tween(\"by2\", ry4, ry1, \"seconds\", 0.3)\n    wait(0.3)\n    angle = 144 + base_angle\n    angle2 = -36 + base_angle\n    tween(\"bx\", x1, x3, \"seconds\", 0.3)\n    tween(\"by\", y1, y3, \"seconds\", 0.3)\n    tween(\"bx2\", rx1, rx3, \"seconds\", 0.3)\n    tween(\"by2\", ry1, ry3, \"seconds\", 0.3)\n    wait(0.3)\n    angle = -72 + base_angle\n    angle2 = 108 + base_angle\n    tween(\"bx\", x3, x0, \"seconds\", 0.3)\n    tween(\"by\", y3, y0, \"seconds\", 0.3)\n    tween(\"bx2\", rx3, rx0, \"seconds\", 0.3)\n    tween(\"by2\", ry3, ry0, \"seconds\", 0.3)\n    wait(0.3)\n    star = 0\n    angle = 90 + base_angle\n    angle2 = -90 + base_angle\n    tween(\"bx\", x0, cx, \"seconds\", 0.3)\n    tween(\"by\", y0, cy, \"seconds\", 0.3)\n    tween(\"bx2\", rx0, cx, \"seconds\", 0.3)\n    tween(\"by2\", ry0, cy, \"seconds\", 0.3)\n    wait(0.3)\n    wait(0.1)\n    t = 1\n    wait(0.3)\n}\nwhile (true) {\n    if (star == 1) {\n        spawnBullet(\"normal\", \"#ff3366\", 0, angle, bx, by, 20, \"b_star\", \"absolute\", \"15\")\n        spawnBullet(\"normal\", \"#ff3366\", 0, angle2, bx2, by2, 20, \"b_star\", \"absolute\", \"15\")\n    }\n    wait(0.00005)\n}\n    ",
    "bulletScript": "\nif (e_t==1) {\n    once {\n        flag = 1\n    }\n}\nif (flag=1) {\n    if (speed==0..400) {\n        speed += 2\n    }\n    if (x < 10) {\n        once {\n            speed = 0\n            angle = -angle\n            angle = angle - 180\n        }\n    }\n    if (x > 758) {\n        once {\n            speed = 0\n            angle = -angle\n            angle = angle - 180\n        }\n    }\n    if (y < 10) {\n        once {\n            speed = 0\n            angle = -angle\n        }\n    }\n    if (y > 886) {\n        once {\n            speed = 0\n            angle = -angle\n        }\n    }\n}\nspriteAngle = spriteAngle + 3\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "恕符「血濡れのアナザーディメンション」",
    "desc": "どう見ても輝針城モチーフ。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnRing(\"normal\", \"#ff3333\", 200, angleg, 8, 0, 0, 6, \"ootama\", \"relative\", \"6\")\n    angleg += 16\n    wait(0.6)\n}\nwhile (true) {\n    offset = 0\n    spawnBullet(\"normal\", \"#dddddd\", 200, angle, 300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ddddde\", 200, angle, -300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    offset = 30\n    spawnBullet(\"normal\", \"#dddddd\", 200, angle, 300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ddddde\", 200, angle, -300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    offset = -30\n    spawnBullet(\"normal\", \"#dddddd\", 200, angle, 300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ddddde\", 200, angle, -300, 0, 30, \"b_knife\", \"relative\", \"6\")\n    aimAtTarget()\n    wait(0.6)\n}\n    ",
    "bulletScript": "\nwhile (true) {\n    if (color==#ff3333) {\n        radius += 0.8\n        hitRadius += 0.7\n        speed += 2\n    }\n    if (color==#dddddd) {\n        radius += 1\n        hitRadius += 0.2\n        speed += 2\n        once {\n            aimAtTarget()\n            angle += offset\n        }\n    }\n    if (color==#ddddde) {\n        radius += 1\n        hitRadius += 0.2\n        speed += 2\n        once {\n            aimAtTarget()\n            angle += offset\n        }\n    }\n    spriteAngle = angle\n    wait(0.01)\n    spriteAngle = angle\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "HARD",
    "name": "真実「静焔のレプティリアン」",
    "desc": "作ってる途中、赤色の幻想郷とレプティリアンインテリジェンスを行ったり来たりしてました。",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 5,
    "emitterScript": "\nwhile (true) {\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(5)\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#3333ff\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(5)\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(5)\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#3333ff\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(5)\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(5)\n    angle = random(0,360)\n    for (let i = 0; i < 9; i++) {\n        spawnBullet(\"normal\", \"#3333ff\", 300, angle, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 40\n    }\n    wait(50)\n}\n    ",
    "bulletScript": "\nif (timer < 3) {\n    if (color==#ff3333) {\n        angle += 0.1\n    }\n    if (color==#3333ff) {\n        angle += -0.1\n    }\n}\ng += 1\nif (g == 40) {\n    g = 0\n}\nif (g == 10) {\n    if (color==#ff3333) {\n        spawnRing(\"normal\", \"#ff3333\", 250, 0, 5, 0, 0, 10, \"uroko\", \"relative\", \"4\")\n    }\n    if (color==#3333ff) {\n        spawnRing(\"normal\", \"#3333ff\", 250, 0, 5, 0, 0, 10, \"uroko\", \"relative\", \"4\")\n    }\n}\n    ",
    "magicCircleScript": "\nif (timer == 3..4) {\n    homing(90)\n    angle += random(-1,1)\n}\nif (timer == 4) {\n    angle += random(-5,5)\n}\nif (timer == 3..5) {\n    speed += random(0.5,1.5)\n}\nif (timer == 0.1) {\n    speed = 1\n}\nspriteAngle = angle\n    "
  },
  {
    "difficulty": "EASY",
    "name": "「完全自動殺戮マシン」",
    "desc": "弾から弾が出るスペルを作るのが楽しいんだよなあ！！",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnBulletResist(\"normal\", \"#ff3333\", 400, -15, 0, 0, 50, \"ootama\", \"relative\", \"40\")\n    spawnBulletResist(\"normal\", \"#ff3333\", 400, 195, 0, 0, 50, \"ootama\", \"relative\", \"40\")\n    wait(1)\n    spawnBulletResist(\"normal\", \"#ff3333\", 400, -15, 0, 0, 50, \"ootama\", \"relative\", \"40\")\n    spawnBulletResist(\"normal\", \"#ff3333\", 400, 195, 0, 0, 50, \"ootama\", \"relative\", \"40\")\n    wait(7)\n}\n    ",
    "bulletScript": "\nwhile (true) {\n    bounce()\n    m += 1\n    if (m==12..20) {\n        spawnBullet(\"normal\", \"#ff3333\", spd, 90 + r, 0, 0, 30, \"b_knife\", \"relative\", \"5\")\n        spd = random(150,250)\n        r = rand(-5,5)\n        m = random(0,6)\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "鋒符「尾を噛む龍」",
    "desc": "万物は流転する。自らの尾を喰らう龍のように、終わりなき円環を描く。",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnRingResist(\"normal\", \"#ffffff\", 500, spangle, 16, 0, 0, 0, \"ohuda\", \"relative\", \"0\")\n    wait(4 - w)\n    wait(2)\n    w += 0.6\n    w2 += 0.1\n    w3 += 10\n    spangle += random(0,360)\n}\n    ",
    "bulletScript": "\nif (timer > 0.2) {\n    wait(0.04)\n    spawnRing(\"normal\", \"#ffffff\", 0, angle, 1, 0, 0, 20, \"kome\", \"relative\", \"6\")\n    angle += kaku\n    spriteAngle = angle\n}\nspeed = 200\nif (flag != 1) {\n    kaku += 2\n}\nif (kaku == 11..500) {\n    flag = 1\n}\nif (flag == 1) {\n    kaku -= 2\n}\nif (kaku == -500..-11) {\n    flag = 0\n}\n    ",
    "magicCircleScript": "\nif (timer == 1) {\n    speed = 50 + random(-10,50) + w3\n    angle += random(-180,180)\n    color = #ff3333\n    hitRadius = 6\n}\nspriteAngle = angle\nif (timer == 3 - w2) {\n    y = -546546456\n}\n    "
  },
  {
    "difficulty": "Lunatic",
    "name": "蛇符「人間殺しの大白蛇」",
    "desc": "最近こういうスペルしか作ってないｗ",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 15; i++) {\n        spawnBullet(\"normal\", \"#ffffff\", 400, 90, spx, 0, 20, \"b_uroko\", \"absolute\", \"12\")\n        spx += 60\n        wait(0.2)\n    }\n    for (let i = 0; i < 15; i++) {\n        spawnBullet(\"normal\", \"#ffffff\", 400, 90, spx, 0, 20, \"b_uroko\", \"absolute\", \"12\")\n        spx -= 60\n        wait(0.2)\n    }\n}\nwhile (true) {\n    for (let i = 0; i < 5; i++) {\n        aimAtTarget()\n        spawnWayResist(\"normal\", \"#ff3333\", 300, angle, 1 + wa, 5, 0, 0, 15, \"b_poihuru\", \"relative\", \"7\")\n        wait(0.3)\n    }\n    wait(2)\n    wa += 1\n}\n    ",
    "bulletScript": "\nif (color == #ffffff) {\n    period = 60\n    amp = 30\n    baseAngle = 90\n    frame2 = 0\n    y = random(-200,0)\n    x += random(-30,30)\n    while (true) {\n        frame2 += 1\n        l += 1\n        angle = baseAngle - amp * sin(frame2 * 360 / period)\n        spriteAngle = angle\n        if (l == 3) {\n            spawnRing(\"normal\", \"#ffffff\", 0, angle + 180, 1, 0, 0, 12, \"b_marutama\", \"relative\", \"12\")\n            l = 0\n        }\n            wait(0.0167)\n    }\n}\n    ",
    "magicCircleScript": "\nif (frame == 60..500) {\n    y = -8000\n}\n    "
  },
  {
    "difficulty": "EASY",
    "name": "獄符「スターアンドプリズン」",
    "desc": "上下で挟んでくるタイプの弾幕。ちなみにガチで苦手。",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    wait(0.5)\n    for (let i = 0; i < 1000000000000; i++) {\n        spawnRing(\"normal\", \"#ffff66\", 35000, angle, 6, 0, 0, 30, \"b_star\", \"relative\", \"20\")\n        spawnRing(\"normal\", \"#ffff66\", 35000, -angle, 6, 0, 0, 30, \"b_star\", \"relative\", \"20\")\n        angle += 6\n        wait(0.5)\n    }\n}\nif (x < 10) {\n}\n    ",
    "bulletScript": "\nonce {\n    wait(0.02)\n    speed = 200\n    angle += 180\n}\nspriteAngle = angle + m\nm += 5\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "ドパ符「ドーパミンの極致」",
    "desc": "ドーパドパドパドパｗｗｗｗｗドーパミンの放出は楽しいドパねぇｗｗｗｗｗｗｗｗ",
    "duration": 10,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 100; i++) {\n        spawnRing(\"normal\", \"#ff3333\", 600, angle, 4, 0, 0, 15, \"b_uroko\", \"relative\", \"10\")\n        angle += random(0,360)\n        wait(0.01)\n    }\n    for (let i = 0; i < 50; i++) {\n        spawnRing(\"normal\", \"#ffaa33\", 700, angle, 2, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n        angle += 12.2\n        wait(0.02)\n    }\n    for (let i = 0; i < 50; i++) {\n        aimAtTarget()\n        angle += random(-7,7)\n        spawnWay(\"normal\", \"#ffdd33\", 700, angle, 5, 25, 0, 0, 20, \"b_ohuda\", \"relative\", \"10\")\n        wait(0.0167)\n    }\n    for (let i = 0; i < 50; i++) {\n        angle += 25\n        w = 0\n        sp = 0\n        for (let i = 0; i < 7; i++) {\n            spawnWay(\"normal\", \"#33ff88\", 300 + sp, angle, 1 + w, 2, 0, 0, 20, \"b_poihuru\", \"relative\", \"20\")\n            w += 1\n            sp += 30\n        }\n        wait(0.03)\n    }\n    for (let i = 0; i < 20; i++) {\n        angle = random(-80,80)\n        spawnWay(\"normal\", \"#3388ff\", 700, -90 + angle, 3, 10, 0, 0, 30, \"b_star\", \"relative\", \"20\")\n        wait(0.05)\n    }\n    for (let i = 0; i < 50; i++) {\n        xsp = random(0,768)\n        spawnWay(\"normal\", \"#9E76B4\", 700, 90, 1, 10, 0, 0, 10, \"poihuru\", \"relative\", \"6\")\n        wait(0.02)\n    }\n    for (let i = 0; i < 500000; i++) {\n        for (let i = 0; i < 10; i++) {\n            spawnRing(\"normal\", \"#ff3333\", 600, angle, 4, 0, 0, 15, \"b_uroko\", \"relative\", \"10\")\n            angle += random(0,360)\n            wait(0.01)\n        }\n        for (let i = 0; i < 10; i++) {\n            spawnRing(\"normal\", \"#ffaa33\", 700, angle, 2, 0, 0, 40, \"ootama\", \"relative\", \"30\")\n            angle += 12.2\n            wait(0.02)\n        }\n        for (let i = 0; i < 10; i++) {\n            aimAtTarget()\n            angle += random(-7,7)\n            spawnWay(\"normal\", \"#ffdd33\", 700, angle, 5, 25, 0, 0, 20, \"b_ohuda\", \"relative\", \"10\")\n            wait(0.02)\n        }\n        for (let i = 0; i < 10; i++) {\n            angle += 25\n            w = 0\n            sp = 0\n            for (let i = 0; i < 7; i++) {\n                spawnWay(\"normal\", \"#33ff88\", 300 + sp, angle, 1 + w, 2, 0, 0, 20, \"b_poihuru\", \"relative\", \"20\")\n                w += 1\n                sp += 30\n            }\n            wait(0.04)\n        }\n        for (let i = 0; i < 10; i++) {\n            angle = random(-80,80)\n            spawnWay(\"normal\", \"#3388ff\", 700, -90 + angle, 3, 10, 0, 0, 30, \"b_star\", \"relative\", \"20\")\n            wait(0.05)\n        }\n        for (let i = 0; i < 10; i++) {\n            xsp = random(0,768)\n            spawnWay(\"normal\", \"#9E76B4\", 700, 90, 1, 10, 0, 0, 10, \"poihuru\", \"relative\", \"6\")\n            wait(0.02)\n        }\n    }\n}\n    ",
    "bulletScript": "\nif (color==#3388ff) {\n    if (y < 10) {\n        angle = 90\n    }\n    spriteAngle += 5\n}\nif (color==#9E76B4) {\n    once {\n        xsp = random(-200,968)\n        x = xsp\n        y = 0\n    }\n    homing(10)\n    spriteAngle = angle\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "LUNATIC",
    "name": "奇跡「九字切り」",
    "desc": "どうみても早苗のパクリです。本当にありがとうございました。",
    "duration": 60,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 10,
    "emitterScript": "\nwhile (true) {\n    xp = 0\n    for (let i = 0; i < 30; i++) {\n        xp += 60\n        spawnWayResist(\"normal\", \"#ff3333\", 100, -90, 1, 0, xp + ex - 700, ey, 10, \"none\", \"absolute\", \"10\")\n        spawnWayResist(\"normal\", \"#ff3333\", 100, 90, 1, 0, xp + ex - 700, ey, 10, \"none\", \"absolute\", \"10\")\n        wait(0.01)\n    }\n    wait(0.4)\n    yp = 0\n    for (let i = 0; i < 30; i++) {\n        yp += 60\n        spawnWayResist(\"normal\", \"#ff3333\", 100, 0, 1, 0, ex, ey + yp - 700, 10, \"none\", \"absolute\", \"10\")\n        spawnWayResist(\"normal\", \"#ff3333\", 100, 180, 1, 0, ex, ey + yp - 700, 10, \"none\", \"absolute\", \"10\")\n        wait(0.01)\n    }\n    wait(2)\n    mx = random(200,568)\n    tween(\"ex\", ex, mx, \"seconds\", 1)\n    my = random(100,443)\n    tween(\"ey\", ey, my, \"seconds\", 1)\n    wait(1.2)\n}\nonce {\n    w = 0.6\n}\nwhile (true) {\n    spawnRing(\"normal\", \"#ffffff\", 200, fff, 36, 0, 0, 10, \"b_uroko\", \"relative\", \"6\")\n    fff += 5\n    wait(0.4)\n    wait(w)\n    w -= 0.005\n}\n    ",
    "bulletScript": "\nif (color == #ff3333) {\n    speed = 0\n    warningTime = 1\n    activeTime = 2\n    laserWidth = 12\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "LUNATIC",
    "name": "威符「完全無欠の幾何学牢」",
    "desc": "こいしのアレのパクリ",
    "duration": 50,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnWay(\"normal\", \"#ff3333\", 100, 0 + m, 3, 60, 0, ty, 20, \"b_knife\", \"absolute\", \"4\")\n    spawnWay(\"normal\", \"#3388ff\", 100, 180 + m, 3, 60, 768, ty, 20, \"b_knife\", \"absolute\", \"4\")\n    spawnWay(\"normal\", \"#ffdd33\", 100, 90 + m, 3, 60, tx, 0, 20, \"b_knife\", \"absolute\", \"4\")\n    spawnWay(\"normal\", \"#33ff88\", 100, -90 + m, 3, 60, tx, 896, 20, \"b_knife\", \"absolute\", \"4\")\n    wait(0.07)\n}\n    ",
    "bulletScript": "\n        // 弾挙動の独自コード\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "フラッシュアンドブラスト",
    "desc": "マインブラストみたいなのが作ってみたくて...",
    "duration": 44,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    aimAtTarget()\n    for (let i = 0; i < 25; i++) {\n        angle += random(-20,20)\n        spawnBulletResist(\"normal\", \"#ffdd33\", 200, angle, 0, 0, 30, \"light\", \"relative\", \"30\")\n        wait(0.04)\n    }\n    wait(6)\n}\nif (x < 10) {\n}\n    ",
    "bulletScript": "\nif (color==#ffdd33) {\n    if (isTouchEdge) {\n        for (let i = 0; i < 2; i++) {\n            spd = random(100,400)\n            ang = random(0,360)\n            wa = random(3,16)\n            sz = random(5,20)\n            spawnRing(\"normal\", \"#ffaa33\", spd, ang, wa, 0, 0, sz, \"light\", \"relative\", sz)\n        }\n        speed = 0\n        auraIntensity = 2.5\n        for (let i = 0; i < 20; i++) {\n            auraRange += 1\n            auraIntensity -= 0.1\n            hitRadius += 12\n            wait(0.03)\n        }\n        for (let i = 0; i < 10; i++) {\n            auraRange += 1\n            auraIntensity -= 0.1\n            hitRadius -= 8\n            radius -= 2\n            wait(0.03)\n        }\n        y = -8000\n    }\n}\nif (color==#ff3333) {\n    angle += 5\n    spriteAngle = angle\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "EASY",
    "name": "輝く双炎の地",
    "desc": "光弾を使ってみたかった。",
    "duration": 35,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 12000; i++) {\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle2, 0, 0, 25, \"light\", \"relative\", \"25\")\n    angle2 += 6\n    wait(0.02 - t)\n    wait(0.01)\n    t += 0.00001\n}\nfor (let i = 0; i < 12000; i++) {\n    aimAtTarget()\n    spawnBullet(\"normal\", \"#33ffff\", 200, angle, 0, 0, 30, \"light\", \"relative\", \"30\")\n    wait(0.8)\n}\n    ",
    "bulletScript": "\n        // 弾挙動の独自コード\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "雷符「ライトニングスパーク」",
    "desc": "どうみてもマスパ",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    playSound(\"charge\")\n    aimAtTarget()\n    for (let i = 0; i < 150; i++) {\n        spd = random(8500,14000)\n        angle2 += random(0,360)\n        spawnBullet(\"normal\", \"#fffffe\", spd, angle2, 0, 0, 10, \"light\", \"relative\", \"10\")\n        wait(0.01)\n    }\n    wait(0.5)\n    playSound(\"maspa_long\")\n    sp = 1\n    for (let i = 0; i < 5000; i++) {\n        ang = angle\n        ag = angle\n        ang += random(-40,40)\n        spawnBulletResist(\"normal\", \"#ffff99\", 1500, ang, 0, 0, 30, \"light\", \"relative\", \"20\")\n        wait(0.000005)\n        angle += 0.001\n    }\n    sp = 0\n    wait(2)\n}\nwhile (true) {\n    if (cardSecond == 3..757575757) {\n        angle2 += random(0,360)\n        spawnRing(\"normal\", \"#ffdd33\", 300, angle2, 10, 0, 0, 15, \"b_star\", \"relative\", \"7\")\n        wait(0.2)\n    }\n}\n    ",
    "bulletScript": "\nif (color==#fffffe) {\n    if (frame == 2..3) {\n        speed = 320\n        once {\n            angle += 180\n        }\n    }\n    if (frame == 20..3000) {\n        if (x == 374..394) {\n            y = -8000\n        }\n    }\n}\nif (color==#ffff99) {\n    once {\n        tween(\"angle\", angle, ag, \"seconds\", 0.6)\n        auraRange = 5.5\n        auraIntensity = 0.1\n    }\n}\nspriteAngle += 3\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "彩符「天使の光輪」",
    "desc": "結構面白く作れたと思う。難易度はしらん。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 7,
    "emitterScript": "\nfor (let i = 0; i < 50000; i++) {\n    ey = 350\n    for (let i = 0; i < 72; i++) {\n        spawnBullet(\"normal\", \"#ff33aa\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#33ff88\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        henkat = random(1.5,2)\n        spawnBullet(\"normal\", \"#3388ff\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        henkat = random(0.5,1)\n        spawnBullet(\"normal\", \"#ffdd33\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        henkat = random(0.5,1)\n        spawnBullet(\"normal\", \"#ffaa33\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        henkat = random(1.5,2)\n        spawnBullet(\"normal\", \"#ff3333\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n        angle += 5\n    }\n    wait(5)\n}\n    ",
    "bulletScript": "\nif (timer == henkat) {\n    if (color == #ff3333) {\n        once {\n            aimAtTarget()\n        }\n    }\n}\nif (color==#ffdd33) {\n    if (timer == henkat..henkat+3) {\n        angle += 1\n    }\n}\nif (color==#ffaa33) {\n    if (timer == henkat..henkat+3) {\n        angle -= 1\n    }\n}\nif (color==#33ff88) {\n    if (isTouchEdge) {\n        once {\n            bounce()\n        }\n    }\n}\nif (color==#ff33aa) {\n    if (dist < 150) {\n        speed = 100\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「処刑人の剣」",
    "desc": "気合避けスペルを作った。",
    "duration": 40,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnRing(\"normal\", \"#ff3333\", 200, angle, 8, 0, 0, 30, \"sword\", \"relative\", \"6\")\n    spawnRing(\"normal\", \"#ff3333\", 200, -angle, 8, 0, 0, 30, \"sword\", \"relative\", \"6\")\n    angle += 5.2\n    wait(0.05)\n}\nwhile (true) {\n    ex = tx\n}\n    ",
    "bulletScript": "\nspeed += 1\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "橙藍",
    "desc": "とても好み。",
    "duration": 40,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nonce {\n    t = 1.5\n}\nfor (let i = 0; i < 1200; i++) {\n    spawnRingResist(\"normal\", \"#ff5100\", 200, angle, 36, 0, 0, 20, \"light\", \"relative\", \"15\")\n    angle = random(0,360)\n    wait(0.5)\n    wait(t)\n    t -= 0.05\n}\n    ",
    "bulletScript": "\nwait(2)\nspawnRing(\"normal\", \"#6200ff\", 120, angle, 4, 0, 0, 6, \"light\", \"relative\", \"6\")\nwait(2)\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Easy",
    "name": "流符「弾幕の川」",
    "desc": "軌道意味不明！",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 12000; i++) {\n    spawnBullet(\"normal\", \"#ffffff\", 300, ang, 0, 0, 20, \"light\", \"relative\", \"15\")\n    spawnBullet(\"normal\", \"#ffffff\", 300, ang + 180, 0, 0, 20, \"light\", \"relative\", \"15\")\n    spawnBullet(\"normal\", \"#ffffff\", 300, ang + 90, 0, 0, 20, \"light\", \"relative\", \"15\")\n    spawnBullet(\"normal\", \"#ffffff\", 300, ang + 180 + 90, 0, 0, 20, \"light\", \"relative\", \"15\")\n    ang += 4 + random(1,6)\n    wait(0.06)\n}\n    ",
    "bulletScript": "\nif (color == #ffffff) {\n    period = 120\n    amp = 60\n    baseAngle = angle\n    frame2 = 0\n    while (true) {\n        frame2 += 1\n        l += 1\n        angle = baseAngle - amp * sin(frame2 * 360 / period)\n        spriteAngle = angle\n        if (l == 3) {\n            l = 0\n        }\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "核熱「核分裂」",
    "desc": "弾幕作りあるある。最初に作る弾幕を決めるんじゃなくて適当に作ってそれっぽい名前にする",
    "duration": 33,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 6; i++) {\n    ey = 448\n    playSound(\"charge2\")\n    wait(1.15)\n    spawnRingResist(\"normal\", \"#ff3333\", 50, -90, 4 + w, 0, 0, 10, \"light\", \"relative\", \"10\")\n    playSound(\"don00\")\n    wait(3.5)\n    w += 1\n}\n    ",
    "bulletScript": "\nif (timer == 0..0.9) {\n    radius += 1\n    hitRadius += 1\n    speed += 1\n    auraIntensity += 0.2\n}\nif (isTouchEdge) {\n    spawnRing(\"normal\", \"#ff8811\", 200, angle + 5, 36, 0, 0, 30, \"light\", \"relative\", \"25\")\n    y = -80000\n}\n    ",
    "magicCircleScript": "\nif (timer == 0.2..1) {\n    radius -= 0.2\n    hitRadius -= 0.2\n}\n    "
  },
  {
    "difficulty": "EASY",
    "name": "四季符「完全な四種の季節」",
    "desc": "6個も残機あるなら、55秒の超激ムズスペルでも許されますよね...?",
    "duration": 55,
    "maxMisses": 6,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnBullet(\"normal\", \"#3388ff\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ffdd33\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#33ff88\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ff33aa\", 200, angle, 0, 0, 8, \"light\", \"relative\", \"6\")\n    angle = random(40,140)\n    wait(0.05)\n}\nwhile (true) {\n    wait(1)\n    r = random(-50,100)\n    tween(\"ex\", ex, 100 + r, \"seconds\", 0.5, \"easeOut\")\n    r = random(-50,100)\n    tween(\"ey\", ey, 100 + r, \"seconds\", 0.5, \"easeOut\")\n    playSound(\"shot\")\n    if (cardSecond == 20..60) {\n        spawnRing(\"normal\", \"#ffffff\", 200, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n    }\n    wait(0.5)\n    r = random(-50,100)\n    tween(\"ex\", ex, 700 + r, \"seconds\", 0.5, \"easeOut\")\n    r = random(-50,100)\n    tween(\"ey\", ey, 100 + r, \"seconds\", 0.5, \"easeOut\")\n    playSound(\"shot\")\n    if (cardSecond == 20..60) {\n        spawnRing(\"normal\", \"#ffffff\", 200, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n    }\n    wait(0.5)\n    r = random(-50,100)\n    tween(\"ex\", ex, 100 + r, \"seconds\", 0.5, \"easeOut\")\n    r = random(-50,100)\n    tween(\"ey\", ey, 400 + r, \"seconds\", 0.5, \"easeOut\")\n    playSound(\"shot\")\n    if (cardSecond == 20..60) {\n        spawnRing(\"normal\", \"#ffffff\", 200, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n    }\n    wait(0.5)\n    r = random(-50,100)\n    tween(\"ex\", ex, 700 + r, \"seconds\", 0.5, \"easeOut\")\n    r = random(-50,100)\n    tween(\"ey\", ey, 400 + r, \"seconds\", 0.5, \"easeOut\")\n    playSound(\"shot\")\n    if (cardSecond == 20..60) {\n        spawnRing(\"normal\", \"#ffffff\", 200, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n    }\n    wait(0.5)\n    tween(\"ex\", ex, 384, \"seconds\", 0.5, \"easeOut\")\n    tween(\"ey\", ey, 224, \"seconds\", 0.5, \"easeOut\")\n    playSound(\"shot\")\n    if (cardSecond == 20..60) {\n        spawnRing(\"normal\", \"#ffffff\", 200, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n    }\n    wait(4)\n}\nwhile (true) {\n    spawnWayResist(\"normal\", \"#ff3333\", 1000, -90, 25, 7, 0, 0, 30, \"b_knife\", \"relative\", \"15\")\n    wait(0.05)\n}\nwhile (true) {\n    if (cardSecond == 40..60) {\n        aimAtTarget()\n        spawnWay(\"normal\", \"#ffffff\", 300, angle, 8, 12, 0, 0, 12, \"uroko\", \"relative\", \"6\")\n        wait(0.7)\n    }\n}\nwhile (true) {\n    if (cardSecond == 50..60) {\n        aimAtTarget()\n        spawnRing(\"normal\", \"#ffffff\", 300, 0, 36, 0, 0, 8, \"light\", \"relative\", \"6\")\n        wait(0.3)\n    }\n}\n    ",
    "bulletScript": "\nif (timer == 0.8) {\n    if (color == #3388ff) {\n        angle += random(10,15)\n    }\n    if (color == #33ff88) {\n        angle -= random(10,15)\n    }\n    if (color == #ffdd33) {\n        angle += random(-15,15)\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Normal",
    "name": "華符「白銀の結晶」",
    "desc": "たまには展開される系の弾幕も作る。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 8,
    "emitterScript": "\nwhile (true) {\n    playSound(\"shot\")\n    ang = 10\n    for (let i = 0; i < 20; i++) {\n        spawnRing(\"normal\", \"#ffffff\", 100 + spdd, 95, 35, 0, 0, 15, \"b_uroko\", \"relative\", \"1\")\n        spdd += 10\n        ang -= 1\n    }\n    spdd = 0\n    ang = 0\n    wait(1.5)\n    playSound(\"boon01\")\n    wait(1.5)\n    playSound(\"shot\")\n    ang = -10\n    for (let i = 0; i < 20; i++) {\n        spawnRing(\"normal\", \"#ffffff\", 100 + spdd, 95, 35, 0, 0, 15, \"uroko\", \"relative\", \"1\")\n        spdd += 10\n        ang += 1\n    }\n    spdd = 0\n    wait(1.5)\n    playSound(\"boon01\")\n    wait(1.5)\n}\n    ",
    "bulletScript": "\nif (timer ==1.5..1.65) {\n    angle += ang\n}\nif (timer ==0.3) {\n    tween(\"hitRadius\", 1, 8, \"seconds\", 6)\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "EASY",
    "name": "純符「純粋な光の粒子」",
    "desc": "説明文や作成者名など",
    "duration": 20,
    "maxMisses": 0,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnRingResist(\"normal\", \"#ffdd33\", 200, angle, 36, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ffdd33\", 200, angle + 5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ffdd33\", 200, angle + 2.5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ffdd33\", 200, angle + 7.5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    playSound(\"shot\")\n    wait(0.5)\n    spawnRingResist(\"normal\", \"#ac008f\", 200, angle, 36, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ac008f\", 200, angle - 5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ac008f\", 200, angle - 2.5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    spawnRingResist(\"normal\", \"#ac008f\", 200, angle - 7.5, 18, 0, 0, 20, \"light\", \"relative\", \"20\")\n    playSound(\"shot\")\n    wait(0.5)\n}\n    ",
    "bulletScript": "\n        // 弾挙動の独自コード\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "枝符「囚人の牢獄」",
    "desc": "回転＆列抜け",
    "duration": 40,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    playSound(\"shot\")\n    playSound(\"laser\")\n    spawnRing(\"normal\", \"#ffaa33\", 200, 0, 6, 384, 448, 6, \"none\", \"absolute\", \"#ff3333\")\n    wait(2)\n    while (true) {\n        for (let i = 0; i < 38; i++) {\n            spawnBulletResist(\"normal\", \"#ff3333\", 300, 90, xp, 0, 20, \"b_knife\", \"absolute\", \"4\")\n            xp += 20\n            wait(0.16)\n            playSound(\"shot\")\n        }\n        for (let i = 0; i < 38; i++) {\n            spawnBulletResist(\"normal\", \"#ff3333\", 300, 90, xp, 0, 20, \"b_knife\", \"absolute\", \"4\")\n            xp -= 20\n            wait(0.16)\n            playSound(\"shot\")\n        }\n        while (true) {\n            for (let i = 0; i < 38; i++) {\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 90, xp, 0, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, -90, 768 - xp, 896, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 0, 0, yp, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 180, 768, 896 - yp, 20, \"b_knife\", \"absolute\", \"4\")\n                xp += 20\n                yp += 23.5789473684\n                wait(0.16)\n                playSound(\"shot\")\n            }\n            for (let i = 0; i < 38; i++) {\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 90, xp, 0, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, -90, 768 - xp, 896, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 0, 0, yp, 20, \"b_knife\", \"absolute\", \"4\")\n                spawnBulletResist(\"normal\", \"#ff3333\", 300, 180, 768, 896 - yp, 20, \"b_knife\", \"absolute\", \"4\")\n                xp -= 20\n                yp -= 23.5789473684\n                wait(0.16)\n                playSound(\"shot\")\n            }\n        }\n    }\n}\nif (y > 886) {\n}\nwhile (true) {\n    if (cardSecond == 15) {\n        playSound(\"boon01\")\n    }\n}\n    ",
    "bulletScript": "\nif (color == #ffaa33) {\n    warningTime = 0\n    activeTime = 150\n    laserWidth = 20\n    if (cardSecond == 15..40) {\n        angle += 0.2\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "上は洪水、下は大火事(リメイク)",
    "desc": "光弾が実装されたのでリメイク。",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    xsp = random(0,768)\n    spawnBulletResist(\"normal\", \"#ff661f\", 200, angle, xsp, 896, 10, \"light\", \"absolute\", \"10\")\n    wait(0.02)\n}\n    ",
    "bulletScript": "\nif (isTouchEdge) {\n    a = random(-20,20)\n    spawnWay(\"normal\", \"#ff3333\", 200, -90 + a, 1, 30, 0, 0, 6, \"light\", \"relative\", \"6\")\n    speed = 0\n    auraIntensity = 2.5\n    for (let i = 0; i < 20; i++) {\n        auraRange += 2.5\n        auraIntensity -= 0.1\n        hitRadius += 12\n        wait(0.03)\n    }\n    for (let i = 0; i < 20; i++) {\n        auraRange += 1.5\n        auraIntensity -= 0.01\n        hitRadius -= 4\n        radius -= 1\n        wait(0.03)\n    }\n    y = -8000\n}\n    ",
    "magicCircleScript": "\nif (color == #ff3333) {\n    if (y < 0) {\n        angle = 90 + random(-5,5)\n        speed = 5\n        color = #33ffff\n        y = 5\n    }\n}\nif (color == #33ffff) {\n    speed += 1.5\n}\nspriteAngle = angle\n    "
  },
  {
    "difficulty": "Easy",
    "name": "封符「停滞するレッドマジック」",
    "desc": "直近でやった作品の弾幕っぽく作っちゃう病",
    "duration": 40,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 8,
    "emitterScript": "\nwhile (true) {\n    playSound(\"boon01\")\n    tween(\"ey\", 180, 448, \"seconds\", 3, \"easeOut\")\n    wait(3)\n    playSound(\"charge2\")\n    wait(1.5)\n    while (true) {\n        playSound(\"shot\")\n        for (let i = 0; i < 4; i++) {\n            for (let i = 0; i < 2; i++) {\n                spawnBulletResist(\"normal\", \"#ff3333\", 34000, angle, 0, 0, 8, \"b_marutama\", \"relative\", \"5\")\n            }\n            angle += 6\n            wait(0.025)\n        }\n    }\n}\n    ",
    "bulletScript": "\nif (frame == 2..4) {\n    once {\n        speed = 80\n        angle += 180\n        angle += random(-10,10)\n        speed += random(-5,5)\n    }\n}\nif (timer == 7.5) {\n    once {\n        speed = 30\n        color = #dddddd\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "奥義「弾幕結界・陽」",
    "desc": "白くなった奴には当たり判定ありません",
    "duration": 78,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 20,
    "emitterScript": "\nwhile (true) {\n    if (cardSecond == 0.1 + 10 * n) {\n        offset = 0\n        spawnBullet(\"normal\", \"#ffaa33\", 200, agg, 0, 0, 6, \"none\", \"relative\", \"3\")\n        offset = 72 * 1\n        spawnBullet(\"normal\", \"#ffaa33\", 200, agg, 0, 0, 6, \"none\", \"relative\", \"3\")\n        offset = 72 * 2\n        spawnBullet(\"normal\", \"#ffaa33\", 200, agg, 0, 0, 6, \"none\", \"relative\", \"3\")\n        offset = 72 * 3\n        spawnBullet(\"normal\", \"#ffaa33\", 200, agg, 0, 0, 6, \"none\", \"relative\", \"3\")\n        offset = 72 * 4\n        spawnBullet(\"normal\", \"#ffaa33\", 200, agg, 0, 0, 6, \"none\", \"relative\", \"3\")\n    }\n}\nwhile (true) {\n    ey = 448\n    if (cardSecond == 1..50000) {\n        if (cardSecond == 0..15) {\n            spawnRingResist(\"normal\", \"#ff3333\", 260, -agg, 5, 0, 0, 20, \"b_ohuda\", \"relative\", \"6\")\n            playSound(\"shot\")\n            wait(0.15)\n        }\n        if (cardSecond == 5) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 10) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 15) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 20) {\n            playSound(\"boon01\")\n        }\n        if (cardSecond == 25..40) {\n            spawnRingResist(\"normal\", \"#ff3333\", 200, agg, 5, 0, 0, 20, \"b_ohuda\", \"relative\", \"6\")\n            playSound(\"shot\")\n            wait(0.15)\n        }\n        if (cardSecond == 30) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 35) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 40) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 45) {\n            playSound(\"boon01\")\n        }\n        if (cardSecond == 50..65) {\n            spawnRingResist(\"normal\", \"#ff3332\", 160, agg * -10, 5, 0, 0, 20, \"b_ohuda\", \"relative\", \"6\")\n            playSound(\"shot\")\n            wait(0.08)\n        }\n        if (cardSecond == 55) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 60) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 65) {\n            playSound(\"change\")\n        }\n        if (cardSecond == 70) {\n            playSound(\"boon01\")\n        }\n    }\n}\nwhile (true) {\n    tween(\"agg\", 0, 360, \"seconds\", 15)\n    wait(15)\n}\n    ",
    "bulletScript": "\nif (color==#ffaa33) {\n    warningTime = 1\n    activeTime = 4\n    laserWidth = 60\n    angle = e_agg + offset\n}\nif (color !=#ffaa33) {\n    if (cardSecond == 5) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 10) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 15) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 20) {\n        aimAt(ex, ey)\n        color = #ff3333\n        hitRadius = 6\n    }\n    if (cardSecond == 20..25) {\n        speed += 1.5\n    }\n    if (cardSecond == 30) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 35) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 40) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 45) {\n        aimAt(ex, ey)\n        color = #ff3333\n        hitRadius = 6\n    }\n    if (cardSecond == 45..50) {\n        speed += 1.5\n    }\n    if (cardSecond == 55) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 60) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 65) {\n        speed = 0\n        color = #ffffff\n        hitRadius = 0\n    }\n    if (cardSecond == 70) {\n        aimAt(ex, ey)\n        color = #ff3333\n        hitRadius = 6\n    }\n    if (cardSecond == 70..73) {\n        speed += 0.5\n    }\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「常闇幻朧月睨」",
    "desc": "説明文や作成者名など",
    "duration": 30,
    "maxMisses": 1,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    idousakix = random(-200,200)\n    idousakiy = random(-100,100)\n    tween(\"ex\", ex, 384 + idousakix, \"seconds\", 1)\n    tween(\"ey\", ey, 179 + idousakiy, \"seconds\", 1)\n    stop = 0\n    for (let i = 0; i < 40; i++) {\n        spawnRing(\"normal\", \"#ff3333\", 200, angle, 12, 0, 0, 6, \"kome\", \"relative\", \"6\")\n        angle += 4\n        wait(0.02)\n    }\n    wait(0.5)\n    stop = 1\n    kaisi = 0.1\n    kaisi2 = 2\n    for (let i = 0; i < 10; i++) {\n        kaisi += 0.01\n        kaisi2 += 0.1\n        spawnRingResist(\"normal\", \"#3333ff\", 800, angle, 24, 0, 0, 30, \"b_knife\", \"relative\", \"6\")\n        angle += 6.5\n        wait(0.02)\n    }\n    for (let i = 0; i < 10; i++) {\n        kaisi += 0.01\n        kaisi2 += 0.1\n        spawnRingResist(\"normal\", \"#3333ff\", 800, angle, 24, 0, 0, 30, \"b_knife\", \"relative\", \"6\")\n        angle -= -2\n        wait(0.02)\n    }\n    wait(1)\n    stop = 2\n    wait(0.5)\n    stop = 0\n    wait(1.5 - minus)\n    minus += 0.1\n}\n    ",
    "bulletScript": "\nif (color==#ff3333) {\n    if (e_stop == 0) {\n        hitRadius = 12\n        radius = 20\n        speed = 500\n    }\n    if (e_stop == 1) {\n        hitRadius = 0\n        radius = 0\n        speed = 0\n    }\n    if (e_stop == 2) {\n        radius = 20\n        speed = 0\n    }\n}\nif (color==#3333ff) {\n    if (timer == kaisi) {\n        speed = 0\n    }\n    if (timer == kaisi2..9000) {\n        speed = 600\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "否符「生命搾取」",
    "desc": "タッチだと簡単です。",
    "duration": 32,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 2; i++) {\n        wait(1.5)\n        sp = 550\n        ll = 4\n        for (let i = 0; i < 120; i++) {\n            sp -= 5\n            angle += 6 + ll\n            spawnRingResist(\"normal\", \"#ff3333\", 0, angle, 12, 0, 0, 6, \"light\", \"relative\", \"3\")\n            //spawnRingResist(\"normal\", \"#ffffff\", 0, angle, 20, 0, 0, 4, \"none\", \"relative\", \"3\")\n            wait(0.03)\n            ll -= 0.02\n        }\n        wait(2)\n        wait(1.5)\n        sp = 550\n        ll = 4\n        for (let i = 0; i < 120; i++) {\n            sp -= 5\n            angle -= 6 + ll\n            spawnRingResist(\"normal\", \"#3333ff\", 0, angle, 12, 0, 0, 6, \"light\", \"relative\", \"3\")\n            //spawnRingResist(\"normal\", \"#ffffff\", 0, angle, 20, 0, 0, 4, \"none\", \"relative\", \"3\")\n            wait(0.03)\n            ll -= 0.02\n        }\n        wait(2)\n    }\n    wait(20)\n}\n    ",
    "bulletScript": "\nonce {\n    hitRadius = 0\n    advance(sp / 4)\n    speed = 50\n    wait(0.5)\n    hitRadius = 3\n}\nif (timer == 3..6) {\n    speed += 1\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "神「暗殺神」",
    "desc": "全方位反射+弾から出る+自機狙い←神すぎる",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    wait(1)\n    spawnRingResist(\"normal\", \"#ff3333\", 200, 0, 6, 0, 0, 45, \"ootama\", \"relative\", \"20\")\n    wait(9)\n}\n    ",
    "bulletScript": "\nif (cardFrame == 30 * n) {\n    l = angle\n    aimAtTarget()\n    spawnRing(\"normal\", \"#ff3333\", 200, angle, 3, 0, 0, 6, \"light\", \"relative\", \"3\")\n    angle = l\n}\nif (x < 10) {\n    once {\n        bounce()\n    }\n}\nif (x > 758) {\n    once {\n        bounce()\n    }\n}\nif (y < 10) {\n    once {\n        bounce()\n    }\n}\nif (y > 886) {\n    once {\n        angle = -angle\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "模倣「レッドマジック」",
    "desc": "レミリアのそれより圧倒的に簡単。というか、ほぼ下位互換ｗ",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    wait(2)\n    angle = random(0,360)\n    spawnRingResist(\"normal\", \"#ff3333\", 300, 0, 7, 0, 0, 60, \"ootama\", \"relative\", \"40\")\n    playSound(\"shot_raw\")\n    wait(3)\n}\n    ",
    "bulletScript": "\nbounce()\nif (cardFrame == 10 * n) {\n    spangle = random(0,360)\n    spawnBullet(\"normal\", \"#ff3333\", 0, spangle, 0, 0, 10, \"b_marutama\", \"relative\", \"4\")\n}\n    ",
    "magicCircleScript": "\nif (timer == 1..4) {\n    speed += 0.4\n}\n    "
  },
  {
    "difficulty": "h",
    "name": "「唸る秘神の牙」",
    "desc": "ついにへにょりレーザー登場",
    "duration": 20,
    "maxMisses": 0,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nfor (let i = 0; i < 12000; i++) {\n    spawnTrail(\"#00ffff\", 400, angle, 0, 0, 8, 0.2, 0.3, 0.2, \"false\", \"relative\", \"8\")\n    spawnTrail(\"#00ffff\", 400, angle + 90, 0, 0, 8, 0.2, 0.3, 0.2, \"false\", \"relative\", \"8\")\n    spawnTrail(\"#00ffff\", 400, angle + 180, 0, 0, 8, 0.2, 0.3, 0.2, \"false\", \"relative\", \"8\")\n    spawnTrail(\"#00ffff\", 400, angle + 270, 0, 0, 8, 0.2, 0.3, 0.2, \"false\", \"relative\", \"8\")\n    angle += random(0,40)\n    wait(0.2)\n    spawnLaserWay(\"#ff3333\", 6, 450, angle, 2, 45, 0, 0, \"relative\", \"3\")\n}\n    ",
    "bulletScript": "\nperiod = 60 + random(-20,20)\namp = 30\nbaseAngle = angle\nframe2 = 0\nwhile (true) {\n    frame2 += 1\n    l += 1\n    angle = baseAngle - amp * sin(frame2 * 360 / period)\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Lunatic",
    "name": "魔符「殺人の流星群」",
    "desc": "シリンダーフォックスっぽいものを作ったけど多分本家よりムズいです。",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    angle = 6 + offset\n    spawnRingResist(\"normal\", \"#ff3333\", 0, angle, 18 + way, 0, 0, 12, \"star\", \"relative\", \"4\")\n    spawnLaserRingResist(\"#ff3333\", 6, 0, angle, 18 + way, 0, 0, \"relative\", \"4\")\n    wait(3)\n    offset += 3\n    way += 1\n}\n    ",
    "bulletScript": "\nspriteAngle += 7\nif (x < 10) {\n    once {\n        angle = -angle\n        angle += 180\n        speed = 0\n    }\n}\nif (x > 758) {\n    once {\n        angle = -angle\n        angle += 180\n        speed = 0\n    }\n}\nif (y < 10) {\n    once {\n        angle = -angle\n        speed = 0\n    }\n}\nif (y > 886) {\n    once {\n        angle = -angle\n        speed = 0\n    }\n}\nif (speed == 0..400) {\n    speed += 3\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "h",
    "name": "恋符「ミラクルスパーク」",
    "desc": "っぱ、レーザーなんよ。",
    "duration": 40,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 3; i++) {\n        aimAtTarget()\n        spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n        playSound(\"laser_heavy\")\n        wait(2)\n        playSound(\"boon01\")\n        wait(3)\n    }\n    for (let i = 0; i < 3; i++) {\n        aimAtTarget()\n        spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n        playSound(\"laser_heavy\")\n        wait(1)\n        aimAtTarget()\n        spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n        playSound(\"laser_heavy\")\n        wait(1)\n        playSound(\"boon01\")\n        wait(1)\n        playSound(\"boon01\")\n        wait(2)\n    }\n    aimAtTarget()\n    spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n    playSound(\"laser_heavy\")\n    wait(0.6666666667)\n    aimAtTarget()\n    spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n    playSound(\"laser_heavy\")\n    wait(0.6666666667)\n    aimAtTarget()\n    spawnLaserRingResist(\"#ffdd33\", 50, 15, angle, 12, 0, 0, \"relative\", \"10\")\n    playSound(\"laser_heavy\")\n    wait(0.6666666667)\n    playSound(\"boon01\")\n    wait(0.6666666667)\n    playSound(\"boon01\")\n    wait(0.6666666667)\n    playSound(\"boon01\")\n    wait(20)\n}\n    ",
    "bulletScript": "\nif (frame == 2) {\n    speed = 1500\n}\nif (frame == 3..870000) {\n    spangle = random(0,360)\n    spawnBullet(\"normal\", \"#ffdd33\", 1, spangle, 0, 0, 6, \"light\", \"relative\", \"6\")\n    wait(0.007)\n}\n    ",
    "magicCircleScript": "\nif (timer == 2..3) {\n    speed += 2\n}\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「混沌なる狂気」",
    "desc": "予告線ありver",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 8,
    "emitterScript": "\nwhile (true) {\n    angle += random(0,360)\n    wait(3)\n    spawnLaserRingResist(\"#ff3333\", 1, 400, angle, 36, 0, 0, \"relative\", \"0\")\n    wait(0.5)\n    spawnLaserRingResist(\"#33ffff\", 1, 500, angle, 36, 0, 0, \"relative\", \"0\")\n    wait(0.5)\n    spawnLaserRingResist(\"#ff3333\", 6, 400, angle, 36, 0, 0, \"relative\", \"6\")\n    wait(0.5)\n    spawnLaserRingResist(\"#33ffff\", 6, 500, angle, 36, 0, 0, \"relative\", \"6\")\n    wait(0.5)\n    ex = 384 + random(-100,100)\n    ey = 400 + random(-100,100)\n}\n    ",
    "bulletScript": "\nif (color==#ff3333) {\n    offset = 0.8\n}\nif (color==#33ffff) {\n    offset = -0.8\n}\nif (timer == 0..0.5) {\n    angle += 3 * offset\n}\nif (timer == 0.5..1.5) {\n    angle += -2 * offset\n}\nif (timer == 1.5..2.5) {\n    angle += 7 - plus * offset\n    plus += 0.2\n}\nif (timer == 3.5..4) {\n    angle += 4 * offset\n}\nif (timer == 4..4.5) {\n    angle -= 1 * offset\n}\nif (timer == 4.5..5) {\n    angle += 1 * offset\n}\nif (timer == 5..5.5) {\n    angle -= 1\n}\nif (timer == 5.5..6) {\n    angle += 1\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Lunatic",
    "name": "「混沌なる狂気～Lunatic」",
    "desc": "予告線なしver",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 8,
    "emitterScript": "\nwhile (true) {\n    angle += random(0,360)\n    wait(4)\n    spawnLaserRingResist(\"#ff3333\", 6, 400, angle, 36, 0, 0, \"relative\", \"6\")\n    wait(0.5)\n    spawnLaserRingResist(\"#33ffff\", 6, 500, angle, 36, 0, 0, \"relative\", \"6\")\n    wait(0.5)\n    ex = 384 + random(-100,100)\n    ey = 400 + random(-100,100)\n}\n    ",
    "bulletScript": "\nif (color==#ff3333) {\n    offset = 0.8\n}\nif (color==#33ffff) {\n    offset = -0.8\n}\nif (timer == 0..0.5) {\n    angle += 3 * offset\n}\nif (timer == 0.5..1.5) {\n    angle += -2 * offset\n}\nif (timer == 1.5..2.5) {\n    angle += 7 - plus * offset\n    plus += 0.2\n}\nif (timer == 3.5..4) {\n    angle += 4 * offset\n}\nif (timer == 4..4.5) {\n    angle -= 1 * offset\n}\nif (timer == 4.5..5) {\n    angle += 1 * offset\n}\nif (timer == 5..5.5) {\n    angle -= 1\n}\nif (timer == 5.5..6) {\n    angle += 1\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「宝永四年の赤蛇」",
    "desc": "説明文や作成者名など",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 3,
    "emitterScript": "\nwhile (true) {\n    spawnBulletResist(\"normal\", \"#ff3333\", 1, angle, 0, 0, 0, \"none\", \"relative\", \"0\")\n    wait(6)\n}\n    ",
    "bulletScript": "\nif (cardFrame == 2 * n) {\n    spawnBullet(\"normal\", \"#ffffff\", 0, angle, rx, ry, 15, \"b_marutama\", \"relative\", \"0\")\n}\nif (cardFrame == 30 * n) {\n    speed = 1600\n    aimAtTarget()\n}\nspeed += -30\n    ",
    "magicCircleScript": "\nif (timer > 0.5) {\n    hitRadius = 6\n    color = = #ff3333\n}\nif (timer > 6) {\n    y = -80000\n}\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「博麗霊夢のエア結界」",
    "desc": "陰陽玉を陰陽弾無しで作ったｗ",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 348\n    wait(0.2)\n    heru = 600000\n    spawnBullet(\"normal\", \"#ff3332\", 120, angle, 0, 0, 30, \"light\", \"relative\", \"30\")\n    spawnBullet(\"normal\", \"#3332ff\", 120, angle + 180, 0, 0, 30, \"light\", \"relative\", \"30\")\n    for (let i = 0; i < 48; i++) {\n        for (let j = 0; j < 3; j++) {\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 180, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            angle += 1.25\n            ikouangle += 0.25\n        }\n        ikouangle += 3.5\n        wait(0.035)\n        playSound(\"shot\")\n    }\n    heru = 1.6\n    for (let i = 0; i < 52; i++) {\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle - 6, 0, 0, 20, \"ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle + 180 + 6, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 6, 0, 0, 20, \"ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 180 - 6, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n        for (let j = 0; j < 3; j++) {\n            angle += 0.7\n            ikouangle += 2\n            heru -= 0.01\n        }\n        wait(0.03)\n        playSound(\"shot\")\n    }\n    wait(2.2 - hakkyou)\n    wait(1 - hakkyou2)\n    hakkyou += 1\n    hakkyou += 0.5\n}\n    ",
    "bulletScript": "\nif (frame == 60..180) {\n    speed -= 4\n}\nif (frame == 180..190) {\n    speed = 0\n}\nif (frame == 260..305) {\n    once {\n        angle -= 90\n        angle += ikouangle * 3\n        speed = 0\n        spriteAngle = angle\n    }\n    if (color == #ff3332) {\n        angle -= 2\n        spriteAngle = angle\n    }\n    if (color == #3332ff) {\n        angle -= 2\n        spriteAngle = angle\n    }\n    speed = 200\n}\nif (timer == heru) {\n    speed = 0\n}\n// ★ここが究極の軽量化ポイント！\n// 305フレームを超えたら、このスクリプトの計算を「99999秒待機（実質停止）」させる\nif (frame > 305) {\n    wait(99999)\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "lunatic",
    "name": "超人「博麗霊夢」",
    "desc": "「博麗霊夢のエア結界」の難易度上昇版",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 348\n    wait(0.2)\n    heru = 600000\n    spawnBullet(\"normal\", \"#ff3332\", 120, angle, 0, 0, 30, \"light\", \"relative\", \"30\")\n    spawnBullet(\"normal\", \"#3332ff\", 120, angle + 180, 0, 0, 30, \"light\", \"relative\", \"30\")\n    for (let i = 0; i < 48; i++) {\n        for (let j = 0; j < 6; j++) {\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 180, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            angle += 1.25 / 2\n            ikouangle += 0.25\n        }\n        ikouangle += 3.5\n        wait(0.035)\n        playSound(\"shot\")\n    }\n    heru = 1.6\n    for (let i = 0; i < 52; i++) {\n        for (let j = 0; j < 3; j++) {\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle - 6, 0, 0, 20, \"ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#ff3333\", 240, angle + 180 + 6, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 6, 0, 0, 20, \"ohuda\", \"relative\", \"4\")\n            spawnBullet(\"normal\", \"#3333ff\", 240, angle + 180 - 6, 0, 0, 20, \"b_ohuda\", \"relative\", \"4\")\n            angle += 0.7\n            ikouangle += 2\n            heru -= 0.01\n        }\n        wait(0.03)\n        playSound(\"shot\")\n    }\n    wait(2.2 - hakkyou)\n    wait(1 - hakkyou2)\n    hakkyou += 1\n    hakkyou += 0.5\n}\n    ",
    "bulletScript": "\nif (frame == 60..180) {\n    speed -= 4\n}\nif (frame == 180..190) {\n    speed = 0\n}\nif (frame == 260..305) {\n    once {\n        angle -= 90\n        angle += ikouangle * 3\n        speed = 0\n        spriteAngle = angle\n    }\n    if (color == #ff3332) {\n        angle -= 2\n        spriteAngle = angle\n    }\n    if (color == #3332ff) {\n        angle -= 2\n        spriteAngle = angle\n    }\n    speed = 200\n}\nif (timer == heru) {\n    speed = 0\n}\n// ★ここが究極の軽量化ポイント！\n// 305フレームを超えたら、このスクリプトの計算を「99999秒待機（実質停止）」させる\nif (frame > 305) {\n    wait(99999)\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Hard",
    "name": "「天守閣の侍」",
    "desc": "青娥パクリ",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    kaisuu += 1\n    angle = seedrandom[1000 + kaisuu](-160,160)\n    angle2 = seedrandom[100 + kaisuu * 2](-160,160)\n    plusangle = seedrandom[200 + kaisuu](-2,2)\n    for (let i = 0; i < 60; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 400, -90 + angle, 0, 0, 19, \"kome\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#ff3333\", 400, -90 + angle2, 0, 0, 19, \"kome\", \"relative\", \"6\")\n        wait(0.016)\n    }\n}\n    ",
    "bulletScript": "\nif (timer == 0..1) {\n    angle += plusangle\n}\nif (timer == 1) {\n    angle = random(0,360)\n    speed = 300\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「イラプトオブリコイル」",
    "desc": "結構頑張った",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 3; i++) {\n        spawnBeam(1.0, 0, 60, 90 + 60, 0, 0, \"relative\", \"#ff3333\")\n        spawnBeam(1.0, 0, 60, 90 - 60, 0, 0, \"relative\", \"#ff3333\")\n        wait(1)\n        spawnTrailResist(\"#00ffff\", 1000, 90 + 60, 0, 0, 20, 0.2, 2, 0.0001, \"true\", \"relative\", \"10\")\n        spawnTrailResist(\"#00ffff\", 1000, 90 - 60, 0, 0, 20, 0.2, 2, 0.0001, \"true\", \"relative\", \"10\")\n        playSound(\"laser_heavy\")\n        wait(4)\n    }\n    wait(400)\n}\n    ",
    "bulletScript": "\nif (color == #00ffff) {\n    if (frame === 10 * n) {\n        second2 = 1\n        spangle = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 0, spangle, 7, 0, 0, 6, \"b_marutama\", \"relative\", \"6\")\n        second2 = 2\n        spangle = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 0, spangle, 7, 0, 0, 6, \"b_marutama\", \"relative\", \"6\")\n        second2 = 3\n        spangle = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 0, spangle, 7, 0, 0, 6, \"b_marutama\", \"relative\", \"6\")\n    }\n}\nif (second == 15..30000) {\n    wait(99999999)\n}\nbounce()\n    ",
    "magicCircleScript": "\nif (second == second2) {\n    speed = 200\n}\nif (second == 4..30000) {\n    wait(99999999)\n}\n    "
  },
  {
    "difficulty": "HARD",
    "name": "フィーリングウォール",
    "desc": "おｗうｗ",
    "duration": 22.5,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n  while (true) {\n        playSound(\"shot\")\n    wait(0.1)\n    playSound(\"charge2\")\n    wait(2)\n    while (true) {\n        spawnRing(\"normal\", \"#9457eb\", 700, angle, 7, 0, 0, 12, \"light\", \"relative\", \"10\")\n        angle += 45.2\n        playSound(\"shot\")\n        wait(0.016)\n    }\n}\nwhile (true) {\n    muki = 1\n    spawnBullet(\"normal\", \"#ff3333\", 200, 90, 0, 0, 6, \"none\", \"absolute\", \"6\")\n    muki = -1\n    spawnBullet(\"normal\", \"#ff3333\", 200, 90, 768, 0, 6, \"none\", \"absolute\", \"6\")\n    wait(2)\n}\nif (x < 10) {\n}\n    ",
    "bulletScript": "\nif (color==#9457eb) {\n    once {\n        advance(10)\n        auraIntensity = 10\n        auraRange = 7\n        tween(\"auraRange\", auraRange, 2.75, \"seconds\", 0.5)\n    }\n    if (speed == 200..70000) {\n        speed += -2\n    }\n}\nif (color==#ff3333) {\n    once {\n        warningTime = 0.0\n        activeTime = 70\n        laserWidth = 30\n        tanaka = 0\n    }\n    if (muki==1) {\n        x += 0.1\n        x += 5 - tanaka\n        if (tanaka == 0..4.9) {\n            tanaka += 0.1\n        }\n    }\n    if (muki==-1) {\n        x -= 0.1\n        x -= 5 - tanaka\n        if (tanaka == 0..4.9) {\n            tanaka += 0.1\n        }\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Normal",
    "name": "「夢想結界」",
    "desc": "失敗作供養",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 2,
    "emitterScript": "\nrot = 0\nwhile (true) {\n    baseRot = rot\n    rot += 11\n    \n    spawnAngle1 = baseRot\n    spawnAngle2 = -baseRot\n    \n    for (let i = 0; i < 10; i++) {\n        spawnRing(\"normal\", \"#cc33ff\", 160, spawnAngle1, 24, -114, 54, 10, \"ohuda\", \"relative\", \"4\")\n        spawnRing(\"normal\", \"#cc33ff\", 160, spawnAngle1, 24, 64, -80, 10, \"ohuda\", \"relative\", \"4\")\n        spawnRing(\"normal\", \"#3333ff\", 160, spawnAngle2, 24, 114, 54, 10, \"ohuda\", \"relative\", \"5\")\n        spawnRing(\"normal\", \"#3333ff\", 160, spawnAngle2, 24, -64, -80, 10, \"ohuda\", \"relative\", \"5\")\n        \n        spawnAngle1 += 36\n        spawnAngle2 -= 36\n        \n        wait(0.13)\n    }\n    \n    wait(1.2)\n}\n    ",
    "bulletScript": "\nif (param == \"4\") {\n    angle -= 0.35\n}\nif (param == \"5\") {\n    angle += 0.35\n}\n    ",
    "magicCircleScript": ""
  },
  {
    "difficulty": "LUNATIC",
    "name": "飛鉢「伝説の飛空円盤」",
    "desc": "ほぼ完全再現。私の最高記録は10ミスです。",
    "duration": 90,
    "maxMisses": "inf",
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 200,
    "emitterScript": "\n    while(true) {\n    for (let i = 0; i < 2; i++) {\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 18, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 18, 114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 18, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 18, 64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        wait(5)\n\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 18, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 18, 114 * 1.5,  54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 18, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 18, 64 * 1.5,  -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        wait(5)\n    }\n    for (let i = 0; i < 2; i++) {\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 140, baseAngle, 24, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 141, baseAngle, 24,  114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 141, baseAngle, 24, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 140, baseAngle, 24,  64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle += 36\n        wait(5)\n\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 141, baseAngle, 24, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 140, baseAngle, 24,  114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 140, baseAngle, 24, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#33e0ff\", 141, baseAngle, 24,  64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        \n        wait(5)\n    }\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, 114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, 64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        wait(5)\n\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, 114 * 1.5,  54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, 64  * 1.5,  -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        wait(5)\n\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, 114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 141, baseAngle, 32, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#cc33ff\", 140, baseAngle, 32, 64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        wait(7)\n    for (let i = 0; i < 20; i++) {\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 140, baseAngle, 32, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 141, baseAngle, 32,  114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 141, baseAngle, 32, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 140, baseAngle, 32,  64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        \n        wait(3.5)\n\n        aimAtTarget()\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 141, baseAngle, 32, -114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 140, baseAngle, 32,  114 * 1.5, 54 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 140, baseAngle, 32, -64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        baseAngle = random(0,360)\n        spawnRingResist(\"normal\", \"#ff3333\", 141, baseAngle, 32,  64 * 1.5, -80 * 1.5, 20, \"ohuda\", \"relative\", \"10\")\n        \n        wait(3.5)\n    }\n}\n    ",
    "bulletScript": "\n    spriteAngle = angle\nonce {\n    if (speed == 140) { curve = -1.6 }\n    if (speed == 141) { curve = 1.6 }\n    speed = 200\n}\nif (frame < 51) {\n    speed -= 0.7\n    angle += curve\n    y += 0.6\n}\nif (frame == 60..600000) {\n    angle += curve / 6\n    y += 1\n}\n    ",
    "magicCircleScript": ""
  },
  {
    "difficulty": "NORMAL",
    "name": "「天邪鬼の死」",
    "desc": "回転避けはできません",
    "duration": 25,
    "maxMisses": "inf",
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spawnRingResist(\"normal\", \"#ff3333\", 200, angle, 150, 0, 0, 12, \"light\", \"relative\", \"10\")\n    angle += random(0,360)\n    wait(1)\n}\nwhile (true) {\n    playSound(\"shot\")\n    aimAtTarget()\n    spawnWay(\"normal\", \"#33ffff\", 1000, angle, 10, 6, 0, 0, 20, \"kome\", \"relative\", \"6\")\n    wait(0.016)\n}\n    ",
    "bulletScript": "\nonce {\n    if (color != #33ffff) {\n        advance(20)\n        auraRange = 6\n        auraIntensity = 3\n        tween(\"auraRange\", auraRange, 2.75, \"seconds\", 1)\n        tween(\"auraIntensity\", auraIntensity, 1, \"seconds\", 1)\n    }\n    if (color == #33ffff) {\n        advance(10)\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "h",
    "name": "快符「朝の光の中で」",
    "desc": "Ah～朝の光の中で Ah Ah Ah～ 光 Ah～",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 100; i++) {\n        angle += 31\n        spawnWay(\"normal\", \"#ff3333\", 500, angle, 6, 1, 0, 0, 6, \"light\", \"relative\", \"6\")\n        wait(0.01)\n    }\n    for (let i = 0; i < 100; i++) {\n        angle += 30.3\n        spawnWay(\"normal\", \"#ff3333\", 700, angle, 6, 2, 0, 0, 6, \"light\", \"relative\", \"6\")\n        wait(0.01)\n    }\n    for (let i = 0; i < 10; i++) {\n        aimAtTarget()\n        spawnRing(\"normal\", \"#ffaa33\", 900, angle, 21, 0, 0, 40, \"ootama\", \"relative\", \"20\")\n        wait(0.1)\n    }\n    angle = 0\n    for (let i = 0; i < 18; i++) {\n        spawnBullet(\"normal\", \"#ffdd33\", 700, angle, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#ffdd33\", 700, -angle, 0, 0, 6, \"light\", \"relative\", \"6\")\n        angle += 20\n        wait(0.01)\n    }\n    for (let i = 0; i < 5; i++) {\n        aimAtTarget()\n        spd = 0\n        wa = 15\n        for (let i = 0; i < 15; i++) {\n            spawnWay(\"normal\", \"#33ff88\", 500 + spd, angle, wa, 1, 0, 0, 6, \"light\", \"relative\", \"6\")\n            spd += 50\n            wa -= 1\n            wait(0.02)\n        }\n        wait(0.05)\n    }\n    for (let i = 0; i < 30; i++) {\n        muki = 1\n        spawnRing(\"normal\", \"#3388ff\", 800, 0, 18, 0, 0, 20, \"light\", \"relative\", \"15\")\n        muki = -1\n        spawnRing(\"normal\", \"#3388ff\", 800, 0, 18, 0, 0, 20, \"light\", \"relative\", \"15\")\n        wait(0.05)\n    }\n    for (let i = 0; i < 50; i++) {\n        spawnRing(\"normal\", \"#884898\", 800, frame * 3, 18, 0, 0, 20, \"light\", \"relative\", \"15\")\n        wait(0.05)\n    }\n    while (true) {\n        for (let i = 0; i < 2; i++) {\n            for (let i = 0; i < 5; i++) {\n                wait(0.06)\n                rspx = seedrandom[6 + seedcount](0,768)\n                seedcount += 1\n                rsped = 0\n                for (let i = 0; i < 3; i++) {\n                    spawnBullet(\"normal\", \"#ff3333\", 400 + rsped, 90, rspx, 0, 6, \"light\", \"absolute\", \"5\")\n                    rsped += 16\n                }\n                orspx = seedrandom[600 + seedcount](0,768)\n                spawnBullet(\"normal\", \"#ffaa32\", 800, 90, orspx, 0, 9, \"light\", \"absolute\", \"6\")\n                spawnBullet(\"normal\", \"#3387ff\", 800, 90, 758, 0, 10, \"light\", \"absolute\", \"7\")\n                spawnBullet(\"normal\", \"#3387ff\", 800, 90, 10, 0, 10, \"light\", \"absolute\", \"7\")\n            }\n            spawnRing(\"normal\", \"#884898\", 800, frame * 3, 18, 0, 0, 20, \"light\", \"relative\", \"15\")\n        }\n        yelangle += 10\n        spawnLaserRing(\"#ffdd32\", 6, 900, yelangle, 8, 0, 0, 0.2, 0.3, 0.2, \"true\", \"relative\", \"6\")\n        wa = 5\n        spd = 0\n        for (let i = 0; i < 5; i++) {\n            spawnWay(\"normal\", \"#33ff87\", 800 + spd, 90, wa, 1, tx, 0, 6, \"light\", \"absolute\", \"6\")\n            spd += 80\n            wa -= 1\n        }\n    }\n}\n    ",
    "bulletScript": "\nif (color == #ffdd33) {\n    if (isBounced) {\n        bounce()\n        warningTime = 1.0\n        activeTime = 0.7\n        laserWidth = 12\n    }\n}\nif (color == #3388ff) {\n    if (timer == 0..0.7) {\n        angle += 2 * muki\n    }\n}\nif (color == #ffaa32) {\n    once {\n        aimAtTarget()\n    }\n}\nif (color == #3387ff) {\n    once {\n        aimAtTarget()\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "h",
    "name": "快符「朝の光の中で」",
    "desc": "軽量版です",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 100; i++) {\n        angle += 31\n        spawnWay(\"normal\", \"#ff3333\", 500, angle, 6, 1, 0, 0, 6, \"None\", \"relative\", \"6\")\n        wait(0.01)\n    }\n    for (let i = 0; i < 100; i++) {\n        angle += 30.3\n        spawnWay(\"normal\", \"#ff3333\", 700, angle, 6, 2, 0, 0, 6, \"None\", \"relative\", \"6\")\n        wait(0.01)\n    }\n    for (let i = 0; i < 10; i++) {\n        aimAtTarget()\n        spawnRing(\"normal\", \"#ffaa33\", 900, angle, 21, 0, 0, 40, \"ootama\", \"relative\", \"20\")\n        wait(0.1)\n    }\n    angle = 0\n    for (let i = 0; i < 18; i++) {\n        spawnBullet(\"normal\", \"#ffdd33\", 700, angle, 0, 0, 6, \"None\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#ffdd33\", 700, -angle, 0, 0, 6, \"None\", \"relative\", \"6\")\n        angle += 20\n        wait(0.01)\n    }\n    for (let i = 0; i < 5; i++) {\n        aimAtTarget()\n        spd = 0\n        wa = 15\n        for (let i = 0; i < 15; i++) {\n            spawnWay(\"normal\", \"#33ff88\", 500 + spd, angle, wa, 1, 0, 0, 6, \"None\", \"relative\", \"6\")\n            spd += 50\n            wa -= 1\n            wait(0.02)\n        }\n        wait(0.05)\n    }\n    for (let i = 0; i < 30; i++) {\n        muki = 1\n        spawnRing(\"normal\", \"#3388ff\", 800, 0, 18, 0, 0, 20, \"None\", \"relative\", \"15\")\n        muki = -1\n        spawnRing(\"normal\", \"#3388ff\", 800, 0, 18, 0, 0, 20, \"None\", \"relative\", \"15\")\n        wait(0.05)\n    }\n    for (let i = 0; i < 50; i++) {\n        spawnRing(\"normal\", \"#884898\", 800, frame * 3, 18, 0, 0, 20, \"None\", \"relative\", \"15\")\n        wait(0.05)\n    }\n    while (true) {\n        for (let i = 0; i < 2; i++) {\n            for (let i = 0; i < 5; i++) {\n                wait(0.06)\n                rspx = seedrandom[6 + seedcount](0,768)\n                seedcount += 1\n                rsped = 0\n                for (let i = 0; i < 3; i++) {\n                    spawnBullet(\"normal\", \"#ff3333\", 400 + rsped, 90, rspx, 0, 6, \"None\", \"absolute\", \"5\")\n                    rsped += 16\n                }\n                orspx = seedrandom[600 + seedcount](0,768)\n                spawnBullet(\"normal\", \"#ffaa32\", 800, 90, orspx, 0, 9, \"None\", \"absolute\", \"6\")\n                spawnBullet(\"normal\", \"#3387ff\", 800, 90, 758, 0, 10,  \"None\", \"absolute\", \"7\")\n                spawnBullet(\"normal\", \"#3387ff\", 800, 90, 10, 0, 10,   \"None\", \"absolute\", \"7\")\n            }\n            spawnRing(\"normal\", \"#884898\", 800, frame * 3, 18, 0, 0, 20, \"None\", \"relative\", \"15\")\n        }\n        yelangle += 10\n        spawnLaserRing(\"#ffdd32\", 6, 900, yelangle, 8, 0, 0, 0.2, 0.3, 0.2, \"true\", \"relative\", \"6\")\n        wa = 5\n        spd = 0\n        for (let i = 0; i < 5; i++) {\n            spawnWay(\"normal\", \"#33ff87\", 800 + spd, 90, wa, 1, tx, 0, 6, \"None\", \"absolute\", \"6\")\n            spd += 80\n            wa -= 1\n        }\n    }\n}\n    ",
    "bulletScript": "\nif (color == #ffdd33) {\n    if (isBounced) {\n        bounce()\n        warningTime = 1.0\n        activeTime = 0.7\n        laserWidth = 12\n    }\n}\nif (color == #3388ff) {\n    if (timer == 0..0.7) {\n        angle += 2 * muki\n    }\n}\nif (color == #ffaa32) {\n    once {\n        aimAtTarget()\n    }\n}\nif (color == #3387ff) {\n    once {\n        aimAtTarget()\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "星の通常",
    "desc": "よりもムズい。",
    "duration": 20,
    "maxMisses": 1,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    type = 1\n    angle += random(-70,70)\n    bairitu = 1\n    spawnLaserRingResist(\"#ff3333\", 7, 800, angle, 24, 0, 0, 0.2, 0.3, 0.5, \"true\", \"relative\", \"5\")\n    wait(0.5)\n    angle += random(-70,70)\n    bairitu = -1\n    spawnLaserRingResist(\"#ff3333\", 7, 800, angle, 24, 0, 0, 0.2, 0.3, 0.5, \"true\", \"relative\", \"5\")\n    wait(0.5)\n    aimAtTarget()\n    bairitu = 0.085\n    type = 2\n    spawnLaserRingResist(\"#ff3333\", 7, 60, angle + 5, 36, 0, 0, 0.2, 0.3, 0.5, \"true\", \"relative\", \"5\")\n    bairitu = -0.085\n    spawnLaserRingResist(\"#ff3333\", 7, 60, angle + 5, 36, 0, 0, 0.2, 0.3, 0.5, \"true\", \"relative\", \"5\")\n    wait(0.5)\n    bairitu = 0\n    type = 3\n    spawnRing(\"normal\", \"#ffffff\", 400, angle, 37, 0, 0, 18, \"b_marutama\", \"relative\", \"15\")\n    spawnRing(\"normal\", \"#ffffff\", 600, angle, 37, 0, 0, 18, \"b_marutama\", \"relative\", \"15\")\n    spawnRing(\"normal\", \"#ffffff\", 800, angle, 37, 0, 0, 18, \"b_marutama\", \"relative\", \"15\")\n    wait(1)\n    angle += random(-70,70)\n}\n    ",
    "bulletScript": "\nif (timer == 0..1) {\n    angle += 3 * bairitu - minus * bairitu\n    minus += 0.02\n}\nangle += 0.6 * bairitu\nif (type == 2) {\n    speed += 4\n    angle += 1.5 * bairitu\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "バレットドミニオンの消失",
    "desc": "説明文や作成者名など",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\n// 楕円のサイズ設定\ney = 248\na = 150\nb = 50\n// ボスからのズレ具合（偏り）\nshiftX = 75\n// 角度の初期化\ntheta = 0\nwhile (true) {\n    // 1. 基本の楕円のX, Y座標を計算し、shiftXぶんズラす\n    cx = a * cos(theta)\n    cy = b * sin(theta)\n    rx = cx + shiftX\n    ry = cy\n    // 2. 0 から 7 まで 8回 繰り返す\n    for (let i = 0; i < 8; i++) {\n        // 角度の計算\n        dir = i * 45\n        // ★ i の数字（方向）に合わせて色を変数 c に入れる\n        c = \"#ffffff\" // 初期値\n        if (i == 0) {\n            c = \"#ff3333\"\n        }\n        // 1方向目：赤\n        if (i == 1) {\n            c = \"#ff9933\"\n        }\n        // 2方向目：橙\n        if (i == 2) {\n            c = \"#ffff33\"\n        }\n        // 3方向目：黄\n        if (i == 3) {\n            c = \"#33ff33\"\n        }\n        // 4方向目：緑\n        if (i == 4) {\n            c = \"#33ffff\"\n        }\n        // 5方向目：シアン\n        if (i == 5) {\n            c = \"#3333ff\"\n        }\n        // 6方向目：青\n        if (i == 6) {\n            c = \"#9933ff\"\n        }\n        // 7方向目：紫\n        if (i == 7) {\n            c = \"#ff33cc\"\n        }\n        // 8方向目：ピンク\n        // 数学の「回転行列」で、rxとryを dir 度ぶん回転させる\n        rotX = rx * cos(dir) - ry * sin(dir)\n        rotY = rx * sin(dir) + ry * cos(dir)\n        // 3. ボスの現在位置(ex, ey)を足して、実際の出現座標を決定\n        ox = ex + rotX\n        oy = ey + rotY\n        // 4. 計算した座標に配置（ここで変数 c を使う）\n        ofangle -= 2\n        spawnBullet(\"normal\", c, 0, theta + dir, ox, oy, 20, \"kome\", \"absolute\", \"5\")\n    }\n    // 5. 角度の更新\n    theta += 12\n    wait(0.02)\n}\n    ",
    "bulletScript": "\nspeed += 1 + kasoku\nkasoku += 0.02\nonce {\n    angle += 45 + ofangle\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "lunatic",
    "name": "「業火優勢」",
    "desc": "通常版",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    wait(0.1)\n    while (true) {\n        angle += seedrandom[50 + kaisu](0,500)\n        kaisu += 1\n        angle += 9 * kakudo\n        kakudo = 1.2\n        spawnWay(\"normal\", \"#ff3333\", 300, angle, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 90, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 180, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 270, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        for (let i = 0; i < 9; i++) {\n            spawnWay(\"normal\", \"#ff3332\", 300, angle + 45, 4, 90, 0, 0, 6, \"light\", \"relative\", \"6\")\n            angle += 3\n            wait(0.1)\n        }\n        angle += seedrandom[50 + kaisu](0,500)\n        kaisu += 1\n        angle += 9 * kakudo\n        kakudo = -1.2\n        spawnWay(\"normal\", \"#ff3333\", 300, angle, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 90, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 180, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        spawnWay(\"normal\", \"#ff3333\", 300, angle + 270, 25, 3, 0, 0, 6, \"light\", \"relative\", \"6\")\n        for (let i = 0; i < 9; i++) {\n            spawnWay(\"normal\", \"#ff3332\", 300, angle + 45, 4, 90, 0, 0, 6, \"light\", \"relative\", \"6\")\n            angle -= 3\n            wait(0.1)\n        }\n    }\n}\nwhile (true) {\n    wait(0.5)\n    spawnLaserWay(\"#33ffff\", 6, 800, -90 + 50, 7, 10, 0, 0, 0.1, 0.01, 0.1, \"true\", \"relative\", \"6\")\n    spawnLaserWay(\"#33ffff\", 6, 800, -90 - 50, 7, 10, 0, 0, 0.1, 0.01, 0.1, \"true\", \"relative\", \"6\")\n    wait(0.4)\n}\n    ",
    "bulletScript": "\nif (color!=#33ffff) {\n    angle += 0.4 * kakudo - l * kakudo\n    l += 0.000\n    once {\n        speed = 100\n    }\n    speed += 0.5\n}\nif (color==#33ffff) {\n    if (frame == 20..50) {\n        speed -= 15\n        homing(360)\n    }\n    if (frame == 50..51) {\n        aimAtTarget()\n    }\n    if (frame == 50..120) {\n        speed += 10\n    }\n}\nradius = 10\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「回天之勢」",
    "desc": "ドパガキ向け！",
    "duration": 15,
    "maxMisses": 1,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 10; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 200, angle, 0, 0, 30, \"b_knife\", \"relative\", \"6\")\n        angle += angled\n        wait(0.016 / 8)\n    }\n    angled = seedrandom[second](-5000,10)\n}\n    ",
    "bulletScript": "\nspeed += 2\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "Lunatic",
    "name": "「嵐光明媚」",
    "desc": "説明文や作成者名など",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    kakudo = 1\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle, 0, 0, 13, \"b_uroko\", \"relative\", \"6\")\n    kakudo = -1\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle, 0, 0, 13, \"b_uroko\", \"relative\", \"6\")\n    wait(0.02)\n}\n    ",
    "bulletScript": "\nonce {\n    x = random(-300,1000)\n    y = 0\n    angle = 90 + 10 * kakudo\n    spriteAngle = angle\n}\nspeed += 5\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "魔砲「大葬滅光線」",
    "desc": "先に作るものを決めてから作り始めた極めて珍しい例の弾幕。",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spd = 0\n    for (let i = 0; i < 10; i++) {\n        spawnRingResist(\"normal\", \"#ff3333\", 50 + spd, angle, 1, 200, 0, 6, \"light\", \"relative\", \"0\")\n        spawnRingResist(\"normal\", \"#ff3333\", 50 + spd, angle, 1, -200, 0, 6, \"light\", \"relative\", \"0\")\n        angle += 10\n        spd += 2.5\n    }\n    spawnTrailResist(\"#ffffff\", 1, angle, 200, 0, 2, 0.001, 2, 0.001, \"true\", \"relative\", \"0\")\n    spawnTrailResist(\"#ffffff\", 1, angle, -200, 0, 2, 0.001, 2, 0.001, \"true\", \"relative\", \"0\")\n    spawnTrailResist(\"#33ffff\", 0, angle, 200, 0, 30, 0.3, 0.6, 1, \"true\", \"relative\", \"0\")\n    spawnTrailResist(\"#33ffff\", 0, angle, -200, 0, 30, 0.3, 0.6, 1, \"true\", \"relative\", \"0\")\n    for (let i = 0; i < 50; i++) {\n        spawnRingResist(\"normal\", \"#ff3333\", 50 + spd, angle, 1, 200, 0, 6, \"light\", \"relative\", \"0\")\n        spawnRingResist(\"normal\", \"#ff3333\", 50 + spd, angle, 1, -200, 0, 6, \"light\", \"relative\", \"0\")\n        angle += 10\n        spd += 2.5\n    }\n    wait(2)\n}\n    ",
    "bulletScript": "\nif (color == #ff3333) {\n    once {\n        advance(120 + seedrandom[5](-0,50))\n        angle += 180\n    }\n    if (frame == 70) {\n        hitRadius = 5\n        idoukakudo = random(-0.3,0.3)\n        speed += random(0,200)\n    }\n    if (frame == 80..) {\n        angle += idoukakudo\n    }\n}\nif (color==#ffffff) {\n    once {\n        aimAtTarget()\n        x += offsetx\n        y += offsety\n    }\n    if (frame == 2) {\n        speed = 8000\n    }\n}\nif (color==#33ffff) {\n    once {\n        aimAtTarget()\n        x += offsetx\n        y += offsety\n    }\n    if (frame == 70) {\n        speed = 80000\n        hitRadius = 20\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "「ミニ弾幕結界]",
    "desc": "だいーぶ前に作ったやつ",
    "duration": 30,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    ey = 448\n    wait(0.03)\n    ey = 448\n    if (cardSecond == 0..10) {\n        spawnWay(\"normal\", \"#ff3333\", 200, angle, 1, 120, 0, 0, 12, \"b_uroko\", \"relative\", \"6\")\n        angle += 4\n    }\n    if (cardSecond == 10..20) {\n        spawnWay(\"normal\", \"#ff3333\", 200, angle, 2, 180, 0, 0, 12, \"b_uroko\", \"relative\", \"6\")\n        angle += 2\n    }\n    if (cardSecond == 20..30) {\n        spawnWay(\"normal\", \"#ff3333\", 200, angle, 3, 120, 0, 0, 12, \"b_uroko\", \"relative\", \"6\")\n        angle += 1.5\n    }\n    if (cardFrame == 5 * n) {\n        playSound(\"shot\")\n    }\n}\n    ",
    "bulletScript": "\nonce {\n    count = 0\n    angle += random(-10,10)\n}\nif (color == #ffffff) {\n    y = -80000\n}\nif (y < 0) {\n    if (count == 1) {\n        color = #ffffff\n        y = -8000\n    }\n    count = 1\n    y = 886\n}\nif (color == #ffffff) {\n    y = -80000\n}\nif (y > 896) {\n    if (count == 1) {\n        color = #ffffff\n        y = -8000\n    }\n    count = 1\n    y = 10\n}\nif (color == #ffffff) {\n    y = -80000\n}\nif (x < 0) {\n    if (count == 1) {\n        color = #ffffff\n        y = -8000\n    }\n    count = 1\n    x = 758\n}\nif (color == #ffffff) {\n    y = -80000\n}\nif (x > 768) {\n    if (count == 1) {\n        color = #ffffff\n        y = -8000\n    }\n    count = 1\n    x = 10\n}\nif (color == #ffffff) {\n    y = -80000\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「やんごとある威光」",
    "desc": "過去に作ったやつ",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    if (cardSecond == 0..5) {\n        offset = 1\n        spawnRing(\"normal\", \"#ff3333\", 50, angle, 26, 0, 0, 6, \"virus\", \"relative\", \"6\")\n        angle = random(0,20)\n        wait(0.05)\n    }\n    if (cardSecond == 4..10) {\n        offset = -1\n        spawnRing(\"normal\", \"#ff3333\", 50, angle, 26, 0, 0, 6, \"virus\", \"relative\", \"6\")\n        angle = random(0,20)\n        wait(0.05)\n    }\n    if (cardSecond == 10..18) {\n        offset = 1\n        spawnRing(\"normal\", \"#ff3333\", 50, angle, 26, 0, 0, 6, \"virus\", \"relative\", \"6\")\n        angle = random(0,20)\n        wait(0.1)\n        offset = -1\n        spawnRing(\"normal\", \"#ff3333\", 50, angle, 26, 0, 0, 6, \"virus\", \"relative\", \"6\")\n        angle = random(0,20)\n        wait(0.1)\n    }\n}\n    ",
    "bulletScript": "\nif (timer == 0..5) {\n    angle += 0.2 * offset\n}\nspeed = 300\nspriteAngle += 7\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「サイド暗器」",
    "desc": "過去作ったやつ",
    "duration": 18,
    "maxMisses": 0,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spx = 300\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle, spx, -160, 15, \"light\", \"relative\", \"6\")\n    spawnBullet(\"normal\", \"#ff3333\", 200, angle + 180, -spx, 850, 15, \"light\", \"relative\", \"6\")\n    wait(0.03)\n    wait(0.1-hindo)\n    hindo += 0.001\n}\n    ",
    "bulletScript": "\nonce {\n    y -= 100\n    speed = 400\n    x += random(-200,200)\n    y += random(-70,70)\n    speed += random(-70,70)\n    angle += random(-3,3)\n}\nspeed += 1\naif[20](y == ty) {\n    once {\n        speed = 0\n        angle = 90\n        if (x < tx) {\n            angle = 0\n        }\n        if (x > tx) {\n            angle = 180\n        }\n        spawnBullet(\"normal\", \"#ff3333\", 100, angle, 0, 0, 30, \"knife\", \"relative\", \"6\")\n        y = -80000\n    }\n}\n    ",
    "magicCircleScript": "\nspeed += 1\nonce {\n    angle += random(-6,6)\n}\n    "
  },
  {
    "difficulty": "Hard",
    "name": "「弾幕の乱」",
    "desc": "ラグテスト用の弾幕を改造してできた。",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    offset += 0\n    for (let i = 0; i < 8; i++) {\n        offset += 40\n        offset = random(0,360)\n        spawnBulletResist(\"normal\", \"#ff3333\", 80, angle, 384, 600, 15, \"kome\", \"absolute\", \"3\")\n    }\n    offset = 90\n    spawnBulletResist(\"normal\", \"#ff3333\", 320, angle, 384, 600, 30, \"knife\", \"absolute\", \"12\")\n    angle += 0.8\n    wait(0.016)\n}\n    ",
    "bulletScript": "\nonce {\n    advance(200)\n    angle += offset\n    spriteAngle = angle\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "「アリュージョンクロック」",
    "desc": "なんと、1000発程度が飛び交う。物量弾幕",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    wait(0.1)\n    for (let i = 0; i < 36; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 500, -angle, 0, 0, 8, \"dangan\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#ffdd33\", 400, angle, 0, 0, 8, \"dangan\", \"relative\", \"6\")\n        spawnBullet(\"normal\", \"#33ff88\", 300, -angle, 0, 0, 8, \"dangan\", \"relative\", \"6\")\n        angle += 5\n    }\n    angle += 160.5\n}\n    ",
    "bulletScript": "\n        // 弾挙動の独自コード\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "normal",
    "name": "「幻視狂言(ファントムディセプション)」",
    "desc": "説明文や作成者名など",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    kaisuu = 0\n    for (let i = 0; i < 700; i++) {\n        spawnBullet(\"normal\", \"#ff3333\", 300 + spd, angle, 0, 0, 10, \"kunai2\", \"relative\", \"3\")\n        angle += seedrandom[kaisuu](0,360)\n        spd = seedrandom[kaisuu](-250,150)\n        kaisuu += 1\n    }\n    wait(2)\n    kaisuu = 0\n    for (let i = 0; i < 700; i++) {\n        spawnBullet(\"normal\", \"#3388ff\", 300 + spd, angle, 0, 0, 10, \"kunai2\", \"relative\", \"3\")\n        angle += seedrandom[kaisuu](0,360)\n        spd = seedrandom[kaisuu](-250,150)\n        kaisuu += 1\n    }\n    wait(2)\n}\n    ",
    "bulletScript": "\nonce {\n    motospd = speed\n    motocolor = color\n}\nif (frame == 120..121) {\n    speed = 0\n    radius = 0\n    hitRadius = 0\n    color = #aaaaaa\n}\nif (frame == 200..241) {\n    radius = 10\n}\nif (frame == 240..241) {\n    speed = 300\n    color = motocolor\n    hitRadius = 3\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "normal",
    "name": "「ワカサギペンデュラム」",
    "desc": "ナズーリンペンデュラム、好きです。",
    "duration": 28,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    ey = 300\n    spawnRing(\"normal\", \"#ff3333\", 200, angle, 6, 0, 0, 10, \"dangan\", \"relative\", \"6\")\n    spawnRing(\"normal\", \"#3388ff\", 200, kasu, 36, 0, 0, 10, \"dangan\", \"relative\", \"7\")\n    kasu += 2\n    wait(0.0334)\n}\nwhile (true) {\n    tween(\"syutugen\", syutugen, 500, \"seconds\", 2, \"easeInOut\")\n    wait(2)\n    tween(\"syutugen\", syutugen, -100, \"seconds\", 2, \"easeInOut\")\n    wait(2)\n}\nwhile (true) {\n    tween(\"angle\", angle, 360, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, -360, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, 0, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, -360, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, 360, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, 720, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n    tween(\"angle\", angle, 0, \"seconds\", 4, \"easeInOut\")\n    wait(4)\n}\n    ",
    "bulletScript": "\nonce {\n    if (color==#ff3333) {\n        advance(syutugen)\n    }\n    if (color==#3388ff) {\n        advance(600)\n        spriteAngle = angle\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "easy",
    "name": "「ソーメンオブナガシ」",
    "desc": "あまりにも神弾幕すぎる...",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 3; i++) {\n        spawnRing(\"normal\", \"#dddddd\", 200, angle, 36, 0, 0, 10, \"kunai2\", \"relative\", \"6\")\n        angle += 0.6\n        wait(0.0835)\n    }\n    for (let i = 0; i < 3; i++) {\n        spawnRing(\"normal\", \"#dddddd\", 200, angle, 36, 0, 0, 10, \"kunai2\", \"relative\", \"6\")\n        angle -= 0.6\n        wait(0.0835)\n    }\n    angle += 5.5555555555\n}\n    ",
    "bulletScript": "\nif (frame == 30 * n) {\n    angle += 60\n    spriteAngle = angle\n    wait(0.5)\n    angle -= 60\n    spriteAngle = angle\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "夢符「封印結界」",
    "desc": "固定弾",
    "duration": 15,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spx = 0\n    for (let i = 0; i < 38; i++) {\n        kaisuu += 1\n        spawnBullet(\"normal\", \"#ff3333\", 200, 90, spx, 0, 20, \"ohuda\", \"absolute\", \"10\")\n        spx += 20\n    }\n    wait(0.5)\n}\n    ",
    "bulletScript": "\nonce {\n    ransuu = seedrandom[5 + kaisuu](0,1)\n    if (ransuu == 0.5..1) {\n        speed = 150\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「花火大会」",
    "desc": "引き延ばすやつめっちゃ良いね！おもろい。",
    "duration": 20,
    "maxMisses": 1,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    for (let i = 0; i < 8; i++) {\n        spx = random(-400,400)\n        spy = random(-200,200)\n        angle = random(0,360)\n        spawnRing(\"normal\", \"#ff3333\", 0, angle, 12, spx, spy, 20, \"redsimple\", \"relative\", \"10\")\n    }\n    wait(1)\n}\n    ",
    "bulletScript": "\nonce {\n    advance(10)\n    tween(\"multf\", 0, 5, \"seconds\", 0.8)\n    tween(\"hitmultf\", 0, 10, \"seconds\", 0.8)\n    tween(\"multlr\", 0, 0.4, \"seconds\", 0.8)\n    wait(0.4)\n    speed = 400\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "hard",
    "name": "「乱刃陣」",
    "desc": "引き延ばしレーザー、やっぱ見た目がそれっぽくてとてもいい。",
    "duration": 20,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 5,
    "emitterScript": "\nwhile (true) {\n    kaisuu += 1\n    angle1 = seedrandom[kaisuu + 1](0,360)\n    angle2 = seedrandom[kaisuu + 2](0,360)\n    angle3 = seedrandom[kaisuu + 3](0,360)\n    angle4 = seedrandom[kaisuu + 4](0,360)\n    spawnRing(\"normal\", \"#fffffe\", 0, angle1, 18, 200, -50, 20, \"whitesimple\", \"relative\", \"0\")\n    spawnRing(\"normal\", \"#fffffe\", 0, angle2, 18, -200, -50, 20, \"whitesimple\", \"relative\", \"0\")\n    spawnRing(\"normal\", \"#fffffe\", 0, angle3, 18, 200, 200, 20, \"whitesimple\", \"relative\", \"0\")\n    spawnRing(\"normal\", \"#fffffe\", 0, angle4, 18, -200, 200, 20, \"whitesimple\", \"relative\", \"0\")\n    wait(0.5)\n    spawnRing(\"normal\", \"#ffffff\", 0, angle1, 18, 200, -50, 20, \"redsimple\", \"relative\", \"10\")\n    spawnRing(\"normal\", \"#ffffff\", 0, angle2, 18, -200, -50, 20, \"redsimple\", \"relative\", \"10\")\n    spawnRing(\"normal\", \"#ffffff\", 0, angle3, 18, 200, 200, 20, \"redsimple\", \"relative\", \"10\")\n    spawnRing(\"normal\", \"#ffffff\", 0, angle4, 18, -200, 200, 20, \"redsimple\", \"relative\", \"10\")\n    wait(0.5)\n    for (let i = 0; i < 4; i++) {\n        kaisuu2 += 1\n        r1 = seedrandom[kaisuu2](0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, r1, 36, 200, -50, 20, \"blueknife\", \"relative\", \"6\")\n        r1 = seedrandom[kaisuu2 + 5](0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, r1, 36, -200, -50, 20, \"blueknife\", \"relative\", \"6\")\n        r1 = seedrandom[kaisuu2 + 10](0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, r1, 36, 200, 200, 20, \"blueknife\", \"relative\", \"6\")\n        r1 = seedrandom[kaisuu2 + 15](0,360)\n        spawnRing(\"normal\", \"#ff3333\", 300, r1, 36, -200, 200, 20, \"blueknife\", \"relative\", \"6\")\n        wait(0.167 * 5)\n    }\n    wait(0.1)\n}\n    ",
    "bulletScript": "\nif (color==#ffffff) {\n    once {\n        advance(-100)\n        multf = 30\n        hitmultf = 80\n        hitmultlr = 0\n        hitRadius = 0\n        tween(\"multlr\", 0, 1, \"seconds\", 1, \"easeOut\")\n        wait(1.5)\n        hitRadius = 10\n        tween(\"hitmultlr\", 0, 1, \"seconds\", 1, \"easeOut\")\n        wait(0.5)\n        tween(\"hitmultlr\", 1, 0, \"seconds\", 1, \"easeIn\")\n        wait(0.5)\n        tween(\"multlr\", 1, 0, \"seconds\", 1, \"easeIn\")\n        wait(0.5)\n        hitRadius = 0\n        wait(0.5)\n        y = -9000\n    }\n}\nif (color==#fffffe) {\n    once {\n        advance(-100)\n        multf = 30\n        multlr = 0.1\n        wait(0.6)\n        y = -90000\n    }\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "淼符「双瀑流」",
    "desc": "りゅうたい！！！",
    "duration": 25,
    "maxMisses": 2,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 40,
    "emitterScript": "\nwhile (true) {\n    spawnBullet(\"normal\", \"#33ffff\", 200, 0 + l, -400, 200, 6, \"light\", \"relative\", \"3\")\n    spawnBullet(\"normal\", \"#3388ff\", 200, 180 - l, 400, 200, 6, \"light\", \"relative\", \"3\")\n    spawnBullet(\"normal\", \"#33ffff\", 200, 80, -400, 200, 6, \"light\", \"relative\", \"3\")\n    spawnBullet(\"normal\", \"#3388ff\", 200, 180 - 80, 400, 200, 6, \"light\", \"relative\", \"3\")\n    wait(0.0167 * 3)\n}\nwhile (true) {\n    tween(\"l\", l, -90, \"seconds\", 2)\n    wait(2)\n    tween(\"l\", l, 60, \"seconds\", 2)\n    wait(2)\n}\n    ",
    "bulletScript": "\nonce {\n    yjiku = -1 + random(-0.4,0.4)\n    speed += random(-40,40)\n    angle += random(-10,10)\n}\ny += yjiku\nyjiku += 0.02\nif (speed == 10..5000) {\n    speed -= 0.2\n}\nif (y > 870) {\n    y = -890000\n}\n    ",
    "magicCircleScript": "\n        // 子弾挙動の独自コード（任意）\n    "
  }
];
window.sharedDanmakuList = window.compiledDanmakuList;
window.compiledDanmakuListS2 = [
  {
    "difficulty": "NORMAL",
    "name": "【S2サンプル】瞬光スパイラルバースト",
    "desc": "S2開幕記念サンプル！HPを削って速攻を狙おう！",
    "hp": 1000,
    "duration": 25,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    aimAtTarget()\n    spawnRing(\"normal\", \"#33ccff\", 180, angle, 6, 0, 0, 8, \"star\", \"relative\", 6)\n    spawnRing(\"normal\", \"#ff3388\", 140, angle + 30, 6, 0, 0, 8, \"star\", \"relative\", 6)\n    wait(0.18)\n}\n        ",
    "bulletScript": "\nspeed = 180\nif(color!=#ffdd00) {\n    if (isBounced) {\n        color = #ffdd00\n        aimAtTarget()\n    }\n}\n        ",
    "magicCircleScript": ""
  },
  {
    "difficulty": "Hard",
    "name": "天秤「光魔のペンデュラム」",
    "desc": "ペンデュラムって天秤って意味なんだね...",
    "hp": 2000,
    "duration": 30,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    once {\n        spawnRingResist(\"normal\", \"#888888\", 0, 0, 9, 0, 0, 20, \"greenscale\", \"relative\", \"20\")\n        spawnRingResist(\"normal\", \"#000000\", 0, 20, 9, 0, 0, 20, \"yellowscale\", \"relative\", \"20\")\n    }\n    spangle += 0.6\n    wait(0.0167)\n}\nwhile (true) {\n    wait(2.0)\n    while (true) {\n        spawnRing(\"normal\", \"#ff3333\", 200, spangle, 9, 0, 0, 8, \"goldbig\", \"relative\", \"3\")\n        spawnRing(\"normal\", \"#ffaa33\", 200, -spangle + 20, 9, 0, 0, 9, \"orangebig\", \"relative\", \"3\")\n        wait(0.0167 * 3)\n    }\n}\nwhile (true) {\n    tween(\"mukukakudo\", 0, 180, \"seconds\", 3, \"easeInOut\")\n    wait(1.5)\n    tween(\"mukukakudo2\", 180, 0, \"seconds\", 3, \"easeInOut\")\n    wait(1.5)\n    tween(\"mukukakudo\", 180, 0, \"seconds\", 3, \"easeInOut\")\n    wait(1.5)\n    tween(\"mukukakudo2\", 0, 180, \"seconds\", 3, \"easeInOut\")\n    wait(1.5)\n}\n    ",
    "bulletScript": "\nif (color==#888888) {\n    once {\n        tween(\"multf\", 0, 7, \"seconds\", 0.6, \"easeOut\")\n        tween(\"hitmultf\", 0, 8, \"seconds\", 0.6, \"easeOut\")\n        tween(\"multlr\", 0, 0.8, \"seconds\", 0.6, \"easeOut\")\n        advance(150)\n    }\n    advance(-150)\n    angle += 0.6\n    spriteAngle = angle\n    advance(150)\n}\nif (color==#000000) {\n    once {\n        tween(\"multf\", 0, 3, \"seconds\", 0.6, \"easeOut\")\n        tween(\"hitmultf\", 0, 6, \"seconds\", 0.6, \"easeOut\")\n        tween(\"multlr\", 0, 0.6, \"seconds\", 0.6, \"easeOut\")\n        advance(90)\n    }\n    advance(-90)\n    angle -= 0.6\n    spriteAngle = angle\n    advance(90)\n}\nif (color==#ff3333) {\n    once {\n        advance(180)\n        angle = mukukakudo\n        angle += random(-3,3)\n    }\n}\nif (color==#ffaa33) {\n    once {\n        advance(90)\n        angle = mukukakudo2\n        angle += random(-3,3)\n    }\n}\n    ",
    "magicCircleScript": "\n\n    "
  },
  {
    "difficulty": "easy",
    "name": "天祝「アポロ11」",
    "desc": "やっぱZUNの弾幕パクっただけあってそこそこおもろい。",
    "hp": 2000,
    "duration": 50,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 4,
    "emitterScript": "\nwhile (true) {\n    angle = -90\n    kawaru = 0\n    kawaru2 = 0.6012\n    for (let i = 0; i < 36; i++) {\n        spd = 0\n        ang = 0\n        for (let i = 0; i < 10; i++) {\n            spawnWay(\"normal\", \"#ff3333\", 0 + spd, angle + ang, 1, 15, 0, 0, 6, \"reddiamond\", \"relative\", \"6\")\n            spawnWay(\"normal\", \"#3388ff\", 200 + spd, angle + ang + 7.5, 1, 15, 0, 0, 6, \"bluediamond\", \"relative\", \"6\")\n            spd += 20\n            ang += -5\n        }\n        angle += 10\n        kawaru += 0.0167\n        kawaru2 -= 0.0167\n    }\n    wait(1.67)\n    wait(1.67)\n    for (let i = 0; i < 10; i++) {\n        wait(0.167)\n        spawnRing(\"normal\", \"#888888\", 300, angle, 12, 0, 0, 30, \"redbig2\", \"relative\", \"20\")\n        angle = random(0,360)\n    }\n}\n    ",
    "bulletScript": "\nif (color!=#888888) {\n    once {\n        advance(50)\n        tween(\"speed\", speed, 1, \"seconds\", 2.5, \"easeOut\")\n        wait(2.3)\n        if (color==#ff3333) {\n            wait(kawaru)\n        }\n        if (color==#3388ff) {\n            wait(kawaru2)\n        }\n        imageTo(\"redeye\")\n        angle += 160\n        if (color==#3388ff) {\n            imageTo(\"blueeye\")\n            angle += 40\n        }\n        if (color==#ff3333) {\n            wait(1 - kawaru)\n        }\n        if (color==#3388ff) {\n            wait(1 - kawaru2)\n        }\n        speed = 200\n    }\n}\n    ",
    "magicCircleScript": "\n\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "「タケミナカタ再臨の儀」",
    "desc": "どうも東風谷早苗です",
    "hp": 3000,
    "duration": 60,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    ey = 250\n    wait(0.5)\n    count = 0\n    s = 0\n    for (let i = 0; i < 5; i++) {\n        count += 1\n        spawnBullet(\"normal\", \"#ff3333\", 0, angle + 180, 0, 0, 0, \"redbig\", \"relative\", \"3\")\n        spawnBullet(\"normal\", \"#ffffff\", 0, angle + 180, 0, 0, 0, \"redbig\", \"relative\", \"3\")\n        spawnBullet(\"normal\", \"#fffff2\", 0, -angle - 180, 0, 0, 0, \"redbig\", \"relative\", \"3\")\n        angle += 72 * 3\n        wait(0.0167 * 11)\n        s += 11\n    }\n    wait(5)\n    angle += 15\n}\n    ",
    "bulletScript": "\nif (color==#ff3333) {\n    advance(200)\n    angle += 180\n    angle += 72 / 4\n    for (let i = 0; i < 10; i++) {\n        for (let i = 0; i < 2; i++) {\n            s2angle = -90\n            for (let i = 0; i < 5; i++) {\n                s2angle += 72\n                spawnBullet(\"normal\", \"#ff3332\", 0, angle, 0, 0, 9, \"goldbig\", \"relative\", \"3\")\n            }\n            advance(20)\n            spangle -= -9.2\n        }\n        wait(0.0167)\n        s += 1\n    }\n    y = -8000\n}\nif (color==#ffffff) {\n    advance(100)\n    angle += 180\n    angle += 72 / 4\n    for (let i = 0; i < 10; i++) {\n        for (let i = 0; i < 2; i++) {\n            s3angle = -90 + 60\n            for (let i = 0; i < 3; i++) {\n                s3angle += 120\n                spawnBullet(\"normal\", \"#ffffff\", 0, angle, 0, 0, 9, \"bluebig\", \"relative\", \"6\")\n            }\n            advance(10)\n            spangle -= -9.2\n        }\n        wait(0.0167 * 2)\n        s += 2\n    }\n    y = -8000\n}\nif (color==#fffff2) {\n    advance(100)\n    angle += 180\n    angle += 72 / 4\n    for (let i = 0; i < 10; i++) {\n        for (let i = 0; i < 2; i++) {\n            s3angle = 90 + 60\n            for (let i = 0; i < 3; i++) {\n                s3angle -= 120\n                spawnBullet(\"normal\", \"#ffffff\", 0, angle, 0, 0, 9, \"bluebig\", \"relative\", \"6\")\n            }\n            advance(10)\n            spangle -= 9.2\n        }\n        wait(0.0167 * 2)\n        s += 2\n    }\n    y = -8000\n}\n    ",
    "magicCircleScript": "\nonce {\n    motoangle = angle\n    if (color==#ff3332) {\n        angle = s2angle\n    }\n    if (color==#ffffff) {\n        angle = s3angle\n    }\n}\nif (frame == 65 - s) {\n    if (color==#ff3332) {\n        tween(\"speed\", 0, 600, \"seconds\", 1)\n        tween(\"speed\", 600, 0, \"seconds\", 1)\n    }\n    if (color==#ffffff) {\n        tween(\"speed\", 0, 300, \"seconds\", 1)\n        tween(\"speed\", 300, 0, \"seconds\", 1)\n    }\n    wait(1)\n    angle = motoangle\n    if (color==#ff3332) {\n        angle += spangle\n    }\n    if (color==#ffffff) {\n        angle += spangle * 0.1\n    }\n    tween(\"speed\", 0, 130, \"seconds\", 1)\n}\n    "
  },
  {
    "difficulty": "hard",
    "name": "「デストラクトオブアロー」",
    "desc": "そこそこ避けれそう。ちな俺は無理",
    "hp": 1200,
    "duration": 60,
    "maxMisses": 3,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 1.5,
    "emitterScript": "\nwhile (true) {\n    spd = 0\n    wa = 0\n    for (let i = 0; i < 20; i++) {\n        spd += 50\n        spawnRing(\"normal\", \"#ff3333\", 100 + spd, angle, 18, 0, 0, 18, \"yellowarrow\", \"relative\", \"6\")\n        angle += 1\n    }\n    angle += 14\n    wait(0.0167 * 60)\n    spd = 0\n    wa = 0\n    for (let i = 0; i < 20; i++) {\n        spd += 50\n        spawnRing(\"normal\", \"#ff3333\", 100 + spd, angle, 18, 0, 0, 18, \"yellowarrow\", \"relative\", \"6\")\n        angle -= 1\n    }\n    angle += 14\n    wait(0.0167 * 60)\n}\n    ",
    "bulletScript": "\nonce {\n    tween(\"speed\", speed, 0, \"seconds\", 1)\n    advance(50)\n    wait(1)\n    imageTo(\"whitearrow\")\n}\nif (frame == 120) {\n    imageTo(\"yellowarrow\")\n    tween(\"speed\", 400, 1000, \"seconds\", 1)\n    angle += random(-11,11)\n}\nspriteAngle = angle\n    ",
    "magicCircleScript": "\n\n    "
  },
  {
    "difficulty": "NORMAL",
    "name": "滅妖「妖怪圧殺陣」",
    "desc": "あまりにも最強すぎて妖怪であるあなたは一発触れただけで四肢がもげてしにます",
    "hp": 1500,
    "duration": 40,
    "maxMisses": 0,
    "x_offset": 0,
    "y_offset": 0,
    "despawnTime": 0.1,
    "emitterScript": "\nwhile (true) {\n    spawnRing(\"normal\", \"#ff3333\", 200, angle * 546, 8, 0, 0, 8, \"redkunai2\", \"relative\", \"3\")\n    spawnRing(\"normal\", \"#ff3333\", 200, angle * 2.2, 8, 0, 0, 8, \"redkunai2\", \"relative\", \"3\")\n    spawnRing(\"normal\", \"#ff3333\", 200, -angle * 4562.2, 8, 0, 0, 8, \"redkunai2\", \"relative\", \"3\")\n    angle += 1.345\n    wait(0.0167 * 2)\n}\n    ",
    "bulletScript": "\n\n    ",
    "magicCircleScript": "\n\n    "
  }
];
window.sharedDanmakuListS2 = window.compiledDanmakuListS2;

// Helpers
window.DanmakuCompilerRuntime.resolveColorParam = function(val, vars) {
    if (!val) return null;
    let s = String(val);
    if (s.startsWith('#')) return s;
    if (vars && vars[s]) return String(vars[s]);
    return s;
};

// 実行エンジン (extracted from emitter.js)
window.DanmakuCompilerRuntime.executeBlock = function(p, state, b, attacker, target, _util) {
    let isPlayerSide = state ? state.isPlayerSide : false;
    let canvas = window.canvas || {width: 640, height: 960};
    let PLAY_WIDTH = typeof window.PLAY_WIDTH !== 'undefined' ? window.PLAY_WIDTH : 768;
    let dt = (state && state.dt !== undefined) ? state.dt : ((window.DanmakuCompilerRuntime && window.DanmakuCompilerRuntime.currentDt) || window.dt || 1 / 60);
    let block = p;
    let brokeToWait = false;
    let advancePC = true;
    
    switch (p.type) {
                    case 'play_sound': {
                        let name = p.soundName || 'shot';
                        if (typeof playSound === 'function') {
                            playSound(name);
                        }
                        break;
                    }
                    case 'wait': {
                        let dur = evalExpr(p.duration, state.variables, block, 'duration');
                        state.waitTimer = Math.max(0.0167, dur);
                        brokeToWait = true;
                        break;
                    }
                    case 'repeat': {
                        let count = Math.round(evalExpr(p.count, state.variables, block, 'count'));
                        let loopCount = Math.max(1, count);
                        if (p.indexVar) state.variables[p.indexVar] = 0;
                        state.stack.push({
                            type: 'repeat',
                            pc: 0,
                            iterationsLeft: loopCount,
                            totalIterations: loopCount,
                            index: 0,
                            indexVar: p.indexVar || null,
                            forever: false,
                            blocks: p.children || []
                        });
                        advancePC = false;
                        break;
                    }
                    case 'forever': {
                        state.stack.push({
                            type: 'forever',
                            pc: 0,
                            iterationsLeft: 999999,
                            forever: true,
                            blocks: p.children || []
                        });
                        advancePC = false;
                        break;
                    }
                    case 'while': {
                        let condStr = p.cond || 'false';
                        if (evalCondition(condStr, state.variables, block, 'cond') && p.children && p.children.length > 0) {
                            state.stack.push({
                                type: 'while',
                                pc: 0,
                                iterationsLeft: 999999,
                                forever: false,
                                cond: condStr,
                                blocks: p.children || []
                            });
                            advancePC = false;
                        }
                        break;
                    }
                    case 'if': {
                        let condStr = p.cond || 'x < 10';
                        let isTrue = evalCondition(condStr, state.variables, block, 'cond');
                        if (isTrue && p.children && p.children.length > 0) {
                            state.stack.push({
                                type: 'if',
                                pc: 0,
                                iterationsLeft: 1,
                                forever: false,
                                blocks: p.children
                            });
                            advancePC = false;
                        }
                        break;
                    }
                    case 'once': {
                        advancePC = executeOnceBlock(block, state);
                        break;
                    }

                    case 'const_var':
                    case 'set_var': {
                        let varName = p.name;
                        let val = evalValue(p.value, state.variables);
                        setScriptVariable(state, varName, val, p.type === 'const_var');
                        syncAttackerFromEmitterVariables(state, attacker);
                        break;
                    }
                    case 'change_var': {
                        let varName = p.name;
                        let val = evalExpr(p.value, state.variables);
                        let delta = p.op === '-' ? -val : val;
                        if (!state.constVars || typeof state.constVars.has !== 'function') state.constVars = new Set();
                        if (!state.constVars.has(varName)) state.variables[varName] = (Number(state.variables[varName]) || 0) + delta;
                        syncAttackerFromEmitterVariables(state, attacker);
                        break;
                    }
                    case 'bullet_image_set': {
                        let img = p.bulletImage || p.value || 'none';
                        if (typeof resolveBulletImageParam === 'function') {
                            img = resolveBulletImageParam(img, state.variables);
                        } else {
                            img = String(img).trim().replace(/^['"]|['"]$/g, '');
                            if (state.variables && state.variables[img] !== undefined) img = state.variables[img];
                        }
                        img = String(img).trim().replace(/^['"]|['"]$/g, '');
                        state.variables.bulletImage = img;
                        state.variables.image = img;
                        break;
                    }
                    case 'aim_at_target': {
                        let dx = target.x - ((window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0));
                        let dy = target.y - ((window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0));
                        if (isPlayerSide) {
                            let targetVirtualY = canvas.height - target.y;
                            let spawnVirtualY = (canvas.height - (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900))) + (state.variables.y_offset || 0);
                            dy = targetVirtualY - spawnVirtualY;
                        }
                        state.variables.angle = Math.atan2(dy, dx) * 180 / Math.PI;
                        break;
                    }
                    case 'aim_at_coord': {
                        let txRaw = evalExpr(p.targetX || '0', state.variables);
                        let tyRaw = evalExpr(p.targetY || '0', state.variables);
                        let txAbs = Number(txRaw) || 0;
                        let tyAbs = isPlayerSide ? (canvas.height - (Number(tyRaw) || 0)) : (Number(tyRaw) || 0);

                        let curX, curY;
                        if (typeof b !== 'undefined' && b) {
                            curX = state.variables.x !== undefined ? state.variables.x : b.x;
                            curY = state.variables.y !== undefined ? (isPlayerSide ? canvas.height - state.variables.y : state.variables.y) : b.y;
                        } else {
                            let baseX = state.variables.x !== undefined ? state.variables.x : (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state));
                            let baseY = state.variables.y !== undefined ? (isPlayerSide ? canvas.height - state.variables.y : state.variables.y) : (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900));
                            curX = baseX + (state.variables.x_offset || 0);
                            curY = baseY + (state.variables.y_offset || 0);
                        }

                        let dxC = txAbs - curX;
                        let dyC = isPlayerSide ? (curY - tyAbs) : (tyAbs - curY);
                        state.variables.angle = Math.atan2(dyC, dxC) * 180 / Math.PI;
                        break;
                    }
                    case 'move_owner': {
                        const owner = attacker === player ? 'PLAYER' : 'CPU';
                        const preset = resolveTextParam(p.preset || 'center', state.variables);
                        const duration = evalExpr(p.duration || '0', state.variables);
                        setCustomOwnerPosition(owner, preset, duration);
                        applyCustomOwnerPositionLock(owner, 0);
                        
                        // 移動先の座標を取得して ex, ey, exy に即座に反映させ、古い座標への引き戻しを防ぐ
                        if (typeof getCustomOwnerPosition !== 'undefined') {
                            const targetPos = getCustomOwnerPosition(owner, preset);
                            if (targetPos) {
                                state.variables.ex = targetPos.x;
                                state.variables.ey = isPlayerSide ? (canvas.height - targetPos.y) : targetPos.y;
                                state.variables.exy = `${targetPos.x},${state.variables.ey}`;
                            }
                        }
                        break;
                    }
                    case 'spawn_bullet':
                    case 'spawn_bullet_resist':
                    case 'spawn_trail':
                    case 'spawn_trail_resist': {
                        let isTrail = p.type === 'spawn_trail' || p.type === 'spawn_trail_resist';
                        let speed = evalExpr(p.speed, state.variables);
                        let angle = evalExpr(p.angle, state.variables);
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                        
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);

                        let coordMode = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (coordMode === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            let baseX = state.variables.x !== undefined ? state.variables.x : (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state));
                            let baseY = state.variables.y !== undefined ? (isPlayerSide ? canvas.height - state.variables.y : state.variables.y) : (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900));
                            spawnX = baseX + (state.variables.x_offset || 0) + ox;
                            spawnY = baseY + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }

                        let angleRad = angle * Math.PI / 180;
                        if (isPlayerSide) {
                            angleRad = -angleRad;
                        }
                        
                        let bRadius = evalExpr(p.radius || (isTrail ? '8' : '6'), state.variables);
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bImg = isTrail ? 'none' : (p.bulletImage || 'none');

                        let newBullet = {
                            x: spawnX,
                            y: spawnY,
                            startX: spawnX, // 初期位置保存
                            startY: spawnY,
                            vx: Math.cos(angleRad) * speed,
                            vy: Math.sin(angleRad) * speed,
                            radius: bRadius,
                            hitRadius: bHitRadius,
                            bulletImage: bImg,
                            team: attacker.team,
                            color: bColor,
                            bulletType: isTrail ? 'trail' : (p.bulletType || 'normal'),
                            customDmg: 20, // custom card damage balanced to 20
                            isCustom: true,
                            update: null
                        };
                        
                        if (p.bulletType === 'laser') {
                            newBullet.isLaser = true;
                        }
                        if (p.bulletType === 'trail') {
                            isTrail = true;
                        }
                        if (isTrail) {
                            newBullet.isTrail = true;
                            newBullet.growTime = (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables)) : 0.2;
                            newBullet.keepTime = (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables)) : 0.3;
                            newBullet.shrinkTime = (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables)) : 0.5;
                            newBullet.round = (p.round !== undefined) ? (p.round === 'true' || p.round === true) : true;
                            newBullet.trailHistory = [];
                        }
                        if (p.type === 'spawn_bullet_resist' || p.type === 'spawn_trail_resist') {
                            newBullet.destroyResist = true;
                        }

                        newBullet.threatWeight = _util._computeBulletThreatWeight(
                            spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                        );

                        let isFromBullet = typeof b !== 'undefined' && b !== null;
                        let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                        let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                        let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                        let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                        let hasScript = (cScript && cScript.length > 0) || !!cFn;
                        newBullet.bulletState = _util._initBulletState(cScript, speed, angle, attacker, target, cFn);
                        newBullet.bulletState.id = state.id || null;
                        newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                        newBullet.bulletState.bulletScript = cScript;
                        newBullet.bulletState.isPlayerSide = isPlayerSide;
                        inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                        newBullet.bulletState.variables.bulletType = isTrail ? 'trail' : (p.bulletType || 'normal');
                        if (isTrail) {
                            newBullet.bulletState.variables.growTime = p.growTime || '0.2';
                            newBullet.bulletState.variables.keepTime = p.keepTime || '0.3';
                            newBullet.bulletState.variables.shrinkTime = p.shrinkTime || '0.5';
                            newBullet.bulletState.variables.round = (p.round !== undefined) ? String(p.round) : 'true';
                        }
                        newBullet.bulletState.variables.color = bColor;
                        newBullet.bulletState.variables.radius = bRadius;
                        newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                        newBullet.bulletState.variables.bulletImage = bImg;
                        newBullet.sharedEmitterState = state;
                        if (hasScript) {
                            newBullet.update = (b, bdt) => {
                                _util._runCustomBulletScript(b, bdt, attacker, target);
                            };
                        } else {
                            newBullet.update = null;
                        }
                        
                        if (window.showDebugProfiler) {
                            console.log(`[DEBUG] spawn_bullet: x=${spawnX.toFixed(1)}, y=${spawnY.toFixed(1)}, color=${bColor}, speed=${speed}, angle=${angle}`);
                        }
                        bullets.push(newBullet);
                        break;
                    }
                    case 'spawn_ring':
                    case 'spawn_ring_resist': {
                        let speed = evalExpr(p.speed, state.variables);
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                        let count = Math.max(3, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables))));
                        
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);

                        let coordMode = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (coordMode === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        
                        let bRadius = evalExpr(p.radius || '6', state.variables);
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bImg = p.bulletImage || 'none';
                        let centerAngle = evalExpr(p.angle || '0', state.variables);

                        for (let k = 0; k < count; k++) {
                            let angle = centerAngle + (360 / count) * k;
                            let angleRad = angle * Math.PI / 180;
                            if (isPlayerSide) {
                                angleRad = -angleRad;
                            }
                            
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX, // 初期位置保存
                                startY: spawnY,
                                vx: Math.cos(angleRad) * speed,
                                vy: Math.sin(angleRad) * speed,
                                radius: bRadius,
                                hitRadius: bHitRadius,
                                bulletImage: bImg,
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                update: null
                            };
                            
                            if (p.bulletType === 'laser') {
                                newBullet.isLaser = true;
                            }
                            if (p.type === 'spawn_ring_resist') {
                                newBullet.destroyResist = true;
                            }
                            
                            newBullet.threatWeight = _util._computeBulletThreatWeight(
                                spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                            );

                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, speed, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = bRadius;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.bulletState.variables.bulletImage = bImg;
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            
                            bullets.push(newBullet);
                        }
                        if (window.showDebugProfiler) {
                            console.log(`[DEBUG] spawn_ring: x=${spawnX.toFixed(1)}, y=${spawnY.toFixed(1)}, color=${bColor}, speed=${speed}, count=${count}, angle=${centerAngle}`);
                        }
                        break;
                    }
                    case 'spawn_way':
                    case 'spawn_way_resist': {
                        let speed = evalExpr(p.speed, state.variables);
                        let centerAngle = evalExpr(p.angle || 'angle', state.variables);
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                        let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables))));
                        let spread = evalExpr(p.spread || '15', state.variables);
                        
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);

                        let coordMode = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (coordMode === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        
                        let bRadius = evalExpr(p.radius || '6', state.variables);
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bImg = p.bulletImage || 'none';

                        let startAngle = centerAngle - (spread * (count - 1)) / 2;
                        
                        for (let k = 0; k < count; k++) {
                            let angle = startAngle + spread * k;
                            let angleRad = angle * Math.PI / 180;
                            if (isPlayerSide) {
                                angleRad = -angleRad;
                            }
                            
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX, // 初期位置保存
                                startY: spawnY,
                                vx: Math.cos(angleRad) * speed,
                                vy: Math.sin(angleRad) * speed,
                                radius: bRadius,
                                hitRadius: bHitRadius,
                                bulletImage: bImg,
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                update: null
                            };
                            
                            if (p.bulletType === 'laser') {
                                newBullet.isLaser = true;
                            }
                            if (p.type === 'spawn_way_resist') {
                                newBullet.destroyResist = true;
                            }
                            
                            newBullet.threatWeight = _util._computeBulletThreatWeight(
                                spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                            );

                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, speed, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = bRadius;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.bulletState.variables.bulletImage = bImg;
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            
                            bullets.push(newBullet);
                        }
                        if (window.showDebugProfiler) {
                            console.log(`[DEBUG] spawn_way: x=${spawnX.toFixed(1)}, y=${spawnY.toFixed(1)}, color=${bColor}, speed=${speed}, count=${count}, spread=${spread}, centerAngle=${centerAngle}`);
                        }
                        break;
                    }
                    case 'spawn_beam':
                    case 'spawn_beam_resist': {
                        let wt = Number(evalExpr(p.warningTime || '1.0', state.variables));
                        let at = Number(evalExpr(p.activeTime || '1.5', state.variables));
                        let lw = Number(evalExpr(p.laserWidth || '12', state.variables));
                        let angle = evalExpr(p.angle || 'angle', state.variables);
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);
                        let cm = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (cm === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);

                        let angleRad = angle * Math.PI / 180;
                        if (isPlayerSide) angleRad = -angleRad;

                        let newBullet = {
                            x: spawnX,
                            y: spawnY,
                            startX: spawnX,
                            startY: spawnY,
                            vx: 0,
                            vy: 0,
                            radius: 8,
                            hitRadius: bHitRadius,
                            bulletImage: 'none',
                            team: attacker.team,
                            color: bColor,
                            customDmg: 20,
                            isCustom: true,
                            isWarningLaser: true,
                            isLaser: false,
                            isCustomBeam: false,
                            laserStartX: spawnX,
                            laserStartY: spawnY,
                            update: null
                        };
                        if (p.type === 'spawn_beam_resist') {
                            newBullet.destroyResist = true;
                        }
                        newBullet.threatWeight = 100;
                        let isFromBullet = typeof b !== 'undefined' && b !== null;
                        let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                        let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                        let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                        let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                        let hasScript = (cScript && cScript.length > 0) || !!cFn;
                        newBullet.bulletState = _util._initBulletState(cScript, 0, angle, attacker, target, cFn);
                        newBullet.bulletState.id = state.id || null;
                        newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                        newBullet.bulletState.bulletScript = cScript;
                        newBullet.bulletState.isPlayerSide = isPlayerSide;
                        inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                        newBullet.bulletState.variables.bulletType = 'laser';
                        newBullet.bulletState.variables.warningTime = String(wt);
                        newBullet.bulletState.variables.activeTime = String(at);
                        newBullet.bulletState.variables.laserWidth = String(lw);
                        newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                        newBullet.bulletState.variables.timer = 0;
                        newBullet.bulletState.variables.color = bColor;
                        newBullet.bulletState.variables.radius = 8;
                        newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                        newBullet.sharedEmitterState = state;
                        if (hasScript) {
                            newBullet.update = (b, bdt) => {
                                _util._runCustomBulletScript(b, bdt, attacker, target);
                            };
                        } else {
                            newBullet.update = null;
                        }
                        bullets.push(newBullet);
                        break;
                    }
                    case 'spawn_laser_way':
                    case 'spawn_laser_way_resist': {
                        let speed = evalExpr(p.speed || '200', state.variables);
                        let centerAngle = evalExpr(p.angle || 'angle', state.variables);
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color || '#ff3333', state.variables);
                        let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables))));
                        let spread = evalExpr(p.spread || '45', state.variables);
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);
                        let cm = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (cm === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        let bRadius = evalExpr(p.radius || '6', state.variables);
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let startAngle = centerAngle - (spread * (count - 1)) / 2;
                        for (let k = 0; k < count; k++) {
                            let angle = startAngle + spread * k;
                            let angleRad = angle * Math.PI / 180;
                            if (isPlayerSide) angleRad = -angleRad;
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX,
                                startY: spawnY,
                                vx: Math.cos(angleRad) * speed,
                                vy: Math.sin(angleRad) * speed,
                                radius: bRadius,
                                hitRadius: bHitRadius,
                                bulletImage: 'none',
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                isTrail: true,
                                trailHistory: [],
                                growTime: (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables)) : 0.2,
                                keepTime: (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables)) : 0.3,
                                shrinkTime: (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables)) : 0.5,
                                round: p.round !== 'false' && p.round !== false,
                                update: null
                            };
                            if (p.type === 'spawn_laser_way_resist') {
                                newBullet.destroyResist = true;
                            }
                            newBullet.threatWeight = _util._computeBulletThreatWeight(spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y);
                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, speed, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = bRadius;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.bulletState.variables.growTime = newBullet.growTime;
                            newBullet.bulletState.variables.keepTime = newBullet.keepTime;
                            newBullet.bulletState.variables.shrinkTime = newBullet.shrinkTime;
                            newBullet.bulletState.variables.bulletImage = 'none';
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            bullets.push(newBullet);
                        }
                        break;
                    }
                    case 'spawn_laser_ring':
                    case 'spawn_laser_ring_resist': {
                        let speed = evalExpr(p.speed || '200', state.variables);
                        let centerAngle = evalExpr(p.angle || 'angle', state.variables);
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color || '#ff3333', state.variables);
                        let count = Math.max(3, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables))));
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);
                        let cm = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (cm === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        let bRadius = evalExpr(p.radius || '6', state.variables);
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        for (let k = 0; k < count; k++) {
                            let angle = centerAngle + (360 / count) * k;
                            let angleRad = angle * Math.PI / 180;
                            if (isPlayerSide) angleRad = -angleRad;
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX,
                                startY: spawnY,
                                vx: Math.cos(angleRad) * speed,
                                vy: Math.sin(angleRad) * speed,
                                radius: bRadius,
                                hitRadius: bHitRadius,
                                bulletImage: 'none',
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                isTrail: true,
                                trailHistory: [],
                                growTime: (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables)) : 0.2,
                                keepTime: (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables)) : 0.3,
                                shrinkTime: (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables)) : 0.5,
                                round: p.round !== 'false' && p.round !== false,
                                update: null
                            };
                            if (p.type === 'spawn_laser_ring_resist') {
                                newBullet.destroyResist = true;
                            }
                            newBullet.threatWeight = _util._computeBulletThreatWeight(spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y);
                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, speed, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = bRadius;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.bulletState.variables.growTime = newBullet.growTime;
                            newBullet.bulletState.variables.keepTime = newBullet.keepTime;
                            newBullet.bulletState.variables.shrinkTime = newBullet.shrinkTime;
                            newBullet.bulletState.variables.bulletImage = 'none';
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            bullets.push(newBullet);
                        }
                        break;
                    }
                    case 'spawn_beam_way':
                    case 'spawn_beam_way_resist': {
                        let wt = Number(evalExpr(p.warningTime || '1.0', state.variables));
                        let at = Number(evalExpr(p.activeTime || '1.5', state.variables));
                        let lw = Number(evalExpr(p.laserWidth || '12', state.variables));
                        let centerAngle = evalExpr(p.angle || 'angle', state.variables);
                        let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables))));
                        let spread = evalExpr(p.spread || '45', state.variables);
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);
                        let cm = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (cm === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);
                        let startAngle = centerAngle - (spread * (count - 1)) / 2;
                        for (let k = 0; k < count; k++) {
                            let angle = startAngle + spread * k;
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX,
                                startY: spawnY,
                                vx: 0,
                                vy: 0,
                                radius: 8,
                                hitRadius: bHitRadius,
                                bulletImage: 'none',
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                isWarningLaser: true,
                                isLaser: false,
                                isCustomBeam: false,
                                laserStartX: spawnX,
                                laserStartY: spawnY,
                                update: null
                            };
                            if (p.type === 'spawn_beam_way_resist') {
                                newBullet.destroyResist = true;
                            }
                            newBullet.threatWeight = 100;
                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, 0, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.bulletType = 'laser';
                            newBullet.bulletState.variables.warningTime = String(wt);
                            newBullet.bulletState.variables.activeTime = String(at);
                            newBullet.bulletState.variables.laserWidth = String(lw);
                            newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                            newBullet.bulletState.variables.timer = 0;
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = 8;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            bullets.push(newBullet);
                        }
                        break;
                    }
                    case 'spawn_beam_ring':
                    case 'spawn_beam_ring_resist': {
                        let wt = Number(evalExpr(p.warningTime || '1.0', state.variables));
                        let at = Number(evalExpr(p.activeTime || '1.5', state.variables));
                        let lw = Number(evalExpr(p.laserWidth || '12', state.variables));
                        let centerAngle = evalExpr(p.angle || 'angle', state.variables);
                        let count = Math.max(3, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables))));
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);
                        let cm = p.coordMode || 'relative';
                        let spawnX, spawnY;
                        if (cm === 'absolute') {
                            spawnX = ox;
                            spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                        } else {
                            spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                            spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                        }
                        let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables) : undefined;
                        if (bHitRadius !== undefined) {
                            let hrNum = Number(bHitRadius);
                            if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                        }
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);
                        for (let k = 0; k < count; k++) {
                            let angle = centerAngle + (360 / count) * k;
                            let newBullet = {
                                x: spawnX,
                                y: spawnY,
                                startX: spawnX,
                                startY: spawnY,
                                vx: 0,
                                vy: 0,
                                radius: 8,
                                hitRadius: bHitRadius,
                                bulletImage: 'none',
                                team: attacker.team,
                                color: bColor,
                                customDmg: 20,
                                isCustom: true,
                                isWarningLaser: true,
                                isLaser: false,
                                isCustomBeam: false,
                                laserStartX: spawnX,
                                laserStartY: spawnY,
                                update: null
                            };
                            if (p.type === 'spawn_beam_ring_resist') {
                                newBullet.destroyResist = true;
                            }
                            newBullet.threatWeight = 100;
                            let isFromBullet = typeof b !== 'undefined' && b !== null;
                            let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                            let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                            let cScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                            let cFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                            let hasScript = (cScript && cScript.length > 0) || !!cFn;
                            newBullet.bulletState = _util._initBulletState(cScript, 0, angle, attacker, target, cFn);
                            newBullet.bulletState.id = state.id || null;
                            newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                            newBullet.bulletState.bulletScript = cScript;
                            newBullet.bulletState.isPlayerSide = isPlayerSide;
                            inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                            newBullet.bulletState.variables.bulletType = 'laser';
                            newBullet.bulletState.variables.warningTime = String(wt);
                            newBullet.bulletState.variables.activeTime = String(at);
                            newBullet.bulletState.variables.laserWidth = String(lw);
                            newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                            newBullet.bulletState.variables.timer = 0;
                            newBullet.bulletState.variables.color = bColor;
                            newBullet.bulletState.variables.radius = 8;
                            newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                            newBullet.sharedEmitterState = state;
                            if (hasScript) {
                                newBullet.update = (b, bdt) => {
                                    _util._runCustomBulletScript(b, bdt, attacker, target);
                                };
                            } else {
                                newBullet.update = null;
                            }
                            bullets.push(newBullet);
                        }
                        break;
                    }
                    case 'spawn_magic_circle': {
                        let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                        let ox = evalExpr(p.offsetX || '0', state.variables);
                        let oy = evalExpr(p.offsetY || '0', state.variables);

                        let spawnX = (window.DanmakuCompilerRuntime._getCurrentX(b, attacker, state)) + (state.variables.x_offset || 0) + ox;
                        let spawnY = (window.DanmakuCompilerRuntime._getCurrentY(b, attacker, state, window.canvas ? window.canvas.height : 900)) + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);

                        let mcId = 'MC_' + Date.now() + '_' + Math.floor(Math.random() * 1000);

                        let newMagicCircle = {
                            id: mcId,
                            x: spawnX,
                            y: spawnY,
                            radius: 18,
                            color: bColor,
                            team: attacker.team,
                            isCustom: true,
                            emitterState: initEmitterState(state.magicCircleScript || [], { x: spawnX, y: spawnY }, target, 0, 0, state.id ? state.id + '_magic' : null),
                            bulletScript: state.magicCircleScript || []
                        };
                        newMagicCircle.emitterState.magicCircleScript = state.magicCircleScript || [];

                        magicCircles.push(newMagicCircle);
                        break;
                    }
                    case 'tween_angle':
                    case 'tween_angle_wait': {
                        let varName = 'angle';
                        let fromVal = evalExpr(p.from || '0', state.variables);
                        let toVal = evalExpr(p.to || '360', state.variables);
                        let duration = evalExpr(p.duration || '1', state.variables);
                        let mode = p.mode || 'seconds';
                        if (mode === 'frames') duration = Math.max(1, duration);
                        else duration = Math.max(0.001, duration);
                        
                        if (!state.tweens) state.tweens = [];
                        state.tweens = state.tweens.filter(t => t.name !== varName);
                        
                        state.tweens.push({
                            name: varName,
                            mode: mode,
                            from: fromVal,
                            to: toVal,
                            total: duration,
                            elapsed: 0,
                            easing: p.easing || 'linear',
                            isAngleTween: true,
                            rotMode: p.rotMode || 'shortest'
                        });
                        
                        if (p.type === 'tween_angle_wait') {
                            state.waitingTweenName = varName;
                        }
                        break;
                    }

                    case 'tween_var':
                    case 'tween_var_wait': {
                        let varName = p.name || 'angle';
                        let isMultiVar = varName.includes(',');
                        
                        let fromVal, toVal;
                        if (isMultiVar) {
                            let fromParts = (p.from || '').split(',').map(p => evalExpr(p.trim(), state.variables));
                            let toParts = (p.to || '').split(',').map(p => evalExpr(p.trim(), state.variables));
                            fromVal = `${fromParts[0] || 0},${fromParts[1] || 0}`;
                            toVal = `${toParts[0] || 0},${toParts[1] || 0}`;
                        } else {
                            fromVal = evalExpr(p.from, state.variables);
                            toVal   = evalExpr(p.to,   state.variables);
                        }

                        let mode    = p.mode || 'seconds'; // 'seconds' | 'frames' | 'step'
                        if (!state.tweens) state.tweens = [];
                        // 同じ変数の既存tweenを上書き
                        state.tweens = state.tweens.filter(t => t.name !== varName);

                        // 座標ペア（コンマ区切り文字列）の tween 補間の処理
                        if (typeof toVal === 'string' && toVal.includes(',')) {
                            let toParts = toVal.split(',').map(p => parseFloat(p.trim()));
                            let fromParts = String(fromVal).split(',').map(p => parseFloat(p.trim()));
                            if (toParts.length === 2 && fromParts.length === 2 && !isNaN(toParts[0]) && !isNaN(toParts[1]) && !isNaN(fromParts[0]) && !isNaN(fromParts[1])) {
                                state.variables[varName] = fromVal;
                                if (mode === 'step' || mode === 'vecstep') {
                                    let stepVal = evalExpr(p.stepVal || p.value || '5', state.variables);
                                    if (mode === 'vecstep') {
                                        state.tweens.push({
                                            name: varName,
                                            isCoordPair: true,
                                            isStep: true,
                                            isVecStep: true,
                                            fromX: fromParts[0],
                                            toX: toParts[0],
                                            fromY: fromParts[1],
                                            toY: toParts[1],
                                            stepVal: stepVal,
                                            mode,
                                            total: 1,
                                            elapsed: 0
                                        });
                                    } else {
                                        // 従来の step モードの復元（各軸独立して加算）
                                        let stepX = stepVal;
                                        let stepY = stepVal;
                                        if (fromParts[0] > toParts[0] && stepX > 0) stepX = -stepX;
                                        if (fromParts[1] > toParts[1] && stepY > 0) stepY = -stepY;
                                        if (fromParts[0] === toParts[0]) stepX = 0;
                                        if (fromParts[1] === toParts[1]) stepY = 0;

                                        state.tweens.push({
                                            name: varName,
                                            isCoordPair: true,
                                            isStep: true,
                                            isVecStep: false,
                                            fromX: fromParts[0],
                                            toX: toParts[0],
                                            fromY: fromParts[1],
                                            toY: toParts[1],
                                            stepX: stepX,
                                            stepY: stepY,
                                            mode,
                                            total: 1,
                                            elapsed: 0
                                        });
                                    }
                                } else {
                                    // 通常の時間ベースの補間
                                    let total = evalExpr(p.duration || '1', state.variables);
                                    if (mode === 'frames') total = Math.max(1, total);
                                    else total = Math.max(0.001, total);
                                    state.tweens.push({
                                        name: varName,
                                        isCoordPair: true,
                                        isStep: false,
                                        fromX: fromParts[0],
                                        toX: toParts[0],
                                        fromY: fromParts[1],
                                        toY: toParts[1],
                                        mode,
                                        total,
                                        elapsed: 0,
                                        easing: p.easing || 'linear'
                                    });
                                }
                                // 子変数を即座に初期化
                                state.variables[varName + '_x'] = fromParts[0];
                                state.variables[varName + '_y'] = fromParts[1];
                                state.variables[varName + '.x'] = fromParts[0];
                                state.variables[varName + '.y'] = fromParts[1];
                                if (p.type === 'tween_var_wait') {
                                    state.waitingTweenName = varName;
                                }
                                break;
                            }
                        }

                        if (mode === 'step') {
                            let stepVal = evalExpr(p.stepVal || '5', state.variables);
                            if (fromVal > toVal && stepVal > 0) stepVal = -stepVal;
                            state.variables[varName] = fromVal;
                            state.tweens.push({ name: varName, to: toVal, mode: 'step', stepVal });
                        } else {
                            let total = evalExpr(p.duration || '1', state.variables);
                            if (mode === 'frames') total = Math.max(1, total);
                            else total = Math.max(0.001, total);
                            let fNum = Number(fromVal);
                            if (isNaN(fNum)) fNum = 0;
                            let tNum = Number(toVal);
                            if (isNaN(tNum)) tNum = 0;
                            state.variables[varName] = fNum;
                            state.tweens.push({ name: varName, from: fNum, to: tNum, mode, total, elapsed: 0, easing: p.easing || 'linear' });
                        }
                        if (p.type === 'tween_var_wait') {
                            state.waitingTweenName = varName;
                        }
                        break;
                    }
                

                            case 'play_sound': {
                                let name = p.soundName || 'shot';
                                if (typeof playSound === 'function') {
                                    playSound(name);
                                }
                                break;
                            }
                            case 'wait': {
                                let dur = evalExpr(p.duration, state.variables, block, 'duration');
                                state.waitTimer = Math.max(0.0167, dur);
                                brokeToWait = true;
                                break;
                            }
                            case 'repeat': {
                                let count = Math.round(evalExpr(p.count, state.variables, block, 'count'));
                                let loopCount = Math.max(1, count);
                                if (p.indexVar) state.variables[p.indexVar] = 0;
                                state.stack.push({
                                    type: 'repeat',
                                    pc: 0,
                                    iterationsLeft: loopCount,
                                    totalIterations: loopCount,
                                    index: 0,
                                    indexVar: p.indexVar || null,
                                    forever: false,
                                    blocks: p.children || []
                                });
                                advancePC = false;
                                break;
                            }
                            case 'forever': {
                                state.stack.push({
                                    type: 'forever',
                                    pc: 0,
                                    iterationsLeft: 999999,
                                    forever: true,
                                    blocks: p.children || []
                                });
                                advancePC = false;
                                break;
                            }
                            case 'while': {
                                let condStr = p.cond || 'false';
                                if (evalCondition(condStr, state.variables, block, 'cond') && p.children && p.children.length > 0) {
                                    state.stack.push({
                                        type: 'while',
                                        pc: 0,
                                        iterationsLeft: 999999,
                                        forever: false,
                                        cond: condStr,
                                        blocks: p.children || []
                                    });
                                    advancePC = false;
                                }
                                break;
                            }
                            case 'if': {
                                let condStr = p.cond || 'x < 10';
                                let isTrue = evalCondition(condStr, state.variables, block, 'cond');
                                
                                if (isTrue && p.children && p.children.length > 0) {
                                    state.stack.push({
                                        type: 'if',
                                        pc: 0,
                                        iterationsLeft: 1,
                                        forever: false,
                                        blocks: p.children
                                    });
                                    advancePC = false;
                                }
                                break;
                            }
                            case 'once': {
                                advancePC = executeOnceBlock(block, state);
                                break;
                            }
                            case 'set_laser': {
                                state.variables.warningTime = evalExpr(p.warningTime || '1.0', state.variables, block, 'warningTime');
                                state.variables.activeTime = evalExpr(p.activeTime || '1.5', state.variables, block, 'activeTime');
                                state.variables.laserWidth = evalExpr(p.laserWidth || '12', state.variables, block, 'laserWidth');
                                if (state.variables.laserStartTime === null || state.variables.laserStartTime === undefined) {
                                    state.variables.laserStartTime = state.variables.timer;
                                    b.laserStartX = b.x;
                                    b.laserStartY = b.y;
                                }
                                break;
                            }
                            case 'bullet_image_set': {
                                let img = p.bulletImage || p.value || 'none';
                                if (typeof resolveBulletImageParam === 'function') {
                                    img = resolveBulletImageParam(img, state.variables);
                                } else {
                                    img = String(img).trim().replace(/^['"]|['"]$/g, '');
                                    if (state.variables && state.variables[img] !== undefined) img = state.variables[img];
                                }
                                img = String(img).trim().replace(/^['"]|['"]$/g, '');
                                state.variables.bulletImage = img;
                                state.variables.image = img;
                                if (b) {
                                    b.bulletImage = img;
                                    if (typeof isBulletImageKey === 'function' && window.paletteBulletSet && window.paletteBulletSet.has(img)) {
                                        b.bulletType = 'palette';
                                    }
                                }
                                break;
                            }
                            case 'spawn_bullet':
                            case 'spawn_trail':
                            case 'spawn_trail_resist': {
                                let isTrail = p.type === 'spawn_trail' || p.type === 'spawn_trail_resist';
                                let speed = evalExpr(p.speed, state.variables, block, 'speed');
                                let angle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');

                                let coordMode = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (coordMode === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    let baseX = state.variables.x !== undefined ? state.variables.x : b.x;
                                    let baseY = state.variables.y !== undefined ? (isPlayerSide ? canvas.height - state.variables.y : state.variables.y) : b.y;
                                    spawnX = baseX + (state.variables.x_offset || 0) + ox;
                                    spawnY = baseY + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                
                                let bRadius = evalExpr(p.radius || (isTrail ? '8' : '6'), state.variables, block, 'radius');
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bImg = isTrail ? 'none' : (p.bulletImage || 'none');

                                let angleRad = angle * Math.PI / 180;
                                if (isPlayerSide) {
                                    angleRad = -angleRad;
                                }

                                let newBullet = {
                                    x: spawnX,
                                    y: spawnY,
                                    startX: spawnX,
                                    startY: spawnY,
                                    vx: Math.cos(angleRad) * speed,
                                    vy: Math.sin(angleRad) * speed,
                                    radius: bRadius,
                                    hitRadius: bHitRadius,
                                    bulletImage: bImg,
                                    team: attacker.team,
                                    color: bColor,
                                    customDmg: 20,
                                    isCustom: true,
                                    update: null
                                };

                                if (p.bulletType === 'laser') {
                                    newBullet.isLaser = true;
                                }
                                if (p.bulletType === 'trail') {
                                    isTrail = true;
                                }
                                if (isTrail) {
                                    newBullet.isTrail = true;
                                    newBullet.growTime = (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables, block, 'growTime')) : 0.2;
                                    newBullet.keepTime = (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables, block, 'keepTime')) : 0.3;
                                    newBullet.shrinkTime = (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables, block, 'shrinkTime')) : 0.5;
                                    newBullet.round = (p.round !== undefined) ? (p.round === 'true' || p.round === true) : true;
                                    newBullet.trailHistory = [];
                                }
                                if (p.type === 'spawn_trail_resist') {
                                    newBullet.destroyResist = true;
                                }

                                newBullet.threatWeight = _util._computeBulletThreatWeight(
                                    spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                                );

                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                newBullet.bulletState = _util._initBulletState(childScript, speed, angle, attacker, target, childFn);
                                newBullet.bulletState.id = state.id || null;
                                newBullet.bulletState.bulletScript = childScript;
                                newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                newBullet.bulletState.isPlayerSide = isPlayerSide;
                                inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                newBullet.bulletState.variables.bulletType = isTrail ? 'trail' : (p.bulletType || 'normal');
                                if (isTrail) {
                                    newBullet.bulletState.variables.growTime = p.growTime || '0.2';
                                    newBullet.bulletState.variables.keepTime = p.keepTime || '0.3';
                                    newBullet.bulletState.variables.shrinkTime = p.shrinkTime || '0.5';
                                    newBullet.bulletState.variables.round = (p.round !== undefined) ? String(p.round) : 'true';
                                }
                                newBullet.bulletState.variables.color = bColor;
                                newBullet.bulletState.variables.radius = bRadius;
                                newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                newBullet.bulletState.variables.bulletImage = bImg;
                                newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                if (hasScript) {
                                    newBullet.update = (childB, childBDT) => {
                                        _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                    };
                                } else {
                                    newBullet.update = null;
                                }

                                bullets.push(newBullet);
                                break;
                            }
                            case 'spawn_ring': {
                                let speed = evalExpr(p.speed, state.variables, block, 'speed');
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                                let count = Math.max(1, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables, block, 'count'))));
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');

                                let coordMode = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (coordMode === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                
                                let bRadius = evalExpr(p.radius || '6', state.variables, block, 'radius');
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bImg = p.bulletImage || 'none';
                                let centerAngle = evalExpr(p.angle || '0', state.variables, block, 'angle');

                                for (let k = 0; k < count; k++) {
                                    let angle = centerAngle + (360 / count) * k;
                                    let angleRad = angle * Math.PI / 180;
                                    if (isPlayerSide) {
                                        angleRad = -angleRad;
                                    }
                                    
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: Math.cos(angleRad) * speed,
                                        vy: Math.sin(angleRad) * speed,
                                        radius: bRadius,
                                        hitRadius: bHitRadius,
                                        bulletImage: bImg,
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        update: null
                                    };
                                    
                                    if (p.bulletType === 'laser') {
                                        newBullet.isLaser = true;
                                    }
                                    
                                    newBullet.threatWeight = _util._computeBulletThreatWeight(
                                        spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                                    );

                                    let isFromBullet = typeof b !== 'undefined' && b !== null;
                                    let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                    let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                    let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                    let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                    let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                    newBullet.bulletState = _util._initBulletState(childScript, speed, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = bRadius;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.bulletState.variables.bulletImage = bImg;
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'spawn_way': {
                                let speed = evalExpr(p.speed, state.variables, block, 'speed');
                                let centerAngle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color, state.variables);
                                let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables, block, 'count'))));
                                let spread = evalExpr(p.spread || '15', state.variables, block, 'spread');
                                
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');

                                let coordMode = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (coordMode === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                
                                let bRadius = evalExpr(p.radius || '6', state.variables, block, 'radius');
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bImg = p.bulletImage || 'none';

                                let startAngle = centerAngle - (spread * (count - 1)) / 2;
                                
                                for (let k = 0; k < count; k++) {
                                    let angle = startAngle + spread * k;
                                    let angleRad = angle * Math.PI / 180;
                                    if (isPlayerSide) {
                                        angleRad = -angleRad;
                                    }
                                    
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: Math.cos(angleRad) * speed,
                                        vy: Math.sin(angleRad) * speed,
                                        radius: bRadius,
                                        hitRadius: bHitRadius,
                                        bulletImage: bImg,
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        update: null
                                    };
                                    
                                    if (p.bulletType === 'laser') {
                                        newBullet.isLaser = true;
                                    }
                                    
                                    newBullet.threatWeight = _util._computeBulletThreatWeight(
                                        spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y
                                    );

                                    let isFromBullet = typeof b !== 'undefined' && b !== null;
                                    let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                    let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                    let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                    let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                    let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                    newBullet.bulletState = _util._initBulletState(childScript, speed, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = bRadius;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.bulletState.variables.bulletImage = bImg;
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'spawn_beam':
                            case 'spawn_beam_resist': {
                                let wt = Number(evalExpr(p.warningTime || '1.0', state.variables, block, 'warningTime'));
                                let at = Number(evalExpr(p.activeTime || '1.5', state.variables, block, 'activeTime'));
                                let lw = Number(evalExpr(p.laserWidth || '12', state.variables, block, 'laserWidth'));
                                let angle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');
                                let cm = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (cm === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);

                                let angleRad = angle * Math.PI / 180;
                                if (isPlayerSide) angleRad = -angleRad;

                                let newBullet = {
                                    x: spawnX,
                                    y: spawnY,
                                    startX: spawnX,
                                    startY: spawnY,
                                    vx: 0,
                                    vy: 0,
                                    radius: 8,
                                    hitRadius: bHitRadius,
                                    bulletImage: 'none',
                                    team: attacker.team,
                                    color: bColor,
                                    customDmg: 20,
                                    isCustom: true,
                                    isWarningLaser: true,
                                    isLaser: false,
                                    isCustomBeam: false,
                                    laserStartX: spawnX,
                                    laserStartY: spawnY,
                                    update: null
                                };
                                if (p.type === 'spawn_beam_resist') {
                                    newBullet.destroyResist = true;
                                }
                                newBullet.threatWeight = 100;
                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                newBullet.bulletState = _util._initBulletState(childScript, 0, angle, attacker, target, childFn);
                                newBullet.bulletState.id = state.id || null;
                                newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                newBullet.bulletState.bulletScript = childScript;
                                newBullet.bulletState.isPlayerSide = isPlayerSide;
                                inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                newBullet.bulletState.variables.bulletType = 'laser';
                                newBullet.bulletState.variables.warningTime = String(wt);
                                newBullet.bulletState.variables.activeTime = String(at);
                                newBullet.bulletState.variables.laserWidth = String(lw);
                                newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                                newBullet.bulletState.variables.timer = 0;
                                newBullet.bulletState.variables.color = bColor;
                                newBullet.bulletState.variables.radius = 8;
                                newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                if (hasScript) {
                                    newBullet.update = (childB, childBDT) => {
                                        _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                    };
                                } else {
                                    newBullet.update = null;
                                }
                                bullets.push(newBullet);
                                break;
                            }
                            case 'spawn_laser_way':
                            case 'spawn_laser_way_resist': {
                                let speed = evalExpr(p.speed || '200', state.variables, block, 'speed');
                                let centerAngle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color || '#ff3333', state.variables);
                                let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables, block, 'count'))));
                                let spread = evalExpr(p.spread || '45', state.variables, block, 'spread');
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');
                                let cm = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (cm === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                let bRadius = evalExpr(p.radius || '6', state.variables, block, 'radius');
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                for (let k = 0; k < count; k++) {
                                    let angle = startAngle + spread * k;
                                    let angleRad = angle * Math.PI / 180;
                                    if (isPlayerSide) angleRad = -angleRad;
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: Math.cos(angleRad) * speed,
                                        vy: Math.sin(angleRad) * speed,
                                        radius: bRadius,
                                        hitRadius: bHitRadius,
                                        bulletImage: 'none',
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        isTrail: true,
                                        trailHistory: [],
                                        growTime: (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables, block, 'growTime')) : 0.2,
                                        keepTime: (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables, block, 'keepTime')) : 0.3,
                                        shrinkTime: (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables, block, 'shrinkTime')) : 0.5,
                                        round: p.round !== 'false' && p.round !== false,
                                        update: null
                                    };
                                    if (p.type === 'spawn_laser_way_resist') {
                                        newBullet.destroyResist = true;
                                    }
                                    newBullet.threatWeight = _util._computeBulletThreatWeight(spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y);
                                    newBullet.bulletState = _util._initBulletState(childScript, speed, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = bRadius;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.bulletState.variables.growTime = newBullet.growTime;
                                    newBullet.bulletState.variables.keepTime = newBullet.keepTime;
                                    newBullet.bulletState.variables.shrinkTime = newBullet.shrinkTime;
                                    newBullet.bulletState.variables.bulletImage = 'none';
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'spawn_laser_ring':
                            case 'spawn_laser_ring_resist': {
                                let speed = evalExpr(p.speed || '200', state.variables, block, 'speed');
                                let centerAngle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam(p.color || '#ff3333', state.variables);
                                let count = Math.max(3, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables, block, 'count'))));
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');
                                let cm = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (cm === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                let bRadius = evalExpr(p.radius || '6', state.variables, block, 'radius');
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                for (let k = 0; k < count; k++) {
                                    let angle = centerAngle + (360 / count) * k;
                                    let angleRad = angle * Math.PI / 180;
                                    if (isPlayerSide) angleRad = -angleRad;
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: Math.cos(angleRad) * speed,
                                        vy: Math.sin(angleRad) * speed,
                                        radius: bRadius,
                                        hitRadius: bHitRadius,
                                        bulletImage: 'none',
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        isTrail: true,
                                        trailHistory: [],
                                        growTime: (p.growTime !== undefined && p.growTime !== '') ? Number(evalExpr(p.growTime, state.variables, block, 'growTime')) : 0.2,
                                        keepTime: (p.keepTime !== undefined && p.keepTime !== '') ? Number(evalExpr(p.keepTime, state.variables, block, 'keepTime')) : 0.3,
                                        shrinkTime: (p.shrinkTime !== undefined && p.shrinkTime !== '') ? Number(evalExpr(p.shrinkTime, state.variables, block, 'shrinkTime')) : 0.5,
                                        round: p.round !== 'false' && p.round !== false,
                                        update: null
                                    };
                                    if (p.type === 'spawn_laser_ring_resist') {
                                        newBullet.destroyResist = true;
                                    }
                                    newBullet.threatWeight = _util._computeBulletThreatWeight(spawnX, spawnY, newBullet.vx, newBullet.vy, target.x, target.y);
                                    newBullet.bulletState = _util._initBulletState(childScript, speed, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = bRadius;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.bulletState.variables.growTime = newBullet.growTime;
                                    newBullet.bulletState.variables.keepTime = newBullet.keepTime;
                                    newBullet.bulletState.variables.shrinkTime = newBullet.shrinkTime;
                                    newBullet.bulletState.variables.bulletImage = 'none';
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'spawn_beam_way':
                            case 'spawn_beam_way_resist': {
                                let wt = Number(evalExpr(p.warningTime || '1.0', state.variables, block, 'warningTime'));
                                let at = Number(evalExpr(p.activeTime || '1.5', state.variables, block, 'activeTime'));
                                let lw = Number(evalExpr(p.laserWidth || '12', state.variables, block, 'laserWidth'));
                                let centerAngle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let count = Math.max(1, Math.min(CUSTOM_SPAWN_WAY_MAX_COUNT, parseInt(evalExpr(p.count || '3', state.variables, block, 'count'))));
                                let spread = evalExpr(p.spread || '45', state.variables, block, 'spread');
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');
                                let cm = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (cm === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);
                                let startAngle = centerAngle - (spread * (count - 1)) / 2;
                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                for (let k = 0; k < count; k++) {
                                    let angle = startAngle + spread * k;
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: 0,
                                        vy: 0,
                                        radius: 8,
                                        hitRadius: bHitRadius,
                                        bulletImage: 'none',
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        isWarningLaser: true,
                                        isLaser: false,
                                        isCustomBeam: false,
                                        laserStartX: spawnX,
                                        laserStartY: spawnY,
                                        update: null
                                    };
                                    if (p.type === 'spawn_beam_way_resist') {
                                        newBullet.destroyResist = true;
                                    }
                                    newBullet.threatWeight = 100;
                                    newBullet.bulletState = _util._initBulletState(childScript, 0, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.bulletType = 'laser';
                                    newBullet.bulletState.variables.warningTime = String(wt);
                                    newBullet.bulletState.variables.activeTime = String(at);
                                    newBullet.bulletState.variables.laserWidth = String(lw);
                                    newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                                    newBullet.bulletState.variables.timer = 0;
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = 8;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'spawn_beam_ring':
                            case 'spawn_beam_ring_resist': {
                                let wt = Number(evalExpr(p.warningTime || '1.0', state.variables, block, 'warningTime'));
                                let at = Number(evalExpr(p.activeTime || '1.5', state.variables, block, 'activeTime'));
                                let lw = Number(evalExpr(p.laserWidth || '12', state.variables, block, 'laserWidth'));
                                let centerAngle = evalExpr(p.angle || 'angle', state.variables, block, 'angle');
                                let count = Math.max(3, Math.min(CUSTOM_SPAWN_RING_MAX_COUNT, parseInt(evalExpr(p.count || '12', state.variables, block, 'count'))));
                                let ox = evalExpr(p.offsetX || '0', state.variables, block, 'offsetX');
                                let oy = evalExpr(p.offsetY || '0', state.variables, block, 'offsetY');
                                let cm = p.coordMode || 'relative';
                                let spawnX, spawnY;
                                if (cm === 'absolute') {
                                    spawnX = ox;
                                    spawnY = isPlayerSide ? (canvas.height - oy) : oy;
                                } else {
                                    spawnX = b.x + (state.variables.x_offset || 0) + ox;
                                    spawnY = b.y + (state.variables.y_offset || 0) + (isPlayerSide ? -oy : oy);
                                }
                                let bHitRadius = p.hitRadius ? evalExpr(p.hitRadius, state.variables, block, 'hitRadius') : undefined;
                                if (bHitRadius !== undefined) {
                                    let hrNum = Number(bHitRadius);
                                    if (!isNaN(hrNum) && hrNum < 0.1) bHitRadius = 0;
                                }
                                let bColor = window.DanmakuCompilerRuntime.resolveColorParam('#ff3333', state.variables);
                                let isFromBullet = typeof b !== 'undefined' && b !== null;
                                let defaultScript = isFromBullet ? (state.magicCircleScript || []) : (state.bulletScript || []);
                                let defaultFn = isFromBullet ? ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_magic'] : null) : ((window.compiledDanmaku && state.id) ? window.compiledDanmaku[state.id + '_bullet'] : (state.compiledFn || null));
                                let childScript = (p.children && p.children.length > 0) ? p.children : defaultScript;
                                let childFn = (p.children && p.children.length > 0) ? (block.compiledFn || null) : defaultFn;
                                let hasScript = (childScript && childScript.length > 0) || !!childFn;
                                for (let k = 0; k < count; k++) {
                                    let angle = centerAngle + (360 / count) * k;
                                    let newBullet = {
                                        x: spawnX,
                                        y: spawnY,
                                        startX: spawnX,
                                        startY: spawnY,
                                        vx: 0,
                                        vy: 0,
                                        radius: 8,
                                        hitRadius: bHitRadius,
                                        bulletImage: 'none',
                                        team: attacker.team,
                                        color: bColor,
                                        customDmg: 20,
                                        isCustom: true,
                                        isWarningLaser: true,
                                        isLaser: false,
                                        isCustomBeam: false,
                                        laserStartX: spawnX,
                                        laserStartY: spawnY,
                                        update: null
                                    };
                                    if (p.type === 'spawn_beam_ring_resist') {
                                        newBullet.destroyResist = true;
                                    }
                                    newBullet.threatWeight = 100;
                                    newBullet.bulletState = _util._initBulletState(childScript, 0, angle, attacker, target, childFn);
                                    newBullet.bulletState.id = state.id || null;
                                    newBullet.bulletState.magicCircleScript = state.magicCircleScript || [];
                                    newBullet.bulletState.bulletScript = childScript;
                                    newBullet.bulletState.isPlayerSide = isPlayerSide;
                                    inheritEmitterVariablesToBullet(state, newBullet.bulletState);
                                    newBullet.bulletState.variables.bulletType = 'laser';
                                    newBullet.bulletState.variables.warningTime = String(wt);
                                    newBullet.bulletState.variables.activeTime = String(at);
                                    newBullet.bulletState.variables.laserWidth = String(lw);
                                    newBullet.bulletState.variables.laserStartTime = state.variables.timer;
                                    newBullet.bulletState.variables.timer = 0;
                                    newBullet.bulletState.variables.color = bColor;
                                    newBullet.bulletState.variables.radius = 8;
                                    newBullet.bulletState.variables.hitRadius = bHitRadius !== undefined ? bHitRadius : '';
                                    newBullet.sharedEmitterState = state.sharedEmitterState || state;
                                    if (hasScript) {
                                        newBullet.update = (childB, childBDT) => {
                                            _util._runCustomBulletScript(childB, childBDT, attacker, target);
                                        };
                                    } else {
                                        newBullet.update = null;
                                    }
                                    bullets.push(newBullet);
                                }
                                break;
                            }
                            case 'const_var':
                            case 'set_var': {
                                let varName = p.name;
                                let val = evalValue(p.value, state.variables, block, 'value');
                                if (shouldLog) console.log(`[DEBUG] Bullet #${b.bulletDebugId} set_var: ${varName} = ${val} (raw: ${p.value})`);
                        setScriptVariable(state, varName, val, p.type === 'const_var');
                                break;
                            }
                            case 'change_var': {
                                let varName = p.name;
                                let val = evalExpr(p.value, state.variables, block, 'value');
                                let delta = p.op === '-' ? -val : val;
                                let before = state.variables[varName];
                                if (!state.constVars || typeof state.constVars.has !== 'function') state.constVars = new Set();
                                if (!state.constVars.has(varName)) state.variables[varName] = (Number(state.variables[varName]) || 0) + delta;
                                if (shouldLog) console.log(`[DEBUG] Bullet #${b.bulletDebugId} change_var: ${varName} ${p.op === '-' ? '-=' : '+='} ${val} (before: ${before}, after: ${state.variables[varName]})`);
                                break;
                            }
                            case 'aim_at_target': {
                                let dx = target.x - b.x;
                                let dy = isPlayerSide ? (b.y - target.y) : (target.y - b.y);
                                state.variables.angle = Math.atan2(dy, dx) * 180 / Math.PI;
                                break;
                            }
                            case 'aim_at_coord': {
                                let txRawB = evalExpr(p.targetX || '0', state.variables, block, 'targetX');
                                let tyRawB = evalExpr(p.targetY || '0', state.variables, block, 'targetY');
                                let txAbsB = Number(txRawB) || 0;
                                let tyAbsB = isPlayerSide ? (canvas.height - (Number(tyRawB) || 0)) : (Number(tyRawB) || 0);
                                let curX = state.variables.x !== undefined ? state.variables.x : b.x;
                                let curY = state.variables.y !== undefined ? (isPlayerSide ? canvas.height - state.variables.y : state.variables.y) : b.y;
                                let dxB = txAbsB - curX;
                                let dyB = isPlayerSide ? (curY - tyAbsB) : (tyAbsB - curY);
                                state.variables.angle = Math.atan2(dyB, dxB) * 180 / Math.PI;
                                break;
                            }
                            case 'speed_scale': {
                                advancePC = applySpeedScaleBlock(block, state);
                                break;
                            }
                            case 'homing': {
                                let turnSpeed = evalExpr(p.turnSpeed || '90', state.variables, block, 'turnSpeed');
                                let dx = target.x - b.x;
                                let dy = isPlayerSide ? (b.y - target.y) : (target.y - b.y);
                                let targetAngle = Math.atan2(dy, dx) * 180 / Math.PI;
                                
                                let diff = targetAngle - state.variables.angle;
                                diff = ((diff + 180) % 360 + 360) % 360 - 180;
                                
                                let step = turnSpeed * dt;
                                if (Math.abs(diff) < step) {
                                    state.variables.angle = targetAngle;
                                } else {
                                    state.variables.angle += Math.sign(diff) * step;
                                }
                                break;
                            }
                            case 'bounce': {
                                let x = state.variables.x !== undefined ? state.variables.x : (b ? b.x : 0);
                                let y = state.variables.y !== undefined ? state.variables.y : (b ? b.y : 0);
                                let angle = Number(state.variables.angle) || 0;
                                let bounced = false;
                                if (x < 10 && Math.cos(angle * Math.PI / 180) < 0) {
                                    angle = 180 - angle;
                                    x = 10;
                                    if (b) b.x = 10;
                                    bounced = true;
                                }
                                if (x > PLAY_WIDTH - 10 && Math.cos(angle * Math.PI / 180) > 0) {
                                    angle = 180 - angle;
                                    x = PLAY_WIDTH - 10;
                                    if (b) b.x = PLAY_WIDTH - 10;
                                    bounced = true;
                                }
                                if (y < 10 && Math.sin(angle * Math.PI / 180) < 0) {
                                    angle = -angle;
                                    y = 10;
                                    if (b) b.y = 10;
                                    bounced = true;
                                }
                                state.variables.x = x;
                                state.variables.y = y;
                                state.variables.angle = ((angle % 360) + 360) % 360;
                                if (bounced) {
                                    state.variables.isBounced = 1;
                                    if (b) {
                                        let finalRad = state.variables.angle * Math.PI / 180;
                                        if (state.isPlayerSide) finalRad = -finalRad;
                                        let spd = Number(state.variables.speed) || Math.sqrt(b.vx * b.vx + b.vy * b.vy);
                                        b.vx = Math.cos(finalRad) * spd;
                                        b.vy = Math.sin(finalRad) * spd;
                                    }
                                }
                                break;
                            }
                            case 'advance': {
                                let dist = evalExpr(p.distance || '50', state.variables, block, 'distance');
                                let rad = (Number(state.variables.angle) || 0) * Math.PI / 180;
                                state.variables.x = (Number(state.variables.x) || 0) + Math.cos(rad) * dist;
                                state.variables.y = (Number(state.variables.y) || 0) + Math.sin(rad) * dist;
                                break;
                            }
                            case 'tween_angle':
                            case 'tween_angle_wait': {
                                let varName = 'angle';
                                let fromVal = evalExpr(p.from || '0', state.variables);
                                let toVal = evalExpr(p.to || '360', state.variables);
                                let duration = evalExpr(p.duration || '1', state.variables);
                                let mode = p.mode || 'seconds';
                                if (mode === 'frames') duration = Math.max(1, duration);
                                else duration = Math.max(0.001, duration);
                                
                                if (!state.tweens) state.tweens = [];
                                state.tweens = state.tweens.filter(t => t.name !== varName);
                                
                                state.tweens.push({
                                    name: varName,
                                    mode: mode,
                                    from: fromVal,
                                    to: toVal,
                                    total: duration,
                                    elapsed: 0,
                                    easing: p.easing || 'linear',
                                    isAngleTween: true,
                                    rotMode: p.rotMode || 'shortest'
                                });
                                
                                if (p.type === 'tween_angle_wait') {
                                    state.waitingTweenName = varName;
                                }
                                break;
                            }
                            case 'tween_var':
                            case 'tween_var_wait': {
                                let varName = p.name || 'angle';
                                let isMultiVar = varName.includes(',');
                                
                                let fromVal, toVal;
                                if (isMultiVar) {
                                    let fromParts = (p.from || '').split(',').map(p => evalExpr(p.trim(), state.variables, block, 'from'));
                                    let toParts = (p.to || '').split(',').map(p => evalExpr(p.trim(), state.variables, block, 'to'));
                                    fromVal = `${fromParts[0] || 0},${fromParts[1] || 0}`;
                                    toVal = `${toParts[0] || 0},${toParts[1] || 0}`;
                                } else {
                                    fromVal = evalExpr(p.from, state.variables, block, 'from');
                                    toVal   = evalExpr(p.to,   state.variables, block, 'to');
                                }

                                let mode    = p.mode || 'seconds';
                                if (!state.tweens) state.tweens = [];
                                state.tweens = state.tweens.filter(t => t.name !== varName);

                                if (typeof toVal === 'string' && toVal.includes(',')) {
                                    let toParts = toVal.split(',').map(p => parseFloat(p.trim()));
                                    let fromParts = String(fromVal).split(',').map(p => parseFloat(p.trim()));
                                    if (toParts.length === 2 && fromParts.length === 2 && !isNaN(toParts[0]) && !isNaN(toParts[1]) && !isNaN(fromParts[0]) && !isNaN(fromParts[1])) {
                                        state.variables[varName] = fromVal;
                                        if (mode === 'step' || mode === 'vecstep') {
                                            let stepVal = evalExpr(p.stepVal || p.value || '5', state.variables, block, 'stepVal');
                                            if (mode === 'vecstep') {
                                                state.tweens.push({
                                                    name: varName,
                                                    isCoordPair: true,
                                                    isStep: true,
                                                    isVecStep: true,
                                                    fromX: fromParts[0],
                                                    toX: toParts[0],
                                                    fromY: fromParts[1],
                                                    toY: toParts[1],
                                                    stepVal: stepVal,
                                                    mode,
                                                    total: 1,
                                                    elapsed: 0
                                                });
                                            } else {
                                                let stepX = stepVal;
                                                let stepY = stepVal;
                                                if (fromParts[0] > toParts[0] && stepX > 0) stepX = -stepX;
                                                if (fromParts[1] > toParts[1] && stepY > 0) stepY = -stepY;
                                                if (fromParts[0] === toParts[0]) stepX = 0;
                                                if (fromParts[1] === toParts[1]) stepY = 0;

                                                state.tweens.push({
                                                    name: varName,
                                                    isCoordPair: true,
                                                    isStep: true,
                                                    isVecStep: false,
                                                    fromX: fromParts[0],
                                                    toX: toParts[0],
                                                    fromY: fromParts[1],
                                                    toY: toParts[1],
                                                    stepX: stepX,
                                                    stepY: stepY,
                                                    mode,
                                                    total: 1,
                                                    elapsed: 0
                                                });
                                            }
                                        } else {
                                            let total = evalExpr(p.duration || '1', state.variables, block, 'duration');
                                            if (mode === 'frames') total = Math.max(1, total);
                                            else total = Math.max(0.001, total);
                                            state.tweens.push({
                                                name: varName,
                                                isCoordPair: true,
                                                isStep: false,
                                                fromX: fromParts[0],
                                                toX: toParts[0],
                                                fromY: fromParts[1],
                                                toY: toParts[1],
                                                mode,
                                                total,
                                                elapsed: 0,
                                                easing: p.easing || 'linear'
                                            });
                                        }
                                        state.variables[varName + '_x'] = fromParts[0];
                                        state.variables[varName + '_y'] = fromParts[1];
                                        state.variables[varName + '.x'] = fromParts[0];
                                        state.variables[varName + '.y'] = fromParts[1];
                                        if (p.type === 'tween_var_wait') {
                                            state.waitingTweenName = varName;
                                        }
                                    }
                                } else {
                                    if (mode === 'step') {
                                        let stepVal = evalExpr(p.stepVal || '5', state.variables, block, 'stepVal');
                                        if (fromVal > toVal && stepVal > 0) stepVal = -stepVal;
                                        state.variables[varName] = fromVal;
                                        state.tweens.push({ name: varName, to: toVal, mode: 'step', stepVal });
                                    } else {
                                        let total = evalExpr(p.duration || '1', state.variables, block, 'duration');
                                        if (mode === 'frames') total = Math.max(1, total);
                                        else total = Math.max(0.001, total);
                                        let fNum = Number(fromVal);
                                        if (isNaN(fNum)) fNum = 0;
                                        let tNum = Number(toVal);
                                        if (isNaN(tNum)) tNum = 0;
                                        state.variables[varName] = fNum;
                                        state.tweens.push({ name: varName, from: fNum, to: tNum, mode, total, elapsed: 0, easing: p.easing || 'linear' });
                                    }
                                    if (p.type === 'tween_var_wait') {
                                        state.waitingTweenName = varName;
                                    }
                                }
                                break;
                            }
                        
        }
    
    return brokeToWait;
};

window.DanmakuCompilerRuntime.fuzzyEqual = function(a, b) {
    let sa = String(a).trim().toLowerCase();
    let sb = String(b).trim().toLowerCase();
    if (sa === sb) return true;
    let na = Number(sa);
    let nb = Number(sb);
    if (!isNaN(na) && !isNaN(nb)) return Math.abs(na - nb) < 0.0001;
    return false;
};
window.DanmakuCompilerRuntime.fuzzyNotEqual = function(a, b) { return !window.DanmakuCompilerRuntime.fuzzyEqual(a, b); };
window.DanmakuCompilerRuntime.rand = function(a, b) {
    if (b !== undefined) return Number(a || 0) + Math.random() * (Number(b || 0) - Number(a || 0));
    if (a !== undefined) return Math.random() * Number(a || 0);
    return Math.random();
};
window.DanmakuCompilerRuntime.seedrandom = function(baseSeed, a, b, vars) {
    let s = (Math.floor(Number(baseSeed)) >>> 0) + 0x6D2B79F5;
    s = Math.imul(s ^ (s >>> 15), s | 1);
    s ^= s + Math.imul(s ^ (s >>> 7), s | 61);
    let r = ((s ^ (s >>> 14)) >>> 0) / 4294967296;
    if (b !== undefined) return Number(a || 0) + r * (Number(b || 0) - Number(a || 0));
    if (a !== undefined) return r * Number(a || 0);
    return r;
};
window.DanmakuCompilerRuntime.checkInterval = function(currentVal, interval, stateKey, variables) {
    if (!interval || interval <= 0) return false;
    let prevVal = variables[stateKey];
    variables[stateKey] = currentVal;
    if (prevVal === undefined) { prevVal = 0; }
    return Math.floor(prevVal / interval) !== Math.floor(currentVal / interval);
};

window.DanmakuCompilerRuntime._getCurrentX = function(b, attacker, state) {
    if (b) {
        return state.variables.x !== undefined ? state.variables.x : b.x;
    }
    if (state && state.variables) {
        if (state.variables.ex !== undefined && !isNaN(Number(state.variables.ex))) {
            let val = Number(state.variables.ex);
            if (attacker) attacker.x = val;
            return val;
        }
        if (state.variables.emitter_x !== undefined && !isNaN(Number(state.variables.emitter_x))) {
            let val = Number(state.variables.emitter_x);
            if (attacker) attacker.x = val;
            return val;
        }
    }
    return attacker.x;
};

window.DanmakuCompilerRuntime._getCurrentY = function(b, attacker, state, canvasHeight) {
    let cH = canvasHeight || (window.canvas ? window.canvas.height : 896);
    if (b) {
        return state.variables.y !== undefined ? (state.isPlayerSide ? cH - state.variables.y : state.variables.y) : b.y;
    }
    if (state && state.variables) {
        let isPlayerSide = state.isPlayerSide;
        if (state.variables.ey !== undefined && !isNaN(Number(state.variables.ey))) {
            let val = isPlayerSide ? (cH - Number(state.variables.ey)) : Number(state.variables.ey);
            if (attacker) attacker.y = val;
            return val;
        }
        if (state.variables.emitter_y !== undefined && !isNaN(Number(state.variables.emitter_y))) {
            let val = isPlayerSide ? (cH - Number(state.variables.emitter_y)) : Number(state.variables.emitter_y);
            if (attacker) attacker.y = val;
            return val;
        }
    }
    return attacker.y;
};
window.compiledDanmaku['danmaku_0'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#33ffff", speed: "180", angle: "angle", count: "8", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (5);
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
  }
};
window.compiledDanmaku['danmaku_0_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = 180;
    if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((vars.isBounced !== undefined ? vars.isBounced : 0))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['color'] = "\"#ff33ff\"";
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_0_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_1'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['angle2'] = "0";
  vars['xspa2'] = "x";
  vars['yspa2'] = "y";
  while (true) {
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "1000", angle: "angle + 180", count: "5", spread: "72", offsetX: "0", offsetY: "0", radius: "16", bulletImage: "ohuda", coordMode: "relative", hitRadius: "2", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "1000", angle: "angle", count: "5", spread: "72", offsetX: "0", offsetY: "0", radius: "16", bulletImage: "ohuda", coordMode: "relative", hitRadius: "2", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((vars.second !== undefined ? vars.second : 0) >= 10)) {
      vars['angle2'] = (vars['angle2'] || 0) - (43.5);
    }
    vars['angle'] = (vars['angle'] || 0) + (0.2);
    state.waitTimer = Math.max(0.0167, 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['angle2'] = "0";
  vars['xspa2'] = "x";
  vars['yspa2'] = "y";
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "xspa", from: "x", to: "384", mode: "step", duration: "1", stepVal: "1", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "yspa", from: "y", to: "448", mode: "step", duration: "1", stepVal: "1", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['xspa'] = 384;
    vars['yspa'] = 448;
    state.waitTimer = Math.max(0.0167, 3);
    yield;
    while (true) {
      if (_util.executeBlock({ type: 'tween_var', name: "xspa", from: "xspa", to: "384", mode: "step", duration: "1", stepVal: "0.1", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'tween_var', name: "yspa", from: "yspa", to: "448", mode: "step", duration: "1", stepVal: "0.1", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 3);
      yield;
    }
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['angle2'] = "0";
  vars['xspa2'] = "x";
  vars['yspa2'] = "y";
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "18", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "poihuru", coordMode: "relative", hitRadius: "7", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_1_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#ffffff").trim().toLowerCase())) {
      if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,1))) {
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
          vars['x'] = (vars.e_xspa !== undefined ? vars.e_xspa : 0);
          vars['y'] = (vars.e_yspa !== undefined ? vars.e_yspa : 0);
          vars['prev_x'] = (vars.e_xspa !== undefined ? vars.e_xspa : 0);
          vars['prev_y'] = (vars.e_yspa !== undefined ? vars.e_yspa : 0);
        }
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3366ff").trim().toLowerCase())) {
          vars['x'] = (vars.e_xspa2 !== undefined ? vars.e_xspa2 : 0);
          vars['y'] = (vars.e_yspa2 !== undefined ? vars.e_yspa2 : 0);
          vars['prev_x'] = (vars.e_xspa2 !== undefined ? vars.e_xspa2 : 0);
          vars['prev_y'] = (vars.e_yspa2 !== undefined ? vars.e_yspa2 : 0);
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        vars['dx'] = (vars.e_xspa !== undefined ? vars.e_xspa : 0) - (vars.prev_x !== undefined ? vars.prev_x : 0);
        vars['dy'] = (vars.e_yspa !== undefined ? vars.e_yspa : 0) - (vars.prev_y !== undefined ? vars.prev_y : 0);
        vars['prev_x'] = (vars.e_xspa !== undefined ? vars.e_xspa : 0);
        vars['prev_y'] = (vars.e_yspa !== undefined ? vars.e_yspa : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3366ff").trim().toLowerCase())) {
        vars['dx'] = (vars.e_xspa2 !== undefined ? vars.e_xspa2 : 0) - (vars.prev_x !== undefined ? vars.prev_x : 0);
        vars['dy'] = (vars.e_yspa2 !== undefined ? vars.e_yspa2 : 0) - (vars.prev_y !== undefined ? vars.prev_y : 0);
        vars['prev_x'] = (vars.e_xspa2 !== undefined ? vars.e_xspa2 : 0);
        vars['prev_y'] = (vars.e_yspa2 !== undefined ? vars.e_yspa2 : 0);
      }
      vars['x'] = (vars['x'] || 0) + ((vars.dx !== undefined ? vars.dx : 0));
      vars['y'] = (vars['y'] || 0) + ((vars.dy !== undefined ? vars.dy : 0));
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (5) && ((vars.frame !== undefined ? vars.frame : 0)) <= (150)))) {
        vars['angle'] = (vars['angle'] || 0) + (2);
      }
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (170) && ((vars.frame !== undefined ? vars.frame : 0)) <= (250)))) {
        vars['angle'] = (vars['angle'] || 0) + (0);
      }
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (110) && ((vars.frame !== undefined ? vars.frame : 0)) <= (2510)))) {
        vars['y'] = (vars['y'] || 0) + (800000);
        vars['angle'] = (vars['angle'] || 0) - (0);
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['t47o9ydj5']) {
        state.onceMap['t47o9ydj5'] = true;
        vars['x'] = 384;
        vars['y'] = 448;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_1_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_2'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 6);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
  }
};
window.compiledDanmaku['danmaku_2_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['1tqrdcjsq']) {
      state.onceMap['1tqrdcjsq'] = true;
      vars['m'] = 1;
    }
    if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 0;
      vars['m'] = 0;
    }
    if (!!((vars.m !== undefined ? vars.m : 0)===0)) {
      let _prev_i_1 = vars['i'];
      for (let _loopIdx_1 = 0, _limit_1 = Math.round(2); _loopIdx_1 < _limit_1; _loopIdx_1++) {
        vars['i'] = _loopIdx_1;
        vars['rang'] = random(0,360);
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "rang", count: "36", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "star", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.2);
        yield;
      }
      vars['i'] = _prev_i_1;
      vars['m'] = 1;
    }
    if (!!((vars.m !== undefined ? vars.m : 0) === 1)) {
      vars['speed'] = (vars['speed'] || 0) + (2.5);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_2_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (3);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_3'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 6);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
    vars['m'] = 0;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "70", bulletImage: "ootama", coordMode: "relative", hitRadius: "70", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 10);
    yield;
  }
};
window.compiledDanmaku['danmaku_3_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['fgnvmjxkq']) {
      state.onceMap['fgnvmjxkq'] = true;
      vars['m'] = 1;
    }
    if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 0;
      vars['m'] = 0;
    }
    if (!!((vars.m !== undefined ? vars.m : 0)===0)) {
      let _prev_i_2 = vars['i'];
      for (let _loopIdx_2 = 0, _limit_2 = Math.round(5); _loopIdx_2 < _limit_2; _loopIdx_2++) {
        vars['i'] = _loopIdx_2;
        vars['rang'] = random(0,360);
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "rang", count: "36", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "star", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_2;
      vars['m'] = 1;
    }
    if (!!((vars.m !== undefined ? vars.m : 0) === 1)) {
      vars['speed'] = (vars['speed'] || 0) + (2.5);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_3_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (3);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_4'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "500", angle: "angle", count: "5", spread: "20", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "ootama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "500", angle: "angle + 50", count: "5", spread: "20", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "ootama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "500", angle: "angle - 50", count: "5", spread: "20", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "ootama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_3 = vars['i'];
    for (let _loopIdx_3 = 0, _limit_3 = Math.round(36); _loopIdx_3 < _limit_3; _loopIdx_3++) {
      vars['i'] = _loopIdx_3;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3333ff", speed: "500", angle: "angle", count: "1", spread: "10", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (25);
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_3;
    state.waitTimer = Math.max(0.0167, 3);
    yield;
  }
};
window.compiledDanmaku['danmaku_4_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#ffffff").trim().toLowerCase())) {
      if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
        vars['angle'] = 180;
        vars['warningTime'] = 1;
        vars['activeTime'] = 1.5;
        vars['laserWidth'] = 20;
      }
      if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
        vars['angle'] = 90;
        vars['warningTime'] = 1;
        vars['activeTime'] = 1.5;
        vars['laserWidth'] = 20;
      }
      if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
        vars['angle'] = 0;
        vars['warningTime'] = 1;
        vars['activeTime'] = 1.5;
        vars['laserWidth'] = 20;
      }
      if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
        vars['angle'] = 270;
        vars['warningTime'] = 1;
        vars['activeTime'] = 1.5;
        vars['laserWidth'] = 20;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_4_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_5'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_4 = vars['i'];
  for (let _loopIdx_4 = 0, _limit_4 = Math.round(1200000000); _loopIdx_4 < _limit_4; _loopIdx_4++) {
    vars['i'] = _loopIdx_4;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "36 + way", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['way'] = (vars['way'] || 0) + (2);
    vars['angle'] = (vars['angle'] || 0) + (16);
    state.waitTimer = Math.max(0.0167, 0.26);
    yield;
  }
  vars['i'] = _prev_i_4;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'move_owner', preset: "center", duration: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_5_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.isBounced !== undefined ? vars.isBounced : 0))) {
      vars['y'] = -80000;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
      vars['y'] = -80000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_5_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_6'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "150", angle: "45", offsetX: "0", offsetY: "0", radius: "100", bulletImage: "b_knife", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "150", angle: "45 + 90 + 90", offsetX: "0", offsetY: "0", radius: "100", bulletImage: "b_knife", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 200);
    yield;
  }
};
window.compiledDanmaku['danmaku_6_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['kwbch04ax']) {
      state.onceMap['kwbch04ax'] = true;
      vars['shotTimer'] = 0;
    }
    vars['speed'] = (vars['speed'] || 0) + (2);
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
      vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 150;
      vars['y'] = 886;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
      vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 150;
      vars['y'] = 10;
    }
    if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
      vars['angle'] = 180 - (vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 150;
      vars['x'] = 10;
    }
    if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
      vars['angle'] = 180 - (vars.angle !== undefined ? vars.angle : 0);
      vars['speed'] = 150;
      vars['x'] = 758;
    }
    vars['shotTimer'] = (vars.shotTimer !== undefined ? vars.shotTimer : 0) + 1;
    if (!!((vars.shotTimer !== undefined ? vars.shotTimer : 0) >= 12)) {
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3388ff", speed: "200", angle: "angle + 180", count: "5", spread: "72", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['shotTimer'] = 0;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_6_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (5);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_7'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = -90 + random(-20,20);
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (30)))) {
      vars['angle'] = (vars['angle'] || 0) + (random(-30,30));
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (30) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (35)))) {
      vars['angle'] = (vars['angle'] || 0) + (random(-50,50));
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "0", offsetY: "886", radius: "20", bulletImage: "poihuru", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (15)))) {
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (30)))) {
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (30) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (35)))) {
      state.waitTimer = Math.max(0.0167, 0.0002);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_7_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!!((vars.y !== undefined ? vars.y : 0) < 0)) {
        vars['angle'] = 90 + random(-5,5);
        vars['speed'] = 5;
        vars['color'] = "#33ffff";
        vars['y'] = 5;
      }
      vars['speed'] = (vars['speed'] || 0) + (0.4);
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      vars['speed'] = (vars['speed'] || 0) + (2);
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_7_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_8'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_5 = vars['i'];
  for (let _loopIdx_5 = 0, _limit_5 = Math.round(1200000); _loopIdx_5 < _limit_5; _loopIdx_5++) {
    vars['i'] = _loopIdx_5;
    vars['spawn'] = random(0,768);
    let _prev_i_6 = vars['i'];
    for (let _loopIdx_6 = 0, _limit_6 = Math.round(3); _loopIdx_6 < _limit_6; _loopIdx_6++) {
      vars['i'] = _loopIdx_6;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "90", offsetX: "0", offsetY: "-200", radius: "7", bulletImage: "uroko", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
    }
    vars['i'] = _prev_i_6;
  }
  vars['i'] = _prev_i_5;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_7 = vars['i'];
  for (let _loopIdx_7 = 0, _limit_7 = Math.round(1200000); _loopIdx_7 < _limit_7; _loopIdx_7++) {
    vars['i'] = _loopIdx_7;
    vars['spawn2'] = random(0,768);
    let _prev_i_8 = vars['i'];
    for (let _loopIdx_8 = 0, _limit_8 = Math.round(5); _loopIdx_8 < _limit_8; _loopIdx_8++) {
      vars['i'] = _loopIdx_8;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3323", speed: "300", angle: "90", offsetX: "0", offsetY: "-200", radius: "7", bulletImage: "uroko", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
    }
    vars['i'] = _prev_i_8;
  }
  vars['i'] = _prev_i_7;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_9 = vars['i'];
  for (let _loopIdx_9 = 0, _limit_9 = Math.round(1200000); _loopIdx_9 < _limit_9; _loopIdx_9++) {
    vars['i'] = _loopIdx_9;
    vars['spawn3'] = random(0,768);
    let _prev_i_10 = vars['i'];
    for (let _loopIdx_10 = 0, _limit_10 = Math.round(7); _loopIdx_10 < _limit_10; _loopIdx_10++) {
      vars['i'] = _loopIdx_10;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "300", angle: "90", offsetX: "0", offsetY: "-200", radius: "7", bulletImage: "uroko", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
    }
    vars['i'] = _prev_i_10;
  }
  vars['i'] = _prev_i_9;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "90", offsetX: "0", offsetY: "-150", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "", }, state, b, attacker, target, _util)) {
      yield;
    }
  }
}
];
window.compiledDanmaku['danmaku_8_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['ow8u4gpsh']) {
      state.onceMap['ow8u4gpsh'] = true;
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        vars['x'] = (vars.spawn !== undefined ? vars.spawn : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        vars['x'] = (vars.spawn2 !== undefined ? vars.spawn2 : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3323").trim().toLowerCase())) {
        vars['x'] = (vars.spawn3 !== undefined ? vars.spawn3 : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
        vars['x'] = (vars.tx !== undefined ? vars.tx : 0);
        vars['warningTime'] = 3;
        vars['activeTime'] = 1.5;
        vars['laserWidth'] = 900;
      }
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 896)) {
      vars['y'] = -580000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_8_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_9'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_11 = vars['i'];
    for (let _loopIdx_11 = 0, _limit_11 = Math.round(20); _loopIdx_11 < _limit_11; _loopIdx_11++) {
      vars['i'] = _loopIdx_11;
      vars['ys'] = 0;
      let _prev_i_12 = vars['i'];
      for (let _loopIdx_12 = 0, _limit_12 = Math.round(10); _loopIdx_12 < _limit_12; _loopIdx_12++) {
        vars['i'] = _loopIdx_12;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "0", offsetX: "-100", offsetY: "ys + 15", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "0", offsetX: "-100", offsetY: "ys - 15", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['ys'] = (vars['ys'] || 0) + (100);
      }
      vars['i'] = _prev_i_12;
      vars['ys'] = 0;
      let _prev_i_13 = vars['i'];
      for (let _loopIdx_13 = 0, _limit_13 = Math.round(10); _loopIdx_13 < _limit_13; _loopIdx_13++) {
        vars['i'] = _loopIdx_13;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "180", offsetX: "868", offsetY: "ys + 65", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "180", offsetX: "868", offsetY: "ys + 35", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['ys'] = (vars['ys'] || 0) + (100);
      }
      vars['i'] = _prev_i_13;
      state.waitTimer = Math.max(0.0167, 0.7);
      yield;
    }
    vars['i'] = _prev_i_11;
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    let _prev_i_14 = vars['i'];
    for (let _loopIdx_14 = 0, _limit_14 = Math.round(20); _loopIdx_14 < _limit_14; _loopIdx_14++) {
      vars['i'] = _loopIdx_14;
      vars['xs'] = 0;
      let _prev_i_15 = vars['i'];
      for (let _loopIdx_15 = 0, _limit_15 = Math.round(10); _loopIdx_15 < _limit_15; _loopIdx_15++) {
        vars['i'] = _loopIdx_15;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "90", offsetX: "xs + 15", offsetY: "0", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "90", offsetX: "xs - 15", offsetY: "0", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (100);
      }
      vars['i'] = _prev_i_15;
      vars['xs'] = 0;
      let _prev_i_16 = vars['i'];
      for (let _loopIdx_16 = 0, _limit_16 = Math.round(10); _loopIdx_16 < _limit_16; _loopIdx_16++) {
        vars['i'] = _loopIdx_16;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "-90", offsetX: "xs + 15 + 50", offsetY: "996", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "-90", offsetX: "xs - 15 + 50", offsetY: "996", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (100);
      }
      vars['i'] = _prev_i_16;
      state.waitTimer = Math.max(0.0167, 0.7);
      yield;
    }
    vars['i'] = _prev_i_14;
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    let _prev_i_17 = vars['i'];
    for (let _loopIdx_17 = 0, _limit_17 = Math.round(20); _loopIdx_17 < _limit_17; _loopIdx_17++) {
      vars['i'] = _loopIdx_17;
      vars['xs'] = -650;
      vars['ys'] = 550;
      let _prev_i_18 = vars['i'];
      for (let _loopIdx_18 = 0, _limit_18 = Math.round(25); _loopIdx_18 < _limit_18; _loopIdx_18++) {
        vars['i'] = _loopIdx_18;
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) + (11);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "45", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (22);
        vars['ys'] = (vars['ys'] || 0) - (22);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "45", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) + (11);
        vars['xs'] = (vars['xs'] || 0) + (71);
        vars['ys'] = (vars['ys'] || 0) - (71);
      }
      vars['i'] = _prev_i_18;
      vars['xs'] = 1500;
      vars['ys'] = 357;
      let _prev_i_19 = vars['i'];
      for (let _loopIdx_19 = 0, _limit_19 = Math.round(25); _loopIdx_19 < _limit_19; _loopIdx_19++) {
        vars['i'] = _loopIdx_19;
        vars['xs'] = (vars['xs'] || 0) + (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "225", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) - (22);
        vars['ys'] = (vars['ys'] || 0) + (22);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "225", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        vars['xs'] = (vars['xs'] || 0) - (71);
        vars['ys'] = (vars['ys'] || 0) + (71);
      }
      vars['i'] = _prev_i_19;
      state.waitTimer = Math.max(0.0167, 0.7);
      yield;
    }
    vars['i'] = _prev_i_17;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    let _prev_i_20 = vars['i'];
    for (let _loopIdx_20 = 0, _limit_20 = Math.round(20); _loopIdx_20 < _limit_20; _loopIdx_20++) {
      vars['i'] = _loopIdx_20;
      vars['xs'] = 1418;
      vars['ys'] = 550;
      let _prev_i_21 = vars['i'];
      for (let _loopIdx_21 = 0, _limit_21 = Math.round(25); _loopIdx_21 < _limit_21; _loopIdx_21++) {
        vars['i'] = _loopIdx_21;
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "135", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (22);
        vars['ys'] = (vars['ys'] || 0) + (22);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "120", angle: "135", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        vars['xs'] = (vars['xs'] || 0) - (71);
        vars['ys'] = (vars['ys'] || 0) - (71);
      }
      vars['i'] = _prev_i_21;
      vars['xs'] = -732;
      vars['ys'] = 357;
      let _prev_i_22 = vars['i'];
      for (let _loopIdx_22 = 0, _limit_22 = Math.round(25); _loopIdx_22 < _limit_22; _loopIdx_22++) {
        vars['i'] = _loopIdx_22;
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "315", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) + (22);
        vars['ys'] = (vars['ys'] || 0) + (22);
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "120", angle: "315", offsetX: "xs", offsetY: "ys", radius: "23", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xs'] = (vars['xs'] || 0) - (11);
        vars['ys'] = (vars['ys'] || 0) - (11);
        vars['xs'] = (vars['xs'] || 0) + (71);
        vars['ys'] = (vars['ys'] || 0) + (71);
      }
      vars['i'] = _prev_i_22;
      state.waitTimer = Math.max(0.0167, 0.7);
      yield;
    }
    vars['i'] = _prev_i_20;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['xdao'] = (vars.tx !== undefined ? vars.tx : 0);
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "1", angle: "angle", count: "1", spread: "20", offsetX: "xdao", offsetY: "0", radius: "30", bulletImage: "knife", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_9_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['8stcioibk']) {
        state.onceMap['8stcioibk'] = true;
        vars['x'] = (vars.tx !== undefined ? vars.tx : 0);
        vars['angle'] = 90;
      }
      vars['speed'] = (vars['speed'] || 0) + (1);
    }
    if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (50) && ((vars.speed !== undefined ? vars.speed : 0)) <= (10000)))) {
      vars['speed'] = (vars.speed !== undefined ? vars.speed : 0) / 1.0005;
    }
    if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (200) && ((vars.speed !== undefined ? vars.speed : 0)) <= (202)))) {
      vars['angle'] = (vars['angle'] || 0) + (random(0,0));
    }
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['nltkfth30']) {
      state.onceMap['nltkfth30'] = true;
      vars['speed'] = (vars['speed'] || 0) + (40);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_9_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_10'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (15)))) {
      vars['xs'] = random(-600,600);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "90", offsetX: "0", offsetY: "-200", radius: "10", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (30)))) {
      vars['xs'] = random(-600,600);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "90", offsetX: "0", offsetY: "-200", radius: "10", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.06);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (30) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      vars['xs'] = random(-600,600);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "90", offsetX: "0", offsetY: "-200", radius: "10", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.04);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      vars['xs'] = random(-600,600);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "90", offsetX: "0", offsetY: "-200", radius: "30", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "35", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.4);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (35) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      vars['xs'] = random(-600,600);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "90", offsetX: "0", offsetY: "-200", radius: "30", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "35", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.4);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (15)))) {
      let _prev_i_23 = vars['i'];
      for (let _loopIdx_23 = 0, _limit_23 = Math.round(8); _loopIdx_23 < _limit_23; _loopIdx_23++) {
        vars['i'] = _loopIdx_23;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (10);
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_23;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (30)))) {
      let _prev_i_24 = vars['i'];
      for (let _loopIdx_24 = 0, _limit_24 = Math.round(16); _loopIdx_24 < _limit_24; _loopIdx_24++) {
        vars['i'] = _loopIdx_24;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (10);
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_24;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (30) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (45)))) {
      let _prev_i_25 = vars['i'];
      for (let _loopIdx_25 = 0, _limit_25 = Math.round(24); _loopIdx_25 < _limit_25; _loopIdx_25++) {
        vars['i'] = _loopIdx_25;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (10);
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_25;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (45) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      let _prev_i_26 = vars['i'];
      for (let _loopIdx_26 = 0, _limit_26 = Math.round(32); _loopIdx_26 < _limit_26; _loopIdx_26++) {
        vars['i'] = _loopIdx_26;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (10);
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_26;
    }
    state.waitTimer = Math.max(0.0167, 0.8);
    yield;
    vars['angle'] = (vars['angle'] || 0) + (20);
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (45) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ffdd33", speed: "400", angle: "angle", count: "3", spread: "30", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "onmyoutama", coordMode: "relative", hitRadius: "35", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_10_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1.4)))) {
        vars['speed'] = (vars['speed'] || 0) + (-7);
      }
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1.4) && ((vars.timer !== undefined ? vars.timer : 0)) <= (2)))) {
        vars['speed'] = 300;
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['8piclepmh']) {
          state.onceMap['8piclepmh'] = true;
          if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
            yield;
          }
          if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (30) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
            vars['angle'] = (vars['angle'] || 0) + (random(-3,3));
          }
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      vars['speed'] = 150;
      vars['m'] = (vars['m'] || 0) + (5);
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['g706z70f9']) {
        state.onceMap['g706z70f9'] = true;
        vars['y'] = 0;
        vars['xs'] = random(-600,600);
        vars['x'] = (vars['x'] || 0) + ((vars.xs !== undefined ? vars.xs : 0));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa33").trim().toLowerCase())) {
      vars['speed'] = 100;
      vars['m'] = (vars['m'] || 0) + (5);
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['j2hn7wel2']) {
        state.onceMap['j2hn7wel2'] = true;
        vars['y'] = 0;
        vars['xs'] = random(-600,600);
        vars['x'] = (vars['x'] || 0) + ((vars.xs !== undefined ? vars.xs : 0));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
      vars['m'] = (vars['m'] || 0) + (5);
    }
    if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,15))) {
      vars['y'] = -8000;
    }
    if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,30))) {
      vars['y'] = -8000;
    }
    if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,45))) {
      vars['y'] = -8000;
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0) + (vars.m !== undefined ? vars.m : 0);
    if (!!((vars.dist !== undefined ? vars.dist : 0) < 50)) {
      vars['y'] = (vars.y !== undefined ? vars.y : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_10_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_11'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = 0;
    let _prev_i_27 = vars['i'];
    for (let _loopIdx_27 = 0, _limit_27 = Math.round(3); _loopIdx_27 < _limit_27; _loopIdx_27++) {
      vars['i'] = _loopIdx_27;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "1000", angle: "angle + 45", count: "20", spread: "18", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "ootama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.08);
      yield;
    }
    vars['i'] = _prev_i_27;
    vars['angle'] = 9;
    let _prev_i_28 = vars['i'];
    for (let _loopIdx_28 = 0, _limit_28 = Math.round(3); _loopIdx_28 < _limit_28; _loopIdx_28++) {
      vars['i'] = _loopIdx_28;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "1000", angle: "angle + 45", count: "20", spread: "18", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "ootama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.08);
      yield;
    }
    vars['i'] = _prev_i_28;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_11_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['zuez1n7g6']) {
      state.onceMap['zuez1n7g6'] = true;
      vars['angle'] = (vars['angle'] || 0) + (random(-2,2));
      vars['speed'] = (vars['speed'] || 0) + (random(0,0));
    }
    if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (400) && ((vars.speed !== undefined ? vars.speed : 0)) <= (100000)))) {
      vars['speed'] = (vars['speed'] || 0) + (-5);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_11_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_12'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_29 = vars['i'];
    for (let _loopIdx_29 = 0, _limit_29 = Math.round(10); _loopIdx_29 < _limit_29; _loopIdx_29++) {
      vars['i'] = _loopIdx_29;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "35000", angle: "angle", count: "10", spread: "36", offsetX: "384", offsetY: "448", radius: "10", bulletImage: "marutama", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
      vars['angle'] = (vars['angle'] || 0) + (2);
    }
    vars['i'] = _prev_i_29;
    let _prev_i_30 = vars['i'];
    for (let _loopIdx_30 = 0, _limit_30 = Math.round(10); _loopIdx_30 < _limit_30; _loopIdx_30++) {
      vars['i'] = _loopIdx_30;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "35000", angle: "angle", count: "10", spread: "36", offsetX: "384", offsetY: "448", radius: "10", bulletImage: "marutama", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
      vars['angle'] = (vars['angle'] || 0) - (9);
    }
    vars['i'] = _prev_i_30;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_12_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (30) && ((vars.frame !== undefined ? vars.frame : 0)) <= (30000)))) {
      if (!!((((vars.x !== undefined ? vars.x : 0)) >= (374) && ((vars.x !== undefined ? vars.x : 0)) <= (394)))) {
        if (!!((((vars.y !== undefined ? vars.y : 0)) >= (438) && ((vars.y !== undefined ? vars.y : 0)) <= (458)))) {
          vars['color'] = "#3333ff";
        }
      }
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (2) && ((vars.frame !== undefined ? vars.frame : 0)) <= (3)))) {
      vars['angle'] = (vars['angle'] || 0) + (180);
      vars['speed'] = 200;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_12_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_13'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_31 = vars['i'];
  for (let _loopIdx_31 = 0, _limit_31 = Math.round(1200); _loopIdx_31 < _limit_31; _loopIdx_31++) {
    vars['i'] = _loopIdx_31;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_32 = vars['i'];
    for (let _loopIdx_32 = 0, _limit_32 = Math.round(12); _loopIdx_32 < _limit_32; _loopIdx_32++) {
      vars['i'] = _loopIdx_32;
      vars['spawnp'] = (vars.spawnp !== undefined ? vars.spawnp : 0) + 1;
      let _prev_i_33 = vars['i'];
      for (let _loopIdx_33 = 0, _limit_33 = Math.round(4); _loopIdx_33 < _limit_33; _loopIdx_33++) {
        vars['i'] = _loopIdx_33;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (20);
      }
      vars['i'] = _prev_i_33;
      vars['spd'] = 0;
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_32;
    let _prev_i_34 = vars['i'];
    for (let _loopIdx_34 = 0, _limit_34 = Math.round(12); _loopIdx_34 < _limit_34; _loopIdx_34++) {
      vars['i'] = _loopIdx_34;
      vars['spawnp'] = (vars.spawnp !== undefined ? vars.spawnp : 0) - 1;
      let _prev_i_35 = vars['i'];
      for (let _loopIdx_35 = 0, _limit_35 = Math.round(4); _loopIdx_35 < _limit_35; _loopIdx_35++) {
        vars['i'] = _loopIdx_35;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (20);
      }
      vars['i'] = _prev_i_35;
      vars['spd'] = 0;
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_34;
    vars['spawnp'] = -10;
    state.waitTimer = Math.max(0.0167, 0.15);
    yield;
  }
  vars['i'] = _prev_i_31;
};
window.compiledDanmaku['danmaku_13_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['4gklskd7g']) {
        state.onceMap['4gklskd7g'] = true;
        vars['x'] = 379 + (vars.spawnp !== undefined ? vars.spawnp : 0) * 60;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['8rmsqsqi7']) {
        state.onceMap['8rmsqsqi7'] = true;
        vars['x'] = 379 - (vars.spawnp !== undefined ? vars.spawnp : 0) * 60;
      }
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 890)) {
      vars['angle'] = -90;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
      vars['y'] = -50000;
    }
    if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_13_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_14'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "tx", mode: "vecstep", duration: "1", stepVal: "0.1", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "ty", mode: "vecstep", duration: "1", stepVal: "0.1", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1 - (vars.t !== undefined ? vars.t : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 1 - (vars.t !== undefined ? vars.t : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "72", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "72", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "72", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    vars['t'] = (vars['t'] || 0) + (0.05);
    vars['t2'] = (vars['t2'] || 0) + (0.0175);
  }
};
window.compiledDanmaku['danmaku_14_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = (vars['speed'] || 0) + (1);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_14_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_15'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_36 = vars['i'];
  for (let _loopIdx_36 = 0, _limit_36 = Math.round(1200000000); _loopIdx_36 < _limit_36; _loopIdx_36++) {
    vars['i'] = _loopIdx_36;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "200", angle: "-90 + angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = random(-50,50);
    state.waitTimer = Math.max(0.0167, 0.02);
    yield;
  }
  vars['i'] = _prev_i_36;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['r'] = random(-100,100);
    vars['r2'] = random(-100,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "384 + r", mode: "seconds", duration: "0.4", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "370 + r2", mode: "seconds", duration: "0.4", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 5);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_15_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['27s698is5']) {
      state.onceMap['27s698is5'] = true;
      vars['yp'] = -40;
    }
    vars['y'] = (vars['y'] || 0) + ((vars.yp !== undefined ? vars.yp : 0) / 10);
    vars['yp'] = (vars['yp'] || 0) + (0.4);
    if (!!((((vars.yp !== undefined ? vars.yp : 0)) >= (-5) && ((vars.yp !== undefined ? vars.yp : 0)) <= (5)))) {
      vars['SpriteAngle'] = 90;
    }
    state.waitTimer = Math.max(0.0167, 0.00001);
    yield;
    if (!!((((vars.yp !== undefined ? vars.yp : 0)) >= (-5) && ((vars.yp !== undefined ? vars.yp : 0)) <= (5)))) {
      vars['SpriteAngle'] = 90;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_15_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_16'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_37 = vars['i'];
  for (let _loopIdx_37 = 0, _limit_37 = Math.round(1200); _loopIdx_37 < _limit_37; _loopIdx_37++) {
    vars['i'] = _loopIdx_37;
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3332", speed: "300", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (random(-5,5));
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
  }
  vars['i'] = _prev_i_37;
};
window.compiledDanmaku['danmaku_16_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) + (0.2 - (vars.m !== undefined ? vars.m : 0));
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) - (0.2 - (vars.m !== undefined ? vars.m : 0));
    }
    vars['m'] = (vars['m'] || 0) + (0.0003);
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_16_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_17'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['cx'] = "384";
  vars['cy'] = "350";
  vars['R'] = "200";
  vars['bx'] = "0";
  vars['by'] = "0";
  vars['bx2'] = "0";
  vars['by2'] = "0";
  vars['angle'] = "0";
  vars['angle2'] = "0";
  while (true) {
    vars['base_angle'] = random(0, 360);
    vars['x0'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(-90 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['y0'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(-90 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['x1'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(-18 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['y1'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(-18 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['x2'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(54 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['y2'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(54 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['x3'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(126 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['y3'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(126 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['x4'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(198 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['y4'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(198 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['rx0'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(90 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['ry0'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(90 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['rx1'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(162 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['ry1'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(162 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['rx2'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(234 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['ry2'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(234 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['rx3'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(306 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['ry3'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(306 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['rx4'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.R !== undefined ? vars.R : 0) * cos(18 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['ry4'] = (vars.cy !== undefined ? vars.cy : 0) + (vars.R !== undefined ? vars.R : 0) * sin(18 + (vars.base_angle !== undefined ? vars.base_angle : 0));
    vars['bx'] = (vars.x0 !== undefined ? vars.x0 : 0);
    vars['by'] = (vars.y0 !== undefined ? vars.y0 : 0);
    vars['bx2'] = (vars.rx0 !== undefined ? vars.rx0 : 0);
    vars['by2'] = (vars.ry0 !== undefined ? vars.ry0 : 0);
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['star'] = 1;
    vars['t'] = 0;
    vars['angle'] = 72 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = -108 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x0", to: "x2", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y0", to: "y2", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx0", to: "rx2", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry0", to: "ry2", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['angle'] = -144 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = 36 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x2", to: "x4", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y2", to: "y4", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx2", to: "rx4", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry2", to: "ry4", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['angle'] = 0 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = 180 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x4", to: "x1", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y4", to: "y1", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx4", to: "rx1", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry4", to: "ry1", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['angle'] = 144 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = -36 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x1", to: "x3", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y1", to: "y3", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx1", to: "rx3", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry1", to: "ry3", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['angle'] = -72 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = 108 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x3", to: "x0", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y3", to: "y0", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx3", to: "rx0", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry3", to: "ry0", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    vars['star'] = 0;
    vars['angle'] = 90 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    vars['angle2'] = -90 + (vars.base_angle !== undefined ? vars.base_angle : 0);
    if (_util.executeBlock({ type: 'tween_var', name: "bx", from: "x0", to: "cx", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by", from: "y0", to: "cy", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "bx2", from: "rx0", to: "cx", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "by2", from: "ry0", to: "cy", mode: "seconds", duration: "0.3", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    vars['t'] = 1;
    state.waitTimer = Math.max(0.0167, 0.3);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['cx'] = "384";
  vars['cy'] = "350";
  vars['R'] = "200";
  vars['bx'] = "0";
  vars['by'] = "0";
  vars['bx2'] = "0";
  vars['by2'] = "0";
  vars['angle'] = "0";
  vars['angle2'] = "0";
  while (true) {
    if (!!(_util.fuzzyEqual((vars.star !== undefined ? vars.star : 0) ,1))) {
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3366", speed: "0", angle: "angle", offsetX: "bx", offsetY: "by", radius: "20", bulletImage: "b_star", coordMode: "absolute", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3366", speed: "0", angle: "angle2", offsetX: "bx2", offsetY: "by2", radius: "20", bulletImage: "b_star", coordMode: "absolute", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 0.00005);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_17_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.e_t !== undefined ? vars.e_t : 0),1))) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['74ykt4klb']) {
        state.onceMap['74ykt4klb'] = true;
        vars['flag'] = 1;
      }
    }
    if (!!(_util.fuzzyEqual((vars.flag !== undefined ? vars.flag : 0),1))) {
      if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (0) && ((vars.speed !== undefined ? vars.speed : 0)) <= (400)))) {
        vars['speed'] = (vars['speed'] || 0) + (2);
      }
      if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['loynhhmkj']) {
          state.onceMap['loynhhmkj'] = true;
          vars['speed'] = 0;
          vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
          vars['angle'] = (vars.angle !== undefined ? vars.angle : 0) - 180;
        }
      }
      if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['ngzorbrod']) {
          state.onceMap['ngzorbrod'] = true;
          vars['speed'] = 0;
          vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
          vars['angle'] = (vars.angle !== undefined ? vars.angle : 0) - 180;
        }
      }
      if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['6in4sqwwm']) {
          state.onceMap['6in4sqwwm'] = true;
          vars['speed'] = 0;
          vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        }
      }
      if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['tkbx17m16']) {
          state.onceMap['tkbx17m16'] = true;
          vars['speed'] = 0;
          vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        }
      }
    }
    vars['spriteAngle'] = (vars.spriteAngle !== undefined ? vars.spriteAngle : 0) + 3;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_17_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_18'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angleg", count: "8", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "ootama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angleg'] = (vars['angleg'] || 0) + (16);
    state.waitTimer = Math.max(0.0167, 0.6);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['offset'] = 0;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#dddddd", speed: "200", angle: "angle", offsetX: "300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ddddde", speed: "200", angle: "angle", offsetX: "-300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['offset'] = 30;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#dddddd", speed: "200", angle: "angle", offsetX: "300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ddddde", speed: "200", angle: "angle", offsetX: "-300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['offset'] = -30;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#dddddd", speed: "200", angle: "angle", offsetX: "300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ddddde", speed: "200", angle: "angle", offsetX: "-300", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_18_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    while (true) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        vars['radius'] = (vars['radius'] || 0) + (0.8);
        vars['hitRadius'] = (vars['hitRadius'] || 0) + (0.7);
        vars['speed'] = (vars['speed'] || 0) + (2);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#dddddd").trim().toLowerCase())) {
        vars['radius'] = (vars['radius'] || 0) + (1);
        vars['hitRadius'] = (vars['hitRadius'] || 0) + (0.2);
        vars['speed'] = (vars['speed'] || 0) + (2);
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['9v5j30e86']) {
          state.onceMap['9v5j30e86'] = true;
          if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['angle'] = (vars['angle'] || 0) + ((vars.offset !== undefined ? vars.offset : 0));
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ddddde").trim().toLowerCase())) {
        vars['radius'] = (vars['radius'] || 0) + (1);
        vars['hitRadius'] = (vars['hitRadius'] || 0) + (0.2);
        vars['speed'] = (vars['speed'] || 0) + (2);
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['one5z33v3']) {
          state.onceMap['one5z33v3'] = true;
          if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['angle'] = (vars['angle'] || 0) + ((vars.offset !== undefined ? vars.offset : 0));
        }
      }
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_18_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_19'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = random(0,360);
    let _prev_i_38 = vars['i'];
    for (let _loopIdx_38 = 0, _limit_38 = Math.round(9); _loopIdx_38 < _limit_38; _loopIdx_38++) {
      vars['i'] = _loopIdx_38;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_38;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = random(0,360);
    let _prev_i_39 = vars['i'];
    for (let _loopIdx_39 = 0, _limit_39 = Math.round(9); _loopIdx_39 < _limit_39; _loopIdx_39++) {
      vars['i'] = _loopIdx_39;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_39;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = random(0,360);
    let _prev_i_40 = vars['i'];
    for (let _loopIdx_40 = 0, _limit_40 = Math.round(9); _loopIdx_40 < _limit_40; _loopIdx_40++) {
      vars['i'] = _loopIdx_40;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_40;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = random(0,360);
    let _prev_i_41 = vars['i'];
    for (let _loopIdx_41 = 0, _limit_41 = Math.round(9); _loopIdx_41 < _limit_41; _loopIdx_41++) {
      vars['i'] = _loopIdx_41;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_41;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = random(0,360);
    let _prev_i_42 = vars['i'];
    for (let _loopIdx_42 = 0, _limit_42 = Math.round(9); _loopIdx_42 < _limit_42; _loopIdx_42++) {
      vars['i'] = _loopIdx_42;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_42;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = random(0,360);
    let _prev_i_43 = vars['i'];
    for (let _loopIdx_43 = 0, _limit_43 = Math.round(9); _loopIdx_43 < _limit_43; _loopIdx_43++) {
      vars['i'] = _loopIdx_43;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "300", angle: "angle", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (40);
    }
    vars['i'] = _prev_i_43;
    state.waitTimer = Math.max(0.0167, 50);
    yield;
  }
};
window.compiledDanmaku['danmaku_19_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.timer !== undefined ? vars.timer : 0) < 3)) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + (0.1);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3333ff").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + (-0.1);
      }
    }
    vars['g'] = (vars['g'] || 0) + (1);
    if (!!(_util.fuzzyEqual((vars.g !== undefined ? vars.g : 0) ,40))) {
      vars['g'] = 0;
    }
    if (!!(_util.fuzzyEqual((vars.g !== undefined ? vars.g : 0) ,10))) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "250", angle: "0", count: "5", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3333ff").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3333ff", speed: "250", angle: "0", count: "5", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "uroko", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_19_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (3) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4)))) {
      if (_util.executeBlock({ type: 'homing', turnSpeed: "90", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (random(-1,1));
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,4))) {
      vars['angle'] = (vars['angle'] || 0) + (random(-5,5));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (3) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5)))) {
      vars['speed'] = (vars['speed'] || 0) + (random(0.5,1.5));
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,0.1))) {
      vars['speed'] = 1;
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_20'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "400", angle: "-15", offsetX: "0", offsetY: "0", radius: "50", bulletImage: "ootama", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "400", angle: "195", offsetX: "0", offsetY: "0", radius: "50", bulletImage: "ootama", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "400", angle: "-15", offsetX: "0", offsetY: "0", radius: "50", bulletImage: "ootama", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "400", angle: "195", offsetX: "0", offsetY: "0", radius: "50", bulletImage: "ootama", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 7);
    yield;
  }
};
window.compiledDanmaku['danmaku_20_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    while (true) {
      if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['m'] = (vars['m'] || 0) + (1);
      if (!!((((vars.m !== undefined ? vars.m : 0)) >= (12) && ((vars.m !== undefined ? vars.m : 0)) <= (20)))) {
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "spd", angle: "90 + r", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = random(150,250);
        vars['r'] = random(-5,5);
        vars['m'] = random(0,6);
      }
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_20_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_21'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ffffff", speed: "500", angle: "spangle", count: "16", offsetX: "0", offsetY: "0", radius: "0", bulletImage: "ohuda", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4 - (vars.w !== undefined ? vars.w : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    vars['w'] = (vars['w'] || 0) + (0.6);
    vars['w2'] = (vars['w2'] || 0) + (0.1);
    vars['w3'] = (vars['w3'] || 0) + (10);
    vars['spangle'] = (vars['spangle'] || 0) + (random(0,360));
  }
};
window.compiledDanmaku['danmaku_21_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.timer !== undefined ? vars.timer : 0) > 0.2)) {
      state.waitTimer = Math.max(0.0167, 0.04);
      yield;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle", count: "1", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "kome", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + ((vars.kaku !== undefined ? vars.kaku : 0));
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    vars['speed'] = 200;
    if (!!(_util.fuzzyNotEqual((vars.flag !== undefined ? vars.flag : 0) ,1))) {
      vars['kaku'] = (vars['kaku'] || 0) + (2);
    }
    if (!!((((vars.kaku !== undefined ? vars.kaku : 0)) >= (11) && ((vars.kaku !== undefined ? vars.kaku : 0)) <= (500)))) {
      vars['flag'] = 1;
    }
    if (!!(_util.fuzzyEqual((vars.flag !== undefined ? vars.flag : 0) ,1))) {
      vars['kaku'] = (vars['kaku'] || 0) - (2);
    }
    if (!!((((vars.kaku !== undefined ? vars.kaku : 0)) >= (-500) && ((vars.kaku !== undefined ? vars.kaku : 0)) <= (-11)))) {
      vars['flag'] = 0;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_21_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,1))) {
      vars['speed'] = 50 + random(-10,50) + (vars.w3 !== undefined ? vars.w3 : 0);
      vars['angle'] = (vars['angle'] || 0) + (random(-180,180));
      vars['color'] = "#ff3333";
      vars['hitRadius'] = 6;
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,3 - (vars.w2 !== undefined ? vars.w2 : 0)))) {
      vars['y'] = -546546456;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_22'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_44 = vars['i'];
    for (let _loopIdx_44 = 0, _limit_44 = Math.round(15); _loopIdx_44 < _limit_44; _loopIdx_44++) {
      vars['i'] = _loopIdx_44;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "400", angle: "90", offsetX: "spx", offsetY: "0", radius: "20", bulletImage: "b_uroko", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spx'] = (vars['spx'] || 0) + (60);
      state.waitTimer = Math.max(0.0167, 0.2);
      yield;
    }
    vars['i'] = _prev_i_44;
    let _prev_i_45 = vars['i'];
    for (let _loopIdx_45 = 0, _limit_45 = Math.round(15); _loopIdx_45 < _limit_45; _loopIdx_45++) {
      vars['i'] = _loopIdx_45;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "400", angle: "90", offsetX: "spx", offsetY: "0", radius: "20", bulletImage: "b_uroko", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spx'] = (vars['spx'] || 0) - (60);
      state.waitTimer = Math.max(0.0167, 0.2);
      yield;
    }
    vars['i'] = _prev_i_45;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_46 = vars['i'];
    for (let _loopIdx_46 = 0, _limit_46 = Math.round(5); _loopIdx_46 < _limit_46; _loopIdx_46++) {
      vars['i'] = _loopIdx_46;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", count: "1 + wa", spread: "5", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "b_poihuru", coordMode: "relative", hitRadius: "7", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.3);
      yield;
    }
    vars['i'] = _prev_i_46;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    vars['wa'] = (vars['wa'] || 0) + (1);
  }
}
];
window.compiledDanmaku['danmaku_22_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['period'] = 60;
      vars['amp'] = 30;
      vars['baseAngle'] = 90;
      vars['frame2'] = 0;
      vars['y'] = random(-200,0);
      vars['x'] = (vars['x'] || 0) + (random(-30,30));
      while (true) {
        vars['frame2'] = (vars['frame2'] || 0) + (1);
        vars['l'] = (vars['l'] || 0) + (1);
        vars['angle'] = (vars.baseAngle !== undefined ? vars.baseAngle : 0) - (vars.amp !== undefined ? vars.amp : 0) * sin((vars.frame2 !== undefined ? vars.frame2 : 0) * 360 / (vars.period !== undefined ? vars.period : 0));
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
        if (!!(_util.fuzzyEqual((vars.l !== undefined ? vars.l : 0) ,3))) {
          if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle + 180", count: "1", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "12", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['l'] = 0;
        }
        state.waitTimer = Math.max(0.0167, 0.0167);
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_22_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (60) && ((vars.frame !== undefined ? vars.frame : 0)) <= (500)))) {
      vars['y'] = -8000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_23'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    let _prev_i_47 = vars['i'];
    for (let _loopIdx_47 = 0, _limit_47 = Math.round(1000000000000); _loopIdx_47 < _limit_47; _loopIdx_47++) {
      vars['i'] = _loopIdx_47;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffff66", speed: "35000", angle: "angle", count: "6", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_star", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffff66", speed: "35000", angle: "-angle", count: "6", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_star", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (6);
      state.waitTimer = Math.max(0.0167, 0.5);
      yield;
    }
    vars['i'] = _prev_i_47;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
  }
}
];
window.compiledDanmaku['danmaku_23_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['tp9i0om5n']) {
      state.onceMap['tp9i0om5n'] = true;
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
      vars['speed'] = 200;
      vars['angle'] = (vars['angle'] || 0) + (180);
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0) + (vars.m !== undefined ? vars.m : 0);
    vars['m'] = (vars['m'] || 0) + (5);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_23_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_24'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_48 = vars['i'];
    for (let _loopIdx_48 = 0, _limit_48 = Math.round(100); _loopIdx_48 < _limit_48; _loopIdx_48++) {
      vars['i'] = _loopIdx_48;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "600", angle: "angle", count: "4", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (random(0,360));
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_48;
    let _prev_i_49 = vars['i'];
    for (let _loopIdx_49 = 0, _limit_49 = Math.round(50); _loopIdx_49 < _limit_49; _loopIdx_49++) {
      vars['i'] = _loopIdx_49;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "700", angle: "angle", count: "2", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (12.2);
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_49;
    let _prev_i_50 = vars['i'];
    for (let _loopIdx_50 = 0, _limit_50 = Math.round(50); _loopIdx_50 < _limit_50; _loopIdx_50++) {
      vars['i'] = _loopIdx_50;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (random(-7,7));
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "angle", count: "5", spread: "25", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.0167);
      yield;
    }
    vars['i'] = _prev_i_50;
    let _prev_i_51 = vars['i'];
    for (let _loopIdx_51 = 0, _limit_51 = Math.round(50); _loopIdx_51 < _limit_51; _loopIdx_51++) {
      vars['i'] = _loopIdx_51;
      vars['angle'] = (vars['angle'] || 0) + (25);
      vars['w'] = 0;
      vars['sp'] = 0;
      let _prev_i_52 = vars['i'];
      for (let _loopIdx_52 = 0, _limit_52 = Math.round(7); _loopIdx_52 < _limit_52; _loopIdx_52++) {
        vars['i'] = _loopIdx_52;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff88", speed: "300 + sp", angle: "angle", count: "1 + w", spread: "2", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_poihuru", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['w'] = (vars['w'] || 0) + (1);
        vars['sp'] = (vars['sp'] || 0) + (30);
      }
      vars['i'] = _prev_i_52;
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
    }
    vars['i'] = _prev_i_51;
    let _prev_i_53 = vars['i'];
    for (let _loopIdx_53 = 0, _limit_53 = Math.round(20); _loopIdx_53 < _limit_53; _loopIdx_53++) {
      vars['i'] = _loopIdx_53;
      vars['angle'] = random(-80,80);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3388ff", speed: "700", angle: "-90 + angle", count: "3", spread: "10", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_star", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_53;
    let _prev_i_54 = vars['i'];
    for (let _loopIdx_54 = 0, _limit_54 = Math.round(50); _loopIdx_54 < _limit_54; _loopIdx_54++) {
      vars['i'] = _loopIdx_54;
      vars['xsp'] = random(0,768);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#9E76B4", speed: "700", angle: "90", count: "1", spread: "10", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "poihuru", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_54;
    let _prev_i_55 = vars['i'];
    for (let _loopIdx_55 = 0, _limit_55 = Math.round(500000); _loopIdx_55 < _limit_55; _loopIdx_55++) {
      vars['i'] = _loopIdx_55;
      let _prev_i_56 = vars['i'];
      for (let _loopIdx_56 = 0, _limit_56 = Math.round(10); _loopIdx_56 < _limit_56; _loopIdx_56++) {
        vars['i'] = _loopIdx_56;
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "600", angle: "angle", count: "4", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (random(0,360));
        state.waitTimer = Math.max(0.0167, 0.01);
        yield;
      }
      vars['i'] = _prev_i_56;
      let _prev_i_57 = vars['i'];
      for (let _loopIdx_57 = 0, _limit_57 = Math.round(10); _loopIdx_57 < _limit_57; _loopIdx_57++) {
        vars['i'] = _loopIdx_57;
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "700", angle: "angle", count: "2", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (12.2);
        state.waitTimer = Math.max(0.0167, 0.02);
        yield;
      }
      vars['i'] = _prev_i_57;
      let _prev_i_58 = vars['i'];
      for (let _loopIdx_58 = 0, _limit_58 = Math.round(10); _loopIdx_58 < _limit_58; _loopIdx_58++) {
        vars['i'] = _loopIdx_58;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (random(-7,7));
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "angle", count: "5", spread: "25", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.02);
        yield;
      }
      vars['i'] = _prev_i_58;
      let _prev_i_59 = vars['i'];
      for (let _loopIdx_59 = 0, _limit_59 = Math.round(10); _loopIdx_59 < _limit_59; _loopIdx_59++) {
        vars['i'] = _loopIdx_59;
        vars['angle'] = (vars['angle'] || 0) + (25);
        vars['w'] = 0;
        vars['sp'] = 0;
        let _prev_i_60 = vars['i'];
        for (let _loopIdx_60 = 0, _limit_60 = Math.round(7); _loopIdx_60 < _limit_60; _loopIdx_60++) {
          vars['i'] = _loopIdx_60;
          if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff88", speed: "300 + sp", angle: "angle", count: "1 + w", spread: "2", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_poihuru", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['w'] = (vars['w'] || 0) + (1);
          vars['sp'] = (vars['sp'] || 0) + (30);
        }
        vars['i'] = _prev_i_60;
        state.waitTimer = Math.max(0.0167, 0.04);
        yield;
      }
      vars['i'] = _prev_i_59;
      let _prev_i_61 = vars['i'];
      for (let _loopIdx_61 = 0, _limit_61 = Math.round(10); _loopIdx_61 < _limit_61; _loopIdx_61++) {
        vars['i'] = _loopIdx_61;
        vars['angle'] = random(-80,80);
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3388ff", speed: "700", angle: "-90 + angle", count: "3", spread: "10", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_star", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.05);
        yield;
      }
      vars['i'] = _prev_i_61;
      let _prev_i_62 = vars['i'];
      for (let _loopIdx_62 = 0, _limit_62 = Math.round(10); _loopIdx_62 < _limit_62; _loopIdx_62++) {
        vars['i'] = _loopIdx_62;
        vars['xsp'] = random(0,768);
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#9E76B4", speed: "700", angle: "90", count: "1", spread: "10", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "poihuru", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.02);
        yield;
      }
      vars['i'] = _prev_i_62;
    }
    vars['i'] = _prev_i_55;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_24_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
      if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
        vars['angle'] = 90;
      }
      vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (5);
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#9E76B4").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['lt9mmmjyg']) {
        state.onceMap['lt9mmmjyg'] = true;
        vars['xsp'] = random(-200,968);
        vars['x'] = (vars.xsp !== undefined ? vars.xsp : 0);
        vars['y'] = 0;
      }
      if (_util.executeBlock({ type: 'homing', turnSpeed: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_24_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_25'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['xp'] = 0;
    let _prev_i_63 = vars['i'];
    for (let _loopIdx_63 = 0, _limit_63 = Math.round(30); _loopIdx_63 < _limit_63; _loopIdx_63++) {
      vars['i'] = _loopIdx_63;
      vars['xp'] = (vars['xp'] || 0) + (60);
      if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "100", angle: "-90", count: "1", spread: "0", offsetX: "xp + ex - 700", offsetY: "ey", radius: "10", bulletImage: "none", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "100", angle: "90", count: "1", spread: "0", offsetX: "xp + ex - 700", offsetY: "ey", radius: "10", bulletImage: "none", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_63;
    state.waitTimer = Math.max(0.0167, 0.4);
    yield;
    vars['yp'] = 0;
    let _prev_i_64 = vars['i'];
    for (let _loopIdx_64 = 0, _limit_64 = Math.round(30); _loopIdx_64 < _limit_64; _loopIdx_64++) {
      vars['i'] = _loopIdx_64;
      vars['yp'] = (vars['yp'] || 0) + (60);
      if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "100", angle: "0", count: "1", spread: "0", offsetX: "ex", offsetY: "ey + yp - 700", radius: "10", bulletImage: "none", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "100", angle: "180", count: "1", spread: "0", offsetX: "ex", offsetY: "ey + yp - 700", radius: "10", bulletImage: "none", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_64;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    vars['mx'] = random(200,568);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "mx", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['my'] = random(100,443);
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "my", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.2);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!state.onceMap) state.onceMap = {};
  if (!state.onceMap['i6j6b0xr6']) {
    state.onceMap['i6j6b0xr6'] = true;
    vars['w'] = 0.6;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "fff", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['fff'] = (vars['fff'] || 0) + (5);
    state.waitTimer = Math.max(0.0167, 0.4);
    yield;
    state.waitTimer = Math.max(0.0167, (vars.w !== undefined ? vars.w : 0));
    yield;
    vars['w'] = (vars['w'] || 0) - (0.005);
  }
}
];
window.compiledDanmaku['danmaku_25_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      vars['speed'] = 0;
      vars['warningTime'] = 1;
      vars['activeTime'] = 2;
      vars['laserWidth'] = 12;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_25_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_26'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "100", angle: "0 + m", count: "3", spread: "60", offsetX: "0", offsetY: "ty", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3388ff", speed: "100", angle: "180 + m", count: "3", spread: "60", offsetX: "768", offsetY: "ty", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ffdd33", speed: "100", angle: "90 + m", count: "3", spread: "60", offsetX: "tx", offsetY: "0", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff88", speed: "100", angle: "-90 + m", count: "3", spread: "60", offsetX: "tx", offsetY: "896", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.07);
    yield;
  }
};
window.compiledDanmaku['danmaku_26_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};
window.compiledDanmaku['danmaku_26_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_27'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_65 = vars['i'];
    for (let _loopIdx_65 = 0, _limit_65 = Math.round(25); _loopIdx_65 < _limit_65; _loopIdx_65++) {
      vars['i'] = _loopIdx_65;
      vars['angle'] = (vars['angle'] || 0) + (random(-20,20));
      if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.04);
      yield;
    }
    vars['i'] = _prev_i_65;
    state.waitTimer = Math.max(0.0167, 6);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
  }
}
];
window.compiledDanmaku['danmaku_27_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
      if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
        let _prev_i_66 = vars['i'];
        for (let _loopIdx_66 = 0, _limit_66 = Math.round(2); _loopIdx_66 < _limit_66; _loopIdx_66++) {
          vars['i'] = _loopIdx_66;
          vars['spd'] = random(100,400);
          vars['ang'] = random(0,360);
          vars['wa'] = random(3,16);
          vars['sz'] = random(5,20);
          if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "spd", angle: "ang", count: "wa", offsetX: "0", offsetY: "0", radius: "sz", bulletImage: "light", coordMode: "relative", hitRadius: "sz", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_66;
        vars['speed'] = 0;
        vars['auraIntensity'] = 2.5;
        let _prev_i_67 = vars['i'];
        for (let _loopIdx_67 = 0, _limit_67 = Math.round(20); _loopIdx_67 < _limit_67; _loopIdx_67++) {
          vars['i'] = _loopIdx_67;
          vars['auraRange'] = (vars['auraRange'] || 0) + (1);
          vars['auraIntensity'] = (vars['auraIntensity'] || 0) - (0.1);
          vars['hitRadius'] = (vars['hitRadius'] || 0) + (12);
          state.waitTimer = Math.max(0.0167, 0.03);
          yield;
        }
        vars['i'] = _prev_i_67;
        let _prev_i_68 = vars['i'];
        for (let _loopIdx_68 = 0, _limit_68 = Math.round(10); _loopIdx_68 < _limit_68; _loopIdx_68++) {
          vars['i'] = _loopIdx_68;
          vars['auraRange'] = (vars['auraRange'] || 0) + (1);
          vars['auraIntensity'] = (vars['auraIntensity'] || 0) - (0.1);
          vars['hitRadius'] = (vars['hitRadius'] || 0) - (8);
          vars['radius'] = (vars['radius'] || 0) - (2);
          state.waitTimer = Math.max(0.0167, 0.03);
          yield;
        }
        vars['i'] = _prev_i_68;
        vars['y'] = -8000;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) + (5);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_27_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_28'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_69 = vars['i'];
  for (let _loopIdx_69 = 0, _limit_69 = Math.round(12000); _loopIdx_69 < _limit_69; _loopIdx_69++) {
    vars['i'] = _loopIdx_69;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle2", offsetX: "0", offsetY: "0", radius: "25", bulletImage: "light", coordMode: "relative", hitRadius: "25", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle2'] = (vars['angle2'] || 0) + (6);
    state.waitTimer = Math.max(0.0167, 0.02 - (vars.t !== undefined ? vars.t : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 0.01);
    yield;
    vars['t'] = (vars['t'] || 0) + (0.00001);
  }
  vars['i'] = _prev_i_69;
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_70 = vars['i'];
  for (let _loopIdx_70 = 0, _limit_70 = Math.round(12000); _loopIdx_70 < _limit_70; _loopIdx_70++) {
    vars['i'] = _loopIdx_70;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.8);
    yield;
  }
  vars['i'] = _prev_i_70;
}
];
window.compiledDanmaku['danmaku_28_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};
window.compiledDanmaku['danmaku_28_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_29'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "charge", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_71 = vars['i'];
    for (let _loopIdx_71 = 0, _limit_71 = Math.round(150); _loopIdx_71 < _limit_71; _loopIdx_71++) {
      vars['i'] = _loopIdx_71;
      vars['spd'] = random(8500,14000);
      vars['angle2'] = (vars['angle2'] || 0) + (random(0,360));
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#fffffe", speed: "spd", angle: "angle2", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "light", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_71;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "maspa_long", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['sp'] = 1;
    let _prev_i_72 = vars['i'];
    for (let _loopIdx_72 = 0, _limit_72 = Math.round(5000); _loopIdx_72 < _limit_72; _loopIdx_72++) {
      vars['i'] = _loopIdx_72;
      vars['ang'] = (vars.angle !== undefined ? vars.angle : 0);
      vars['ag'] = (vars.angle !== undefined ? vars.angle : 0);
      vars['ang'] = (vars['ang'] || 0) + (random(-40,40));
      if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ffff99", speed: "1500", angle: "ang", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.000005);
      yield;
      vars['angle'] = (vars['angle'] || 0) + (0.001);
    }
    vars['i'] = _prev_i_72;
    vars['sp'] = 0;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (3) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (757575757)))) {
      vars['angle2'] = (vars['angle2'] || 0) + (random(0,360));
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffdd33", speed: "300", angle: "angle2", count: "10", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "b_star", coordMode: "relative", hitRadius: "7", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.2);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_29_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#fffffe").trim().toLowerCase())) {
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (2) && ((vars.frame !== undefined ? vars.frame : 0)) <= (3)))) {
        vars['speed'] = 320;
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['v0zdvpwrc']) {
          state.onceMap['v0zdvpwrc'] = true;
          vars['angle'] = (vars['angle'] || 0) + (180);
        }
      }
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (20) && ((vars.frame !== undefined ? vars.frame : 0)) <= (3000)))) {
        if (!!((((vars.x !== undefined ? vars.x : 0)) >= (374) && ((vars.x !== undefined ? vars.x : 0)) <= (394)))) {
          vars['y'] = -8000;
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffff99").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['hfdkvupht']) {
        state.onceMap['hfdkvupht'] = true;
        if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "ag", mode: "seconds", duration: "0.6", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['auraRange'] = 5.5;
        vars['auraIntensity'] = 0.1;
      }
    }
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (3);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_29_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_30'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_73 = vars['i'];
  for (let _loopIdx_73 = 0, _limit_73 = Math.round(50000); _loopIdx_73 < _limit_73; _loopIdx_73++) {
    vars['i'] = _loopIdx_73;
    vars['ey'] = 350;
    let _prev_i_74 = vars['i'];
    for (let _loopIdx_74 = 0, _limit_74 = Math.round(72); _loopIdx_74 < _limit_74; _loopIdx_74++) {
      vars['i'] = _loopIdx_74;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff33aa", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['henkat'] = random(1.5,2);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3388ff", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['henkat'] = random(0.5,1);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['henkat'] = random(0.5,1);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['henkat'] = random(1.5,2);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (5);
    }
    vars['i'] = _prev_i_74;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
  }
  vars['i'] = _prev_i_73;
};
window.compiledDanmaku['danmaku_30_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,(vars.henkat !== undefined ? vars.henkat : 0)))) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['ujvotosu2']) {
          state.onceMap['ujvotosu2'] = true;
          if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
            yield;
          }
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= ((vars.henkat !== undefined ? vars.henkat : 0)) && ((vars.timer !== undefined ? vars.timer : 0)) <= ((vars.henkat !== undefined ? vars.henkat : 0)+3)))) {
        vars['angle'] = (vars['angle'] || 0) + (1);
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa33").trim().toLowerCase())) {
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= ((vars.henkat !== undefined ? vars.henkat : 0)) && ((vars.timer !== undefined ? vars.timer : 0)) <= ((vars.henkat !== undefined ? vars.henkat : 0)+3)))) {
        vars['angle'] = (vars['angle'] || 0) - (1);
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ff88").trim().toLowerCase())) {
      if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
        if (!state.onceMap) state.onceMap = {};
        if (!state.onceMap['cqlzrb28c']) {
          state.onceMap['cqlzrb28c'] = true;
          if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
            yield;
          }
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff33aa").trim().toLowerCase())) {
      if (!!((vars.dist !== undefined ? vars.dist : 0) < 150)) {
        vars['speed'] = 100;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_30_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_31'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "8", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "sword", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "-angle", count: "8", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "sword", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (5.2);
    state.waitTimer = Math.max(0.0167, 0.05);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ex'] = (vars.tx !== undefined ? vars.tx : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_31_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = (vars['speed'] || 0) + (1);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_31_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_32'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!state.onceMap) state.onceMap = {};
  if (!state.onceMap['fiv138qq8']) {
    state.onceMap['fiv138qq8'] = true;
    vars['t'] = 1.5;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_75 = vars['i'];
  for (let _loopIdx_75 = 0, _limit_75 = Math.round(1200); _loopIdx_75 < _limit_75; _loopIdx_75++) {
    vars['i'] = _loopIdx_75;
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff5100", speed: "200", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = random(0,360);
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    state.waitTimer = Math.max(0.0167, (vars.t !== undefined ? vars.t : 0));
    yield;
    vars['t'] = (vars['t'] || 0) - (0.05);
  }
  vars['i'] = _prev_i_75;
}
];
window.compiledDanmaku['danmaku_32_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#6200ff", speed: "120", angle: "angle", count: "4", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_32_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_33'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_76 = vars['i'];
  for (let _loopIdx_76 = 0, _limit_76 = Math.round(12000); _loopIdx_76 < _limit_76; _loopIdx_76++) {
    vars['i'] = _loopIdx_76;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "300", angle: "ang", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "300", angle: "ang + 180", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "300", angle: "ang + 90", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "300", angle: "ang + 180 + 90", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['ang'] = (vars['ang'] || 0) + (4 + random(1,6));
    state.waitTimer = Math.max(0.0167, 0.06);
    yield;
  }
  vars['i'] = _prev_i_76;
};
window.compiledDanmaku['danmaku_33_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['period'] = 120;
      vars['amp'] = 60;
      vars['baseAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      vars['frame2'] = 0;
      while (true) {
        vars['frame2'] = (vars['frame2'] || 0) + (1);
        vars['l'] = (vars['l'] || 0) + (1);
        vars['angle'] = (vars.baseAngle !== undefined ? vars.baseAngle : 0) - (vars.amp !== undefined ? vars.amp : 0) * sin((vars.frame2 !== undefined ? vars.frame2 : 0) * 360 / (vars.period !== undefined ? vars.period : 0));
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
        if (!!(_util.fuzzyEqual((vars.l !== undefined ? vars.l : 0) ,3))) {
          vars['l'] = 0;
        }
        state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_33_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_34'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_77 = vars['i'];
  for (let _loopIdx_77 = 0, _limit_77 = Math.round(6); _loopIdx_77 < _limit_77; _loopIdx_77++) {
    vars['i'] = _loopIdx_77;
    vars['ey'] = 448;
    if (_util.executeBlock({ type: 'play_sound', soundName: "charge2", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.15);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "50", angle: "-90", count: "4 + w", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "light", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "don00", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 3.5);
    yield;
    vars['w'] = (vars['w'] || 0) + (1);
  }
  vars['i'] = _prev_i_77;
};
window.compiledDanmaku['danmaku_34_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (0.9)))) {
      vars['radius'] = (vars['radius'] || 0) + (1);
      vars['hitRadius'] = (vars['hitRadius'] || 0) + (1);
      vars['speed'] = (vars['speed'] || 0) + (1);
      vars['auraIntensity'] = (vars['auraIntensity'] || 0) + (0.2);
    }
    if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff8811", speed: "200", angle: "angle + 5", count: "36", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "25", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['y'] = -80000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_34_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0.2) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1)))) {
      vars['radius'] = (vars['radius'] || 0) - (0.2);
      vars['hitRadius'] = (vars['hitRadius'] || 0) - (0.2);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_35'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3388ff", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff33aa", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = random(40,140);
    state.waitTimer = Math.max(0.0167, 0.05);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "100 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "100 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "700 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "100 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "100 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "400 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "700 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['r'] = random(-50,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "400 + r", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "384", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "224", mode: "seconds", duration: "0.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "200", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_way_resist', bulletType: "normal", color: "#ff3333", speed: "1000", angle: "-90", count: "25", spread: "7", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.05);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (40) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ffffff", speed: "300", angle: "angle", count: "8", spread: "12", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.7);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (50) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (60)))) {
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "300", angle: "0", count: "36", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.3);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_35_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,0.8))) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + (random(10,15));
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ff88").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) - (random(10,15));
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + (random(-15,15));
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_35_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_36'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['ang'] = 10;
    let _prev_i_78 = vars['i'];
    for (let _loopIdx_78 = 0, _limit_78 = Math.round(20); _loopIdx_78 < _limit_78; _loopIdx_78++) {
      vars['i'] = _loopIdx_78;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "100 + spdd", angle: "95", count: "35", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "1", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spdd'] = (vars['spdd'] || 0) + (10);
      vars['ang'] = (vars['ang'] || 0) - (1);
    }
    vars['i'] = _prev_i_78;
    vars['spdd'] = 0;
    vars['ang'] = 0;
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['ang'] = -10;
    let _prev_i_79 = vars['i'];
    for (let _loopIdx_79 = 0, _limit_79 = Math.round(20); _loopIdx_79 < _limit_79; _loopIdx_79++) {
      vars['i'] = _loopIdx_79;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "100 + spdd", angle: "95", count: "35", offsetX: "0", offsetY: "0", radius: "15", bulletImage: "uroko", coordMode: "relative", hitRadius: "1", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spdd'] = (vars['spdd'] || 0) + (10);
      vars['ang'] = (vars['ang'] || 0) + (1);
    }
    vars['i'] = _prev_i_79;
    vars['spdd'] = 0;
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
  }
};
window.compiledDanmaku['danmaku_36_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1.65)))) {
      vars['angle'] = (vars['angle'] || 0) + ((vars.ang !== undefined ? vars.ang : 0));
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,0.3))) {
      if (_util.executeBlock({ type: 'tween_var', name: "hitRadius", from: "1", to: "8", mode: "seconds", duration: "6", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_36_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_37'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle + 5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle + 2.5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ffdd33", speed: "200", angle: "angle + 7.5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ac008f", speed: "200", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ac008f", speed: "200", angle: "angle - 5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ac008f", speed: "200", angle: "angle - 2.5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ac008f", speed: "200", angle: "angle - 7.5", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
  }
};
window.compiledDanmaku['danmaku_37_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};
window.compiledDanmaku['danmaku_37_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_38'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "laser", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "0", count: "6", offsetX: "384", offsetY: "448", radius: "6", bulletImage: "none", coordMode: "absolute", hitRadius: "#ff3333", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    while (true) {
      let _prev_i_80 = vars['i'];
      for (let _loopIdx_80 = 0, _limit_80 = Math.round(38); _loopIdx_80 < _limit_80; _loopIdx_80++) {
        vars['i'] = _loopIdx_80;
        if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "90", offsetX: "xp", offsetY: "0", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xp'] = (vars['xp'] || 0) + (20);
        state.waitTimer = Math.max(0.0167, 0.16);
        yield;
        if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      vars['i'] = _prev_i_80;
      let _prev_i_81 = vars['i'];
      for (let _loopIdx_81 = 0, _limit_81 = Math.round(38); _loopIdx_81 < _limit_81; _loopIdx_81++) {
        vars['i'] = _loopIdx_81;
        if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "90", offsetX: "xp", offsetY: "0", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['xp'] = (vars['xp'] || 0) - (20);
        state.waitTimer = Math.max(0.0167, 0.16);
        yield;
        if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      vars['i'] = _prev_i_81;
      while (true) {
        let _prev_i_82 = vars['i'];
        for (let _loopIdx_82 = 0, _limit_82 = Math.round(38); _loopIdx_82 < _limit_82; _loopIdx_82++) {
          vars['i'] = _loopIdx_82;
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "90", offsetX: "xp", offsetY: "0", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-90", offsetX: "768 - xp", offsetY: "896", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "0", offsetX: "0", offsetY: "yp", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "180", offsetX: "768", offsetY: "896 - yp", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['xp'] = (vars['xp'] || 0) + (20);
          vars['yp'] = (vars['yp'] || 0) + (23.5789473684);
          state.waitTimer = Math.max(0.0167, 0.16);
          yield;
          if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_82;
        let _prev_i_83 = vars['i'];
        for (let _loopIdx_83 = 0, _limit_83 = Math.round(38); _loopIdx_83 < _limit_83; _loopIdx_83++) {
          vars['i'] = _loopIdx_83;
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "90", offsetX: "xp", offsetY: "0", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "-90", offsetX: "768 - xp", offsetY: "896", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "0", offsetX: "0", offsetY: "yp", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "180", offsetX: "768", offsetY: "896 - yp", radius: "20", bulletImage: "b_knife", coordMode: "absolute", hitRadius: "4", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['xp'] = (vars['xp'] || 0) - (20);
          vars['yp'] = (vars['yp'] || 0) - (23.5789473684);
          state.waitTimer = Math.max(0.0167, 0.16);
          yield;
          if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_83;
        state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
        yield;
      }
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,15))) {
      if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_38_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa33").trim().toLowerCase())) {
      vars['warningTime'] = 0;
      vars['activeTime'] = 150;
      vars['laserWidth'] = 20;
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (15) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (40)))) {
        vars['angle'] = (vars['angle'] || 0) + (0.2);
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_38_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_39'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['xsp'] = random(0,768);
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff661f", speed: "200", angle: "angle", offsetX: "xsp", offsetY: "896", radius: "10", bulletImage: "light", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.02);
    yield;
  }
};
window.compiledDanmaku['danmaku_39_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.isTouchEdge !== undefined ? vars.isTouchEdge : 0))) {
      vars['a'] = random(-20,20);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "200", angle: "-90 + a", count: "1", spread: "30", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['speed'] = 0;
      vars['auraIntensity'] = 2.5;
      let _prev_i_84 = vars['i'];
      for (let _loopIdx_84 = 0, _limit_84 = Math.round(20); _loopIdx_84 < _limit_84; _loopIdx_84++) {
        vars['i'] = _loopIdx_84;
        vars['auraRange'] = (vars['auraRange'] || 0) + (2.5);
        vars['auraIntensity'] = (vars['auraIntensity'] || 0) - (0.1);
        vars['hitRadius'] = (vars['hitRadius'] || 0) + (12);
        state.waitTimer = Math.max(0.0167, 0.03);
        yield;
      }
      vars['i'] = _prev_i_84;
      let _prev_i_85 = vars['i'];
      for (let _loopIdx_85 = 0, _limit_85 = Math.round(20); _loopIdx_85 < _limit_85; _loopIdx_85++) {
        vars['i'] = _loopIdx_85;
        vars['auraRange'] = (vars['auraRange'] || 0) + (1.5);
        vars['auraIntensity'] = (vars['auraIntensity'] || 0) - (0.01);
        vars['hitRadius'] = (vars['hitRadius'] || 0) - (4);
        vars['radius'] = (vars['radius'] || 0) - (1);
        state.waitTimer = Math.max(0.0167, 0.03);
        yield;
      }
      vars['i'] = _prev_i_85;
      vars['y'] = -8000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_39_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!!((vars.y !== undefined ? vars.y : 0) < 0)) {
        vars['angle'] = 90 + random(-5,5);
        vars['speed'] = 5;
        vars['color'] = "#33ffff";
        vars['y'] = 5;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      vars['speed'] = (vars['speed'] || 0) + (1.5);
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_40'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "180", to: "448", mode: "seconds", duration: "3", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 3);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "charge2", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    while (true) {
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
      let _prev_i_86 = vars['i'];
      for (let _loopIdx_86 = 0, _limit_86 = Math.round(4); _loopIdx_86 < _limit_86; _loopIdx_86++) {
        vars['i'] = _loopIdx_86;
        let _prev_i_87 = vars['i'];
        for (let _loopIdx_87 = 0, _limit_87 = Math.round(2); _loopIdx_87 < _limit_87; _loopIdx_87++) {
          vars['i'] = _loopIdx_87;
          if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "34000", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_87;
        vars['angle'] = (vars['angle'] || 0) + (6);
        state.waitTimer = Math.max(0.0167, 0.025);
        yield;
      }
      vars['i'] = _prev_i_86;
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
  }
};
window.compiledDanmaku['danmaku_40_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (2) && ((vars.frame !== undefined ? vars.frame : 0)) <= (4)))) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['6ra6f0nhe']) {
        state.onceMap['6ra6f0nhe'] = true;
        vars['speed'] = 80;
        vars['angle'] = (vars['angle'] || 0) + (180);
        vars['angle'] = (vars['angle'] || 0) + (random(-10,10));
        vars['speed'] = (vars['speed'] || 0) + (random(-5,5));
      }
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,7.5))) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['66sbqr8lm']) {
        state.onceMap['66sbqr8lm'] = true;
        vars['speed'] = 30;
        vars['color'] = "#dddddd";
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_40_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_41'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(((function(){ if (vars.n !== undefined) { const n = vars.n; return (_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,0.1 + 10 * n)); } else { for (let n = 1; n <= 100000; n++) { if (_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,0.1 + 10 * n)) return true; } return false; } })()))) {
      vars['offset'] = 0;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "agg", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['offset'] = 72 * 1;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "agg", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['offset'] = 72 * 2;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "agg", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['offset'] = 72 * 3;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "agg", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['offset'] = 72 * 4;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "agg", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (1) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (50000)))) {
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (15)))) {
        if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "260", angle: "-agg", count: "5", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.15);
        yield;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,5))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,10))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,15))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,20))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (25) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (40)))) {
        if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "200", angle: "agg", count: "5", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.15);
        yield;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,30))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,35))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,40))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,45))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (50) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (65)))) {
        if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3332", speed: "160", angle: "agg * -10", count: "5", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.08);
        yield;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,55))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,60))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,65))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "change", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,70))) {
        if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "agg", from: "0", to: "360", mode: "seconds", duration: "15", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 15);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_41_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa33").trim().toLowerCase())) {
      vars['warningTime'] = 1;
      vars['activeTime'] = 4;
      vars['laserWidth'] = 60;
      vars['angle'] = (vars.e_agg !== undefined ? vars.e_agg : 0) + (vars.offset !== undefined ? vars.offset : 0);
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#ffaa33").trim().toLowerCase())) {
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,5))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,10))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,15))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,20))) {
        if (_util.executeBlock({ type: 'aim_at_coord', targetX: "ex", targetY: "ey", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['color'] = "#ff3333";
        vars['hitRadius'] = 6;
      }
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (25)))) {
        vars['speed'] = (vars['speed'] || 0) + (1.5);
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,30))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,35))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,40))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,45))) {
        if (_util.executeBlock({ type: 'aim_at_coord', targetX: "ex", targetY: "ey", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['color'] = "#ff3333";
        vars['hitRadius'] = 6;
      }
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (45) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (50)))) {
        vars['speed'] = (vars['speed'] || 0) + (1.5);
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,55))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,60))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,65))) {
        vars['speed'] = 0;
        vars['color'] = "#ffffff";
        vars['hitRadius'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.cardSecond !== undefined ? vars.cardSecond : 0) ,70))) {
        if (_util.executeBlock({ type: 'aim_at_coord', targetX: "ex", targetY: "ey", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['color'] = "#ff3333";
        vars['hitRadius'] = 6;
      }
      if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (70) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (73)))) {
        vars['speed'] = (vars['speed'] || 0) + (0.5);
      }
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_41_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_42'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['idousakix'] = random(-200,200);
    vars['idousakiy'] = random(-100,100);
    if (_util.executeBlock({ type: 'tween_var', name: "ex", from: "ex", to: "384 + idousakix", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'tween_var', name: "ey", from: "ey", to: "179 + idousakiy", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['stop'] = 0;
    let _prev_i_88 = vars['i'];
    for (let _loopIdx_88 = 0, _limit_88 = Math.round(40); _loopIdx_88 < _limit_88; _loopIdx_88++) {
      vars['i'] = _loopIdx_88;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "12", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "kome", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (4);
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_88;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['stop'] = 1;
    vars['kaisi'] = 0.1;
    vars['kaisi2'] = 2;
    let _prev_i_89 = vars['i'];
    for (let _loopIdx_89 = 0, _limit_89 = Math.round(10); _loopIdx_89 < _limit_89; _loopIdx_89++) {
      vars['i'] = _loopIdx_89;
      vars['kaisi'] = (vars['kaisi'] || 0) + (0.01);
      vars['kaisi2'] = (vars['kaisi2'] || 0) + (0.1);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#3333ff", speed: "800", angle: "angle", count: "24", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (6.5);
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_89;
    let _prev_i_90 = vars['i'];
    for (let _loopIdx_90 = 0, _limit_90 = Math.round(10); _loopIdx_90 < _limit_90; _loopIdx_90++) {
      vars['i'] = _loopIdx_90;
      vars['kaisi'] = (vars['kaisi'] || 0) + (0.01);
      vars['kaisi2'] = (vars['kaisi2'] || 0) + (0.1);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#3333ff", speed: "800", angle: "angle", count: "24", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) - (-2);
      state.waitTimer = Math.max(0.0167, 0.02);
      yield;
    }
    vars['i'] = _prev_i_90;
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    vars['stop'] = 2;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['stop'] = 0;
    state.waitTimer = Math.max(0.0167, 1.5 - (vars.minus !== undefined ? vars.minus : 0));
    yield;
    vars['minus'] = (vars['minus'] || 0) + (0.1);
  }
};
window.compiledDanmaku['danmaku_42_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!!(_util.fuzzyEqual((vars.e_stop !== undefined ? vars.e_stop : 0) ,0))) {
        vars['hitRadius'] = 12;
        vars['radius'] = 20;
        vars['speed'] = 500;
      }
      if (!!(_util.fuzzyEqual((vars.e_stop !== undefined ? vars.e_stop : 0) ,1))) {
        vars['hitRadius'] = 0;
        vars['radius'] = 0;
        vars['speed'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.e_stop !== undefined ? vars.e_stop : 0) ,2))) {
        vars['radius'] = 20;
        vars['speed'] = 0;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3333ff").trim().toLowerCase())) {
      if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,(vars.kaisi !== undefined ? vars.kaisi : 0)))) {
        vars['speed'] = 0;
      }
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= ((vars.kaisi2 !== undefined ? vars.kaisi2 : 0)) && ((vars.timer !== undefined ? vars.timer : 0)) <= (9000)))) {
        vars['speed'] = 600;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_42_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_43'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_91 = vars['i'];
    for (let _loopIdx_91 = 0, _limit_91 = Math.round(2); _loopIdx_91 < _limit_91; _loopIdx_91++) {
      vars['i'] = _loopIdx_91;
      state.waitTimer = Math.max(0.0167, 1.5);
      yield;
      vars['sp'] = 550;
      vars['ll'] = 4;
      let _prev_i_92 = vars['i'];
      for (let _loopIdx_92 = 0, _limit_92 = Math.round(120); _loopIdx_92 < _limit_92; _loopIdx_92++) {
        vars['i'] = _loopIdx_92;
        vars['sp'] = (vars['sp'] || 0) - (5);
        vars['angle'] = (vars['angle'] || 0) + (6 + (vars.ll !== undefined ? vars.ll : 0));
        if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "0", angle: "angle", count: "12", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.03);
        yield;
        vars['ll'] = (vars['ll'] || 0) - (0.02);
      }
      vars['i'] = _prev_i_92;
      state.waitTimer = Math.max(0.0167, 2);
      yield;
      state.waitTimer = Math.max(0.0167, 1.5);
      yield;
      vars['sp'] = 550;
      vars['ll'] = 4;
      let _prev_i_93 = vars['i'];
      for (let _loopIdx_93 = 0, _limit_93 = Math.round(120); _loopIdx_93 < _limit_93; _loopIdx_93++) {
        vars['i'] = _loopIdx_93;
        vars['sp'] = (vars['sp'] || 0) - (5);
        vars['angle'] = (vars['angle'] || 0) - (6 + (vars.ll !== undefined ? vars.ll : 0));
        if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#3333ff", speed: "0", angle: "angle", count: "12", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.03);
        yield;
        vars['ll'] = (vars['ll'] || 0) - (0.02);
      }
      vars['i'] = _prev_i_93;
      state.waitTimer = Math.max(0.0167, 2);
      yield;
    }
    vars['i'] = _prev_i_91;
    state.waitTimer = Math.max(0.0167, 20);
    yield;
  }
};
window.compiledDanmaku['danmaku_43_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['q1dgpqxqj']) {
      state.onceMap['q1dgpqxqj'] = true;
      vars['hitRadius'] = 0;
      if (_util.executeBlock({ type: 'advance', distance: "sp / 4", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['speed'] = 50;
      state.waitTimer = Math.max(0.0167, 0.5);
      yield;
      vars['hitRadius'] = 3;
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (3) && ((vars.timer !== undefined ? vars.timer : 0)) <= (6)))) {
      vars['speed'] = (vars['speed'] || 0) + (1);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_43_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_44'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "200", angle: "0", count: "6", offsetX: "0", offsetY: "0", radius: "45", bulletImage: "ootama", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 9);
    yield;
  }
};
window.compiledDanmaku['danmaku_44_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.n !== undefined ? (vars.cardFrame === 30 * vars.n) : (Math.round(vars.cardFrame / 30) >= 1 && Math.abs(vars.cardFrame - 30 * Math.round(vars.cardFrame / 30)) < 0.017)))) {
      vars['l'] = (vars.angle !== undefined ? vars.angle : 0);
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars.l !== undefined ? vars.l : 0);
    }
    if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['sgdmfr48q']) {
        state.onceMap['sgdmfr48q'] = true;
        if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['84i27shjg']) {
        state.onceMap['84i27shjg'] = true;
        if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['t2mbisfh0']) {
        state.onceMap['t2mbisfh0'] = true;
        if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['t6nymrecj']) {
        state.onceMap['t6nymrecj'] = true;
        vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_44_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_45'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    vars['angle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "300", angle: "0", count: "7", offsetX: "0", offsetY: "0", radius: "60", bulletImage: "ootama", coordMode: "relative", hitRadius: "40", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot_raw", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 3);
    yield;
  }
};
window.compiledDanmaku['danmaku_45_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (!!((vars.n !== undefined ? (vars.cardFrame === 10 * vars.n) : (Math.round(vars.cardFrame / 10) >= 1 && Math.abs(vars.cardFrame - 10 * Math.round(vars.cardFrame / 10)) < 0.017)))) {
      vars['spangle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "0", angle: "spangle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_45_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4)))) {
      vars['speed'] = (vars['speed'] || 0) + (0.4);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_46'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  let _prev_i_94 = vars['i'];
  for (let _loopIdx_94 = 0, _limit_94 = Math.round(12000); _loopIdx_94 < _limit_94; _loopIdx_94++) {
    vars['i'] = _loopIdx_94;
    if (_util.executeBlock({ type: 'spawn_trail', bulletType: "trail", color: "#00ffff", speed: "400", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "false", coordMode: "relative", hitRadius: "8", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail', bulletType: "trail", color: "#00ffff", speed: "400", angle: "angle + 90", offsetX: "0", offsetY: "0", radius: "8", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "false", coordMode: "relative", hitRadius: "8", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail', bulletType: "trail", color: "#00ffff", speed: "400", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "8", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "false", coordMode: "relative", hitRadius: "8", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail', bulletType: "trail", color: "#00ffff", speed: "400", angle: "angle + 270", offsetX: "0", offsetY: "0", radius: "8", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "false", coordMode: "relative", hitRadius: "8", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (random(0,40));
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_way', bulletType: "laser", color: "#ff3333", radius: "6", speed: "450", angle: "angle", count: "2", spread: "45", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
  }
  vars['i'] = _prev_i_94;
};
window.compiledDanmaku['danmaku_46_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['period'] = 60 + random(-20,20);
    vars['amp'] = 30;
    vars['baseAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    vars['frame2'] = 0;
    while (true) {
      vars['frame2'] = (vars['frame2'] || 0) + (1);
      vars['l'] = (vars['l'] || 0) + (1);
      vars['angle'] = (vars.baseAngle !== undefined ? vars.baseAngle : 0) - (vars.amp !== undefined ? vars.amp : 0) * sin((vars.frame2 !== undefined ? vars.frame2 : 0) * 360 / (vars.period !== undefined ? vars.period : 0));
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_46_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_47'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = 6 + (vars.offset !== undefined ? vars.offset : 0);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "0", angle: "angle", count: "18 + way", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "star", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "6", speed: "0", angle: "angle", count: "18 + way", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 3);
    yield;
    vars['offset'] = (vars['offset'] || 0) + (3);
    vars['way'] = (vars['way'] || 0) + (1);
  }
};
window.compiledDanmaku['danmaku_47_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (7);
    if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['35cf3irmg']) {
        state.onceMap['35cf3irmg'] = true;
        vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        vars['angle'] = (vars['angle'] || 0) + (180);
        vars['speed'] = 0;
      }
    }
    if (!!((vars.x !== undefined ? vars.x : 0) > 758)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['chcbbes42']) {
        state.onceMap['chcbbes42'] = true;
        vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        vars['angle'] = (vars['angle'] || 0) + (180);
        vars['speed'] = 0;
      }
    }
    if (!!((vars.y !== undefined ? vars.y : 0) < 10)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['8osmpmo6z']) {
        state.onceMap['8osmpmo6z'] = true;
        vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        vars['speed'] = 0;
      }
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 886)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['vyhp7lsua']) {
        state.onceMap['vyhp7lsua'] = true;
        vars['angle'] = -(vars.angle !== undefined ? vars.angle : 0);
        vars['speed'] = 0;
      }
    }
    if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (0) && ((vars.speed !== undefined ? vars.speed : 0)) <= (400)))) {
      vars['speed'] = (vars['speed'] || 0) + (3);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_47_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_48'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_95 = vars['i'];
    for (let _loopIdx_95 = 0, _limit_95 = Math.round(3); _loopIdx_95 < _limit_95; _loopIdx_95++) {
      vars['i'] = _loopIdx_95;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 2);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 3);
      yield;
    }
    vars['i'] = _prev_i_95;
    let _prev_i_96 = vars['i'];
    for (let _loopIdx_96 = 0, _limit_96 = Math.round(3); _loopIdx_96 < _limit_96; _loopIdx_96++) {
      vars['i'] = _loopIdx_96;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 2);
      yield;
    }
    vars['i'] = _prev_i_96;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6666666667);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6666666667);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ffdd33", radius: "50", speed: "15", angle: "angle", count: "12", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6666666667);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6666666667);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.6666666667);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "boon01", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 20);
    yield;
  }
};
window.compiledDanmaku['danmaku_48_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,2))) {
      vars['speed'] = 1500;
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (3) && ((vars.frame !== undefined ? vars.frame : 0)) <= (870000)))) {
      vars['spangle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "1", angle: "spangle", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.007);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_48_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (2) && ((vars.timer !== undefined ? vars.timer : 0)) <= (3)))) {
      vars['speed'] = (vars['speed'] || 0) + (2);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_49'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = (vars['angle'] || 0) + (random(0,360));
    state.waitTimer = Math.max(0.0167, 3);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "1", speed: "400", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#33ffff", radius: "1", speed: "500", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "6", speed: "400", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#33ffff", radius: "6", speed: "500", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['ex'] = 384 + random(-100,100);
    vars['ey'] = 400 + random(-100,100);
  }
};
window.compiledDanmaku['danmaku_49_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      vars['offset'] = 0.8;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      vars['offset'] = -0.8;
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (0.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (3 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (-2 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (2.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (7 - (vars.plus !== undefined ? vars.plus : 0) * (vars.offset !== undefined ? vars.offset : 0));
      vars['plus'] = (vars['plus'] || 0) + (0.2);
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (3.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4)))) {
      vars['angle'] = (vars['angle'] || 0) + (4 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (4) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4.5)))) {
      vars['angle'] = (vars['angle'] || 0) - (1 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (4.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5)))) {
      vars['angle'] = (vars['angle'] || 0) + (1 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5.5)))) {
      vars['angle'] = (vars['angle'] || 0) - (1);
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (5.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (6)))) {
      vars['angle'] = (vars['angle'] || 0) + (1);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_49_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_50'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = (vars['angle'] || 0) + (random(0,360));
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "6", speed: "400", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#33ffff", radius: "6", speed: "500", angle: "angle", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['ex'] = 384 + random(-100,100);
    vars['ey'] = 400 + random(-100,100);
  }
};
window.compiledDanmaku['danmaku_50_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      vars['offset'] = 0.8;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      vars['offset'] = -0.8;
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (0.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (3 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (-2 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (1.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (2.5)))) {
      vars['angle'] = (vars['angle'] || 0) + (7 - (vars.plus !== undefined ? vars.plus : 0) * (vars.offset !== undefined ? vars.offset : 0));
      vars['plus'] = (vars['plus'] || 0) + (0.2);
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (3.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4)))) {
      vars['angle'] = (vars['angle'] || 0) + (4 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (4) && ((vars.timer !== undefined ? vars.timer : 0)) <= (4.5)))) {
      vars['angle'] = (vars['angle'] || 0) - (1 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (4.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5)))) {
      vars['angle'] = (vars['angle'] || 0) + (1 * (vars.offset !== undefined ? vars.offset : 0));
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5.5)))) {
      vars['angle'] = (vars['angle'] || 0) - (1);
    }
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (5.5) && ((vars.timer !== undefined ? vars.timer : 0)) <= (6)))) {
      vars['angle'] = (vars['angle'] || 0) + (1);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_50_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_51'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "1", angle: "angle", offsetX: "0", offsetY: "0", radius: "0", bulletImage: "none", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 6);
    yield;
  }
};
window.compiledDanmaku['danmaku_51_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.n !== undefined ? (vars.cardFrame === 2 * vars.n) : (Math.round(vars.cardFrame / 2) >= 1 && Math.abs(vars.cardFrame - 2 * Math.round(vars.cardFrame / 2)) < 0.017)))) {
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle", offsetX: "rx", offsetY: "ry", radius: "15", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    if (!!((vars.n !== undefined ? (vars.cardFrame === 30 * vars.n) : (Math.round(vars.cardFrame / 30) >= 1 && Math.abs(vars.cardFrame - 30 * Math.round(vars.cardFrame / 30)) < 0.017)))) {
      vars['speed'] = 1600;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['speed'] = (vars['speed'] || 0) + (-30);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_51_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.timer !== undefined ? vars.timer : 0) > 0.5)) {
      vars['hitRadius'] = 6;
      vars['color'] = "= #ff3333";
    }
    if (!!((vars.timer !== undefined ? vars.timer : 0) > 6)) {
      vars['y'] = -80000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_52'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 348;
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    vars['heru'] = 600000;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "120", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3332ff", speed: "120", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_97 = vars['i'];
    for (let _loopIdx_97 = 0, _limit_97 = Math.round(48); _loopIdx_97 < _limit_97; _loopIdx_97++) {
      vars['i'] = _loopIdx_97;
      let _prev_j_98 = vars['j'];
      for (let _loopIdx_98 = 0, _limit_98 = Math.round(3); _loopIdx_98 < _limit_98; _loopIdx_98++) {
        vars['j'] = _loopIdx_98;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (1.25);
        vars['ikouangle'] = (vars['ikouangle'] || 0) + (0.25);
      }
      vars['j'] = _prev_j_98;
      vars['ikouangle'] = (vars['ikouangle'] || 0) + (3.5);
      state.waitTimer = Math.max(0.0167, 0.035);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_97;
    vars['heru'] = 1.6;
    let _prev_i_99 = vars['i'];
    for (let _loopIdx_99 = 0, _limit_99 = Math.round(52); _loopIdx_99 < _limit_99; _loopIdx_99++) {
      vars['i'] = _loopIdx_99;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle - 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle + 180 + 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 180 - 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      let _prev_j_100 = vars['j'];
      for (let _loopIdx_100 = 0, _limit_100 = Math.round(3); _loopIdx_100 < _limit_100; _loopIdx_100++) {
        vars['j'] = _loopIdx_100;
        vars['angle'] = (vars['angle'] || 0) + (0.7);
        vars['ikouangle'] = (vars['ikouangle'] || 0) + (2);
        vars['heru'] = (vars['heru'] || 0) - (0.01);
      }
      vars['j'] = _prev_j_100;
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_99;
    state.waitTimer = Math.max(0.0167, 2.2 - (vars.hakkyou !== undefined ? vars.hakkyou : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 1 - (vars.hakkyou2 !== undefined ? vars.hakkyou2 : 0));
    yield;
    vars['hakkyou'] = (vars['hakkyou'] || 0) + (1);
    vars['hakkyou'] = (vars['hakkyou'] || 0) + (0.5);
  }
};
window.compiledDanmaku['danmaku_52_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (60) && ((vars.frame !== undefined ? vars.frame : 0)) <= (180)))) {
      vars['speed'] = (vars['speed'] || 0) - (4);
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (180) && ((vars.frame !== undefined ? vars.frame : 0)) <= (190)))) {
      vars['speed'] = 0;
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (260) && ((vars.frame !== undefined ? vars.frame : 0)) <= (305)))) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['24549go66']) {
        state.onceMap['24549go66'] = true;
        vars['angle'] = (vars['angle'] || 0) - (90);
        vars['angle'] = (vars['angle'] || 0) + ((vars.ikouangle !== undefined ? vars.ikouangle : 0) * 3);
        vars['speed'] = 0;
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) - (2);
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3332ff").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) - (2);
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      vars['speed'] = 200;
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,(vars.heru !== undefined ? vars.heru : 0)))) {
      vars['speed'] = 0;
    }
    if (!!((vars.frame !== undefined ? vars.frame : 0) > 305)) {
      state.waitTimer = Math.max(0.0167, 99999);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_52_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_53'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 348;
    state.waitTimer = Math.max(0.0167, 0.2);
    yield;
    vars['heru'] = 600000;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "120", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3332ff", speed: "120", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "light", coordMode: "relative", hitRadius: "30", }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_101 = vars['i'];
    for (let _loopIdx_101 = 0, _limit_101 = Math.round(48); _loopIdx_101 < _limit_101; _loopIdx_101++) {
      vars['i'] = _loopIdx_101;
      let _prev_j_102 = vars['j'];
      for (let _loopIdx_102 = 0, _limit_102 = Math.round(6); _loopIdx_102 < _limit_102; _loopIdx_102++) {
        vars['j'] = _loopIdx_102;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (1.25 / 2);
        vars['ikouangle'] = (vars['ikouangle'] || 0) + (0.25);
      }
      vars['j'] = _prev_j_102;
      vars['ikouangle'] = (vars['ikouangle'] || 0) + (3.5);
      state.waitTimer = Math.max(0.0167, 0.035);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_101;
    vars['heru'] = 1.6;
    let _prev_i_103 = vars['i'];
    for (let _loopIdx_103 = 0, _limit_103 = Math.round(52); _loopIdx_103 < _limit_103; _loopIdx_103++) {
      vars['i'] = _loopIdx_103;
      let _prev_j_104 = vars['j'];
      for (let _loopIdx_104 = 0, _limit_104 = Math.round(3); _loopIdx_104 < _limit_104; _loopIdx_104++) {
        vars['j'] = _loopIdx_104;
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle - 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "240", angle: "angle + 180 + 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3333ff", speed: "240", angle: "angle + 180 - 6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "b_ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (0.7);
        vars['ikouangle'] = (vars['ikouangle'] || 0) + (2);
        vars['heru'] = (vars['heru'] || 0) - (0.01);
      }
      vars['j'] = _prev_j_104;
      state.waitTimer = Math.max(0.0167, 0.03);
      yield;
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_103;
    state.waitTimer = Math.max(0.0167, 2.2 - (vars.hakkyou !== undefined ? vars.hakkyou : 0));
    yield;
    state.waitTimer = Math.max(0.0167, 1 - (vars.hakkyou2 !== undefined ? vars.hakkyou2 : 0));
    yield;
    vars['hakkyou'] = (vars['hakkyou'] || 0) + (1);
    vars['hakkyou'] = (vars['hakkyou'] || 0) + (0.5);
  }
};
window.compiledDanmaku['danmaku_53_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (60) && ((vars.frame !== undefined ? vars.frame : 0)) <= (180)))) {
      vars['speed'] = (vars['speed'] || 0) - (4);
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (180) && ((vars.frame !== undefined ? vars.frame : 0)) <= (190)))) {
      vars['speed'] = 0;
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (260) && ((vars.frame !== undefined ? vars.frame : 0)) <= (305)))) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['okbvwi0cu']) {
        state.onceMap['okbvwi0cu'] = true;
        vars['angle'] = (vars['angle'] || 0) - (90);
        vars['angle'] = (vars['angle'] || 0) + ((vars.ikouangle !== undefined ? vars.ikouangle : 0) * 3);
        vars['speed'] = 0;
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) - (2);
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3332ff").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) - (2);
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
      vars['speed'] = 200;
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,(vars.heru !== undefined ? vars.heru : 0)))) {
      vars['speed'] = 0;
    }
    if (!!((vars.frame !== undefined ? vars.frame : 0) > 305)) {
      state.waitTimer = Math.max(0.0167, 99999);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_53_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_54'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    vars['kaisuu'] = (vars['kaisuu'] || 0) + (1);
    vars['angle'] = _util.seedrandom(1000 + (vars.kaisuu !== undefined ? vars.kaisuu : 0), -160, 160, vars);
    vars['angle2'] = _util.seedrandom(100 + (vars.kaisuu !== undefined ? vars.kaisuu : 0) * 2, -160, 160, vars);
    vars['plusangle'] = _util.seedrandom(200 + (vars.kaisuu !== undefined ? vars.kaisuu : 0), -2, 2, vars);
    let _prev_i_105 = vars['i'];
    for (let _loopIdx_105 = 0, _limit_105 = Math.round(60); _loopIdx_105 < _limit_105; _loopIdx_105++) {
      vars['i'] = _loopIdx_105;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "400", angle: "-90 + angle", offsetX: "0", offsetY: "0", radius: "19", bulletImage: "kome", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "400", angle: "-90 + angle2", offsetX: "0", offsetY: "0", radius: "19", bulletImage: "kome", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.016);
      yield;
    }
    vars['i'] = _prev_i_105;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_54_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1)))) {
      vars['angle'] = (vars['angle'] || 0) + ((vars.plusangle !== undefined ? vars.plusangle : 0));
    }
    if (!!(_util.fuzzyEqual((vars.timer !== undefined ? vars.timer : 0) ,1))) {
      vars['angle'] = random(0,360);
      vars['speed'] = 300;
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_54_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_55'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_106 = vars['i'];
    for (let _loopIdx_106 = 0, _limit_106 = Math.round(3); _loopIdx_106 < _limit_106; _loopIdx_106++) {
      vars['i'] = _loopIdx_106;
      if (_util.executeBlock({ type: 'spawn_beam', warningTime: "1.0", activeTime: "0", laserWidth: "60", angle: "90 + 60", offsetX: "0", offsetY: "0", coordMode: "relative", hitRadius: "#ff3333", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_beam', warningTime: "1.0", activeTime: "0", laserWidth: "60", angle: "90 - 60", offsetX: "0", offsetY: "0", coordMode: "relative", hitRadius: "#ff3333", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#00ffff", speed: "1000", angle: "90 + 60", offsetX: "0", offsetY: "0", radius: "20", growTime: "0.2", keepTime: "2", shrinkTime: "0.0001", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#00ffff", speed: "1000", angle: "90 - 60", offsetX: "0", offsetY: "0", radius: "20", growTime: "0.2", keepTime: "2", shrinkTime: "0.0001", round: "true", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'play_sound', soundName: "laser_heavy", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 4);
      yield;
    }
    vars['i'] = _prev_i_106;
    state.waitTimer = Math.max(0.0167, 400);
    yield;
  }
};
window.compiledDanmaku['danmaku_55_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#00ffff").trim().toLowerCase())) {
      if (!!((vars.n !== undefined ? (vars.frame === 10 * vars.n) : (Math.round(vars.frame / 10) >= 1 && Math.abs(vars.frame - 10 * Math.round(vars.frame / 10)) < 0.017)))) {
        vars['second2'] = 1;
        vars['spangle'] = random(0,360);
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "0", angle: "spangle", count: "7", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['second2'] = 2;
        vars['spangle'] = random(0,360);
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "0", angle: "spangle", count: "7", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['second2'] = 3;
        vars['spangle'] = random(0,360);
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "0", angle: "spangle", count: "7", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if (!!((((vars.second !== undefined ? vars.second : 0)) >= (15) && ((vars.second !== undefined ? vars.second : 0)) <= (30000)))) {
      state.waitTimer = Math.max(0.0167, 99999999);
      yield;
    }
    if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_55_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!(_util.fuzzyEqual((vars.second !== undefined ? vars.second : 0) ,(vars.second2 !== undefined ? vars.second2 : 0)))) {
      vars['speed'] = 200;
    }
    if (!!((((vars.second !== undefined ? vars.second : 0)) >= (4) && ((vars.second !== undefined ? vars.second : 0)) <= (30000)))) {
      state.waitTimer = Math.max(0.0167, 99999999);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_56'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    if (_util.executeBlock({ type: 'play_sound', soundName: "charge2", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    while (true) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#9457eb", speed: "700", angle: "angle", count: "7", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "light", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (45.2);
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.016);
      yield;
    }
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['muki'] = 1;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "90", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['muki'] = -1;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "90", offsetX: "768", offsetY: "0", radius: "6", bulletImage: "none", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  if (!!((vars.x !== undefined ? vars.x : 0) < 10)) {
  }
}
];
window.compiledDanmaku['danmaku_56_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#9457eb").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['qlt9hg1vp']) {
        state.onceMap['qlt9hg1vp'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "10", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['auraIntensity'] = 10;
        vars['auraRange'] = 7;
        if (_util.executeBlock({ type: 'tween_var', name: "auraRange", from: "auraRange", to: "2.75", mode: "seconds", duration: "0.5", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (200) && ((vars.speed !== undefined ? vars.speed : 0)) <= (70000)))) {
        vars['speed'] = (vars['speed'] || 0) + (-2);
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['m1vixxo1s']) {
        state.onceMap['m1vixxo1s'] = true;
        vars['warningTime'] = 0.0;
        vars['activeTime'] = 70;
        vars['laserWidth'] = 30;
        vars['tanaka'] = 0;
      }
      if (!!(_util.fuzzyEqual((vars.muki !== undefined ? vars.muki : 0),1))) {
        vars['x'] = (vars['x'] || 0) + (0.1);
        vars['x'] = (vars['x'] || 0) + (5 - (vars.tanaka !== undefined ? vars.tanaka : 0));
        if (!!((((vars.tanaka !== undefined ? vars.tanaka : 0)) >= (0) && ((vars.tanaka !== undefined ? vars.tanaka : 0)) <= (4.9)))) {
          vars['tanaka'] = (vars['tanaka'] || 0) + (0.1);
        }
      }
      if (!!(_util.fuzzyEqual((vars.muki !== undefined ? vars.muki : 0),-1))) {
        vars['x'] = (vars['x'] || 0) - (0.1);
        vars['x'] = (vars['x'] || 0) - (5 - (vars.tanaka !== undefined ? vars.tanaka : 0));
        if (!!((((vars.tanaka !== undefined ? vars.tanaka : 0)) >= (0) && ((vars.tanaka !== undefined ? vars.tanaka : 0)) <= (4.9)))) {
          vars['tanaka'] = (vars['tanaka'] || 0) + (0.1);
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_56_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_57'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['rot'] = 0;
  while (true) {
    vars['baseRot'] = (vars.rot !== undefined ? vars.rot : 0);
    vars['rot'] = (vars['rot'] || 0) + (11);
    vars['spawnAngle1'] = (vars.baseRot !== undefined ? vars.baseRot : 0);
    vars['spawnAngle2'] = -(vars.baseRot !== undefined ? vars.baseRot : 0);
    let _prev_i_107 = vars['i'];
    for (let _loopIdx_107 = 0, _limit_107 = Math.round(10); _loopIdx_107 < _limit_107; _loopIdx_107++) {
      vars['i'] = _loopIdx_107;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#cc33ff", speed: "160", angle: "spawnAngle1", count: "24", offsetX: "-114", offsetY: "54", radius: "10", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#cc33ff", speed: "160", angle: "spawnAngle1", count: "24", offsetX: "64", offsetY: "-80", radius: "10", bulletImage: "ohuda", coordMode: "relative", hitRadius: "4", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3333ff", speed: "160", angle: "spawnAngle2", count: "24", offsetX: "114", offsetY: "54", radius: "10", bulletImage: "ohuda", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3333ff", speed: "160", angle: "spawnAngle2", count: "24", offsetX: "-64", offsetY: "-80", radius: "10", bulletImage: "ohuda", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spawnAngle1'] = (vars['spawnAngle1'] || 0) + (36);
      vars['spawnAngle2'] = (vars['spawnAngle2'] || 0) - (36);
      state.waitTimer = Math.max(0.0167, 0.13);
      yield;
    }
    vars['i'] = _prev_i_107;
    state.waitTimer = Math.max(0.0167, 1.2);
    yield;
  }
};
window.compiledDanmaku['danmaku_57_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.param !== undefined ? vars.param : "")).trim().toLowerCase() === String("4").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) - (0.35);
    }
    if ((String((vars.param !== undefined ? vars.param : "")).trim().toLowerCase() === String("5").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) + (0.35);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_57_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_58'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_108 = vars['i'];
    for (let _loopIdx_108 = 0, _limit_108 = Math.round(2); _loopIdx_108 < _limit_108; _loopIdx_108++) {
      vars['i'] = _loopIdx_108;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "18", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "18", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "18", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "18", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      state.waitTimer = Math.max(0.0167, 5);
      yield;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "18", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "18", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "18", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "18", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      state.waitTimer = Math.max(0.0167, 5);
      yield;
    }
    vars['i'] = _prev_i_108;
    let _prev_i_109 = vars['i'];
    for (let _loopIdx_109 = 0, _limit_109 = Math.round(2); _loopIdx_109 < _limit_109; _loopIdx_109++) {
      vars['i'] = _loopIdx_109;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "140", angle: "baseAngle", count: "24", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "141", angle: "baseAngle", count: "24", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "141", angle: "baseAngle", count: "24", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "140", angle: "baseAngle", count: "24", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = (vars['baseAngle'] || 0) + (36);
      state.waitTimer = Math.max(0.0167, 5);
      yield;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "141", angle: "baseAngle", count: "24", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "140", angle: "baseAngle", count: "24", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "140", angle: "baseAngle", count: "24", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#33e0ff", speed: "141", angle: "baseAngle", count: "24", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 5);
      yield;
    }
    vars['i'] = _prev_i_109;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "64  * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "141", angle: "baseAngle", count: "32", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#cc33ff", speed: "140", angle: "baseAngle", count: "32", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['baseAngle'] = random(0,360);
    state.waitTimer = Math.max(0.0167, 7);
    yield;
    let _prev_i_110 = vars['i'];
    for (let _loopIdx_110 = 0, _limit_110 = Math.round(20); _loopIdx_110 < _limit_110; _loopIdx_110++) {
      vars['i'] = _loopIdx_110;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "140", angle: "baseAngle", count: "32", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "141", angle: "baseAngle", count: "32", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "141", angle: "baseAngle", count: "32", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "140", angle: "baseAngle", count: "32", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 3.5);
      yield;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "141", angle: "baseAngle", count: "32", offsetX: "-114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "140", angle: "baseAngle", count: "32", offsetX: "114 * 1.5", offsetY: "54 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "140", angle: "baseAngle", count: "32", offsetX: "-64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['baseAngle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "141", angle: "baseAngle", count: "32", offsetX: "64 * 1.5", offsetY: "-80 * 1.5", radius: "20", bulletImage: "ohuda", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 3.5);
      yield;
    }
    vars['i'] = _prev_i_110;
  }
};
window.compiledDanmaku['danmaku_58_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['5mo6giewv']) {
      state.onceMap['5mo6giewv'] = true;
      if (!!(_util.fuzzyEqual((vars.speed !== undefined ? vars.speed : 0) ,140))) {
        vars['curve'] = -1.6;
      }
      if (!!(_util.fuzzyEqual((vars.speed !== undefined ? vars.speed : 0) ,141))) {
        vars['curve'] = 1.6;
      }
      vars['speed'] = 200;
    }
    if (!!((vars.frame !== undefined ? vars.frame : 0) < 51)) {
      vars['speed'] = (vars['speed'] || 0) - (0.7);
      vars['angle'] = (vars['angle'] || 0) + ((vars.curve !== undefined ? vars.curve : 0));
      vars['y'] = (vars['y'] || 0) + (0.6);
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (60) && ((vars.frame !== undefined ? vars.frame : 0)) <= (600000)))) {
      vars['angle'] = (vars['angle'] || 0) + ((vars.curve !== undefined ? vars.curve : 0) / 6);
      vars['y'] = (vars['y'] || 0) + (1);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_58_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_59'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "150", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "light", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (random(0,360));
    state.waitTimer = Math.max(0.0167, 1);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ffff", speed: "1000", angle: "angle", count: "10", spread: "6", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "kome", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.016);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_59_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['d026cxbl6']) {
      state.onceMap['d026cxbl6'] = true;
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#33ffff").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'advance', distance: "20", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['auraRange'] = 6;
        vars['auraIntensity'] = 3;
        if (_util.executeBlock({ type: 'tween_var', name: "auraRange", from: "auraRange", to: "2.75", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "auraIntensity", from: "auraIntensity", to: "1", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'advance', distance: "10", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_59_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_60'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_111 = vars['i'];
    for (let _loopIdx_111 = 0, _limit_111 = Math.round(100); _loopIdx_111 < _limit_111; _loopIdx_111++) {
      vars['i'] = _loopIdx_111;
      vars['angle'] = (vars['angle'] || 0) + (31);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "500", angle: "angle", count: "6", spread: "1", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_111;
    let _prev_i_112 = vars['i'];
    for (let _loopIdx_112 = 0, _limit_112 = Math.round(100); _loopIdx_112 < _limit_112; _loopIdx_112++) {
      vars['i'] = _loopIdx_112;
      vars['angle'] = (vars['angle'] || 0) + (30.3);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "700", angle: "angle", count: "6", spread: "2", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_112;
    let _prev_i_113 = vars['i'];
    for (let _loopIdx_113 = 0, _limit_113 = Math.round(10); _loopIdx_113 < _limit_113; _loopIdx_113++) {
      vars['i'] = _loopIdx_113;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "900", angle: "angle", count: "21", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
    }
    vars['i'] = _prev_i_113;
    vars['angle'] = 0;
    let _prev_i_114 = vars['i'];
    for (let _loopIdx_114 = 0, _limit_114 = Math.round(18); _loopIdx_114 < _limit_114; _loopIdx_114++) {
      vars['i'] = _loopIdx_114;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "angle", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "-angle", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (20);
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_114;
    let _prev_i_115 = vars['i'];
    for (let _loopIdx_115 = 0, _limit_115 = Math.round(5); _loopIdx_115 < _limit_115; _loopIdx_115++) {
      vars['i'] = _loopIdx_115;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spd'] = 0;
      vars['wa'] = 15;
      let _prev_i_116 = vars['i'];
      for (let _loopIdx_116 = 0, _limit_116 = Math.round(15); _loopIdx_116 < _limit_116; _loopIdx_116++) {
        vars['i'] = _loopIdx_116;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff88", speed: "500 + spd", angle: "angle", count: "wa", spread: "1", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (50);
        vars['wa'] = (vars['wa'] || 0) - (1);
        state.waitTimer = Math.max(0.0167, 0.02);
        yield;
      }
      vars['i'] = _prev_i_116;
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_115;
    let _prev_i_117 = vars['i'];
    for (let _loopIdx_117 = 0, _limit_117 = Math.round(30); _loopIdx_117 < _limit_117; _loopIdx_117++) {
      vars['i'] = _loopIdx_117;
      vars['muki'] = 1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3388ff", speed: "800", angle: "0", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['muki'] = -1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3388ff", speed: "800", angle: "0", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_117;
    let _prev_i_118 = vars['i'];
    for (let _loopIdx_118 = 0, _limit_118 = Math.round(50); _loopIdx_118 < _limit_118; _loopIdx_118++) {
      vars['i'] = _loopIdx_118;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#884898", speed: "800", angle: "frame * 3", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_118;
    while (true) {
      let _prev_i_119 = vars['i'];
      for (let _loopIdx_119 = 0, _limit_119 = Math.round(2); _loopIdx_119 < _limit_119; _loopIdx_119++) {
        vars['i'] = _loopIdx_119;
        let _prev_i_120 = vars['i'];
        for (let _loopIdx_120 = 0, _limit_120 = Math.round(5); _loopIdx_120 < _limit_120; _loopIdx_120++) {
          vars['i'] = _loopIdx_120;
          state.waitTimer = Math.max(0.0167, 0.06);
          yield;
          vars['rspx'] = _util.seedrandom(6 + (vars.seedcount !== undefined ? vars.seedcount : 0), 0, 768, vars);
          vars['seedcount'] = (vars['seedcount'] || 0) + (1);
          vars['rsped'] = 0;
          let _prev_i_121 = vars['i'];
          for (let _loopIdx_121 = 0, _limit_121 = Math.round(3); _loopIdx_121 < _limit_121; _loopIdx_121++) {
            vars['i'] = _loopIdx_121;
            if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "400 + rsped", angle: "90", offsetX: "rspx", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "absolute", hitRadius: "5", }, state, b, attacker, target, _util)) {
              yield;
            }
            vars['rsped'] = (vars['rsped'] || 0) + (16);
          }
          vars['i'] = _prev_i_121;
          vars['orspx'] = _util.seedrandom(600 + (vars.seedcount !== undefined ? vars.seedcount : 0), 0, 768, vars);
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa32", speed: "800", angle: "90", offsetX: "orspx", offsetY: "0", radius: "9", bulletImage: "light", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3387ff", speed: "800", angle: "90", offsetX: "758", offsetY: "0", radius: "10", bulletImage: "light", coordMode: "absolute", hitRadius: "7", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3387ff", speed: "800", angle: "90", offsetX: "10", offsetY: "0", radius: "10", bulletImage: "light", coordMode: "absolute", hitRadius: "7", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_120;
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#884898", speed: "800", angle: "frame * 3", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "light", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      vars['i'] = _prev_i_119;
      vars['yelangle'] = (vars['yelangle'] || 0) + (10);
      if (_util.executeBlock({ type: 'spawn_laser_ring', bulletType: "laser", color: "#ffdd32", radius: "6", speed: "900", angle: "yelangle", count: "8", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['wa'] = 5;
      vars['spd'] = 0;
      let _prev_i_122 = vars['i'];
      for (let _loopIdx_122 = 0, _limit_122 = Math.round(5); _loopIdx_122 < _limit_122; _loopIdx_122++) {
        vars['i'] = _loopIdx_122;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff87", speed: "800 + spd", angle: "90", count: "wa", spread: "1", offsetX: "tx", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (80);
        vars['wa'] = (vars['wa'] || 0) - (1);
      }
      vars['i'] = _prev_i_122;
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_60_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
      if (!!((vars.isBounced !== undefined ? vars.isBounced : 0))) {
        if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['warningTime'] = 1.0;
        vars['activeTime'] = 0.7;
        vars['laserWidth'] = 12;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (0.7)))) {
        vars['angle'] = (vars['angle'] || 0) + (2 * (vars.muki !== undefined ? vars.muki : 0));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa32").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['0xzcyncli']) {
        state.onceMap['0xzcyncli'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3387ff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['mq82m8wth']) {
        state.onceMap['mq82m8wth'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_60_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_61'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_123 = vars['i'];
    for (let _loopIdx_123 = 0, _limit_123 = Math.round(100); _loopIdx_123 < _limit_123; _loopIdx_123++) {
      vars['i'] = _loopIdx_123;
      vars['angle'] = (vars['angle'] || 0) + (31);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "500", angle: "angle", count: "6", spread: "1", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_123;
    let _prev_i_124 = vars['i'];
    for (let _loopIdx_124 = 0, _limit_124 = Math.round(100); _loopIdx_124 < _limit_124; _loopIdx_124++) {
      vars['i'] = _loopIdx_124;
      vars['angle'] = (vars['angle'] || 0) + (30.3);
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "700", angle: "angle", count: "6", spread: "2", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_124;
    let _prev_i_125 = vars['i'];
    for (let _loopIdx_125 = 0, _limit_125 = Math.round(10); _loopIdx_125 < _limit_125; _loopIdx_125++) {
      vars['i'] = _loopIdx_125;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "900", angle: "angle", count: "21", offsetX: "0", offsetY: "0", radius: "40", bulletImage: "ootama", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
    }
    vars['i'] = _prev_i_125;
    vars['angle'] = 0;
    let _prev_i_126 = vars['i'];
    for (let _loopIdx_126 = 0, _limit_126 = Math.round(18); _loopIdx_126 < _limit_126; _loopIdx_126++) {
      vars['i'] = _loopIdx_126;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "angle", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "700", angle: "-angle", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (20);
      state.waitTimer = Math.max(0.0167, 0.01);
      yield;
    }
    vars['i'] = _prev_i_126;
    let _prev_i_127 = vars['i'];
    for (let _loopIdx_127 = 0, _limit_127 = Math.round(5); _loopIdx_127 < _limit_127; _loopIdx_127++) {
      vars['i'] = _loopIdx_127;
      if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spd'] = 0;
      vars['wa'] = 15;
      let _prev_i_128 = vars['i'];
      for (let _loopIdx_128 = 0, _limit_128 = Math.round(15); _loopIdx_128 < _limit_128; _loopIdx_128++) {
        vars['i'] = _loopIdx_128;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff88", speed: "500 + spd", angle: "angle", count: "wa", spread: "1", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (50);
        vars['wa'] = (vars['wa'] || 0) - (1);
        state.waitTimer = Math.max(0.0167, 0.02);
        yield;
      }
      vars['i'] = _prev_i_128;
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_127;
    let _prev_i_129 = vars['i'];
    for (let _loopIdx_129 = 0, _limit_129 = Math.round(30); _loopIdx_129 < _limit_129; _loopIdx_129++) {
      vars['i'] = _loopIdx_129;
      vars['muki'] = 1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3388ff", speed: "800", angle: "0", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "None", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['muki'] = -1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3388ff", speed: "800", angle: "0", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "None", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_129;
    let _prev_i_130 = vars['i'];
    for (let _loopIdx_130 = 0, _limit_130 = Math.round(50); _loopIdx_130 < _limit_130; _loopIdx_130++) {
      vars['i'] = _loopIdx_130;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#884898", speed: "800", angle: "frame * 3", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "None", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    vars['i'] = _prev_i_130;
    while (true) {
      let _prev_i_131 = vars['i'];
      for (let _loopIdx_131 = 0, _limit_131 = Math.round(2); _loopIdx_131 < _limit_131; _loopIdx_131++) {
        vars['i'] = _loopIdx_131;
        let _prev_i_132 = vars['i'];
        for (let _loopIdx_132 = 0, _limit_132 = Math.round(5); _loopIdx_132 < _limit_132; _loopIdx_132++) {
          vars['i'] = _loopIdx_132;
          state.waitTimer = Math.max(0.0167, 0.06);
          yield;
          vars['rspx'] = _util.seedrandom(6 + (vars.seedcount !== undefined ? vars.seedcount : 0), 0, 768, vars);
          vars['seedcount'] = (vars['seedcount'] || 0) + (1);
          vars['rsped'] = 0;
          let _prev_i_133 = vars['i'];
          for (let _loopIdx_133 = 0, _limit_133 = Math.round(3); _loopIdx_133 < _limit_133; _loopIdx_133++) {
            vars['i'] = _loopIdx_133;
            if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "400 + rsped", angle: "90", offsetX: "rspx", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "absolute", hitRadius: "5", }, state, b, attacker, target, _util)) {
              yield;
            }
            vars['rsped'] = (vars['rsped'] || 0) + (16);
          }
          vars['i'] = _prev_i_133;
          vars['orspx'] = _util.seedrandom(600 + (vars.seedcount !== undefined ? vars.seedcount : 0), 0, 768, vars);
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffaa32", speed: "800", angle: "90", offsetX: "orspx", offsetY: "0", radius: "9", bulletImage: "None", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3387ff", speed: "800", angle: "90", offsetX: "758", offsetY: "0", radius: "10", bulletImage: "None", coordMode: "absolute", hitRadius: "7", }, state, b, attacker, target, _util)) {
            yield;
          }
          if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3387ff", speed: "800", angle: "90", offsetX: "10", offsetY: "0", radius: "10", bulletImage: "None", coordMode: "absolute", hitRadius: "7", }, state, b, attacker, target, _util)) {
            yield;
          }
        }
        vars['i'] = _prev_i_132;
        if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#884898", speed: "800", angle: "frame * 3", count: "18", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "None", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      vars['i'] = _prev_i_131;
      vars['yelangle'] = (vars['yelangle'] || 0) + (10);
      if (_util.executeBlock({ type: 'spawn_laser_ring', bulletType: "laser", color: "#ffdd32", radius: "6", speed: "900", angle: "yelangle", count: "8", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.2", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['wa'] = 5;
      vars['spd'] = 0;
      let _prev_i_134 = vars['i'];
      for (let _loopIdx_134 = 0, _limit_134 = Math.round(5); _loopIdx_134 < _limit_134; _loopIdx_134++) {
        vars['i'] = _loopIdx_134;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#33ff87", speed: "800 + spd", angle: "90", count: "wa", spread: "1", offsetX: "tx", offsetY: "0", radius: "6", bulletImage: "None", coordMode: "absolute", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (80);
        vars['wa'] = (vars['wa'] || 0) - (1);
      }
      vars['i'] = _prev_i_134;
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_61_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffdd33").trim().toLowerCase())) {
      if (!!((vars.isBounced !== undefined ? vars.isBounced : 0))) {
        if (_util.executeBlock({ type: 'bounce', }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['warningTime'] = 1.0;
        vars['activeTime'] = 0.7;
        vars['laserWidth'] = 12;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
      if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (0.7)))) {
        vars['angle'] = (vars['angle'] || 0) + (2 * (vars.muki !== undefined ? vars.muki : 0));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa32").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['av8qvgrrh']) {
        state.onceMap['av8qvgrrh'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3387ff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['nte6xjnnx']) {
        state.onceMap['nte6xjnnx'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_61_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_62'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['type'] = 1;
    vars['angle'] = (vars['angle'] || 0) + (random(-70,70));
    vars['bairitu'] = 1;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "7", speed: "800", angle: "angle", count: "24", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['angle'] = (vars['angle'] || 0) + (random(-70,70));
    vars['bairitu'] = -1;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "7", speed: "800", angle: "angle", count: "24", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['bairitu'] = 0.085;
    vars['type'] = 2;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "7", speed: "60", angle: "angle + 5", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['bairitu'] = -0.085;
    if (_util.executeBlock({ type: 'spawn_laser_ring_resist', bulletType: "laser", color: "#ff3333", radius: "7", speed: "60", angle: "angle + 5", count: "36", offsetX: "0", offsetY: "0", growTime: "0.2", keepTime: "0.3", shrinkTime: "0.5", round: "true", coordMode: "relative", hitRadius: "5", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['bairitu'] = 0;
    vars['type'] = 3;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "400", angle: "angle", count: "37", offsetX: "0", offsetY: "0", radius: "18", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "600", angle: "angle", count: "37", offsetX: "0", offsetY: "0", radius: "18", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "800", angle: "angle", count: "37", offsetX: "0", offsetY: "0", radius: "18", bulletImage: "b_marutama", coordMode: "relative", hitRadius: "15", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1);
    yield;
    vars['angle'] = (vars['angle'] || 0) + (random(-70,70));
  }
};
window.compiledDanmaku['danmaku_62_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (1)))) {
      vars['angle'] = (vars['angle'] || 0) + (3 * (vars.bairitu !== undefined ? vars.bairitu : 0) - (vars.minus !== undefined ? vars.minus : 0) * (vars.bairitu !== undefined ? vars.bairitu : 0));
      vars['minus'] = (vars['minus'] || 0) + (0.02);
    }
    vars['angle'] = (vars['angle'] || 0) + (0.6 * (vars.bairitu !== undefined ? vars.bairitu : 0));
    if (!!(_util.fuzzyEqual((vars.type !== undefined ? vars.type : 0) ,2))) {
      vars['speed'] = (vars['speed'] || 0) + (4);
      vars['angle'] = (vars['angle'] || 0) + (1.5 * (vars.bairitu !== undefined ? vars.bairitu : 0));
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_62_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_63'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  vars['ey'] = 248;
  vars['a'] = 150;
  vars['b'] = 50;
  vars['shiftX'] = 75;
  vars['theta'] = 0;
  while (true) {
    vars['cx'] = (vars.a !== undefined ? vars.a : 0) * cos((vars.theta !== undefined ? vars.theta : 0));
    vars['cy'] = (vars.b !== undefined ? vars.b : 0) * sin((vars.theta !== undefined ? vars.theta : 0));
    vars['rx'] = (vars.cx !== undefined ? vars.cx : 0) + (vars.shiftX !== undefined ? vars.shiftX : 0);
    vars['ry'] = (vars.cy !== undefined ? vars.cy : 0);
    let _prev_i_135 = vars['i'];
    for (let _loopIdx_135 = 0, _limit_135 = Math.round(8); _loopIdx_135 < _limit_135; _loopIdx_135++) {
      vars['i'] = _loopIdx_135;
      vars['dir'] = (vars.i !== undefined ? vars.i : 0) * 45;
      vars['c'] = "\"#ffffff\" // 初期値";
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,0))) {
        vars['c'] = "#ff3333";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,1))) {
        vars['c'] = "#ff9933";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,2))) {
        vars['c'] = "#ffff33";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,3))) {
        vars['c'] = "#33ff33";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,4))) {
        vars['c'] = "#33ffff";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,5))) {
        vars['c'] = "#3333ff";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,6))) {
        vars['c'] = "#9933ff";
      }
      if (!!(_util.fuzzyEqual((vars.i !== undefined ? vars.i : 0) ,7))) {
        vars['c'] = "#ff33cc";
      }
      vars['rotX'] = (vars.rx !== undefined ? vars.rx : 0) * cos((vars.dir !== undefined ? vars.dir : 0)) - (vars.ry !== undefined ? vars.ry : 0) * sin((vars.dir !== undefined ? vars.dir : 0));
      vars['rotY'] = (vars.rx !== undefined ? vars.rx : 0) * sin((vars.dir !== undefined ? vars.dir : 0)) + (vars.ry !== undefined ? vars.ry : 0) * cos((vars.dir !== undefined ? vars.dir : 0));
      vars['ox'] = (vars.ex !== undefined ? vars.ex : 0) + (vars.rotX !== undefined ? vars.rotX : 0);
      vars['oy'] = (vars.ey !== undefined ? vars.ey : 0) + (vars.rotY !== undefined ? vars.rotY : 0);
      vars['ofangle'] = (vars['ofangle'] || 0) - (2);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "c", speed: "0", angle: "theta + dir", offsetX: "ox", offsetY: "oy", radius: "20", bulletImage: "kome", coordMode: "absolute", hitRadius: "5", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_135;
    vars['theta'] = (vars['theta'] || 0) + (12);
    state.waitTimer = Math.max(0.0167, 0.02);
    yield;
  }
};
window.compiledDanmaku['danmaku_63_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = (vars['speed'] || 0) + (1 + (vars.kasoku !== undefined ? vars.kasoku : 0));
    vars['kasoku'] = (vars['kasoku'] || 0) + (0.02);
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['hqunjpymu']) {
      state.onceMap['hqunjpymu'] = true;
      vars['angle'] = (vars['angle'] || 0) + (45 + (vars.ofangle !== undefined ? vars.ofangle : 0));
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_63_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_64'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    while (true) {
      vars['angle'] = (vars['angle'] || 0) + (_util.seedrandom(50 + (vars.kaisu !== undefined ? vars.kaisu : 0), 0, 500, vars));
      vars['kaisu'] = (vars['kaisu'] || 0) + (1);
      vars['angle'] = (vars['angle'] || 0) + (9 * (vars.kakudo !== undefined ? vars.kakudo : 0));
      vars['kakudo'] = 1.2;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 90", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 180", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 270", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      let _prev_i_136 = vars['i'];
      for (let _loopIdx_136 = 0, _limit_136 = Math.round(9); _loopIdx_136 < _limit_136; _loopIdx_136++) {
        vars['i'] = _loopIdx_136;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3332", speed: "300", angle: "angle + 45", count: "4", spread: "90", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (3);
        state.waitTimer = Math.max(0.0167, 0.1);
        yield;
      }
      vars['i'] = _prev_i_136;
      vars['angle'] = (vars['angle'] || 0) + (_util.seedrandom(50 + (vars.kaisu !== undefined ? vars.kaisu : 0), 0, 500, vars));
      vars['kaisu'] = (vars['kaisu'] || 0) + (1);
      vars['angle'] = (vars['angle'] || 0) + (9 * (vars.kakudo !== undefined ? vars.kakudo : 0));
      vars['kakudo'] = -1.2;
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 90", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 180", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "300", angle: "angle + 270", count: "25", spread: "3", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      let _prev_i_137 = vars['i'];
      for (let _loopIdx_137 = 0, _limit_137 = Math.round(9); _loopIdx_137 < _limit_137; _loopIdx_137++) {
        vars['i'] = _loopIdx_137;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3332", speed: "300", angle: "angle + 45", count: "4", spread: "90", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) - (3);
        state.waitTimer = Math.max(0.0167, 0.1);
        yield;
      }
      vars['i'] = _prev_i_137;
      state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
      yield;
    }
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_laser_way', bulletType: "laser", color: "#33ffff", radius: "6", speed: "800", angle: "-90 + 50", count: "7", spread: "10", offsetX: "0", offsetY: "0", growTime: "0.1", keepTime: "0.01", shrinkTime: "0.1", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_laser_way', bulletType: "laser", color: "#33ffff", radius: "6", speed: "800", angle: "-90 - 50", count: "7", spread: "10", offsetX: "0", offsetY: "0", growTime: "0.1", keepTime: "0.01", shrinkTime: "0.1", round: "true", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.4);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_64_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#33ffff").trim().toLowerCase())) {
      vars['angle'] = (vars['angle'] || 0) + (0.4 * (vars.kakudo !== undefined ? vars.kakudo : 0) - (vars.l !== undefined ? vars.l : 0) * (vars.kakudo !== undefined ? vars.kakudo : 0));
      vars['l'] = (vars['l'] || 0) + (0.000);
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['9cvh4w40t']) {
        state.onceMap['9cvh4w40t'] = true;
        vars['speed'] = 100;
      }
      vars['speed'] = (vars['speed'] || 0) + (0.5);
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (20) && ((vars.frame !== undefined ? vars.frame : 0)) <= (50)))) {
        vars['speed'] = (vars['speed'] || 0) - (15);
        if (_util.executeBlock({ type: 'homing', turnSpeed: "360", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (50) && ((vars.frame !== undefined ? vars.frame : 0)) <= (51)))) {
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (50) && ((vars.frame !== undefined ? vars.frame : 0)) <= (120)))) {
        vars['speed'] = (vars['speed'] || 0) + (10);
      }
    }
    vars['radius'] = 10;
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_64_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_65'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_138 = vars['i'];
    for (let _loopIdx_138 = 0, _limit_138 = Math.round(10); _loopIdx_138 < _limit_138; _loopIdx_138++) {
      vars['i'] = _loopIdx_138;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "b_knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + ((vars.angled !== undefined ? vars.angled : 0));
      state.waitTimer = Math.max(0.0167, 0.016 / 8);
      yield;
    }
    vars['i'] = _prev_i_138;
    vars['angled'] = _util.seedrandom((vars.second !== undefined ? vars.second : 0), -5000, 10, vars);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_65_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = (vars['speed'] || 0) + (2);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_65_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_66'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['kakudo'] = 1;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "13", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['kakudo'] = -1;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "0", offsetY: "0", radius: "13", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.02);
    yield;
  }
};
window.compiledDanmaku['danmaku_66_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['mdnr6m421']) {
      state.onceMap['mdnr6m421'] = true;
      vars['x'] = random(-300,1000);
      vars['y'] = 0;
      vars['angle'] = 90 + 10 * (vars.kakudo !== undefined ? vars.kakudo : 0);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    vars['speed'] = (vars['speed'] || 0) + (5);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_66_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_67'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spd'] = 0;
    let _prev_i_139 = vars['i'];
    for (let _loopIdx_139 = 0, _limit_139 = Math.round(10); _loopIdx_139 < _limit_139; _loopIdx_139++) {
      vars['i'] = _loopIdx_139;
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "50 + spd", angle: "angle", count: "1", offsetX: "200", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "50 + spd", angle: "angle", count: "1", offsetX: "-200", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (10);
      vars['spd'] = (vars['spd'] || 0) + (2.5);
    }
    vars['i'] = _prev_i_139;
    if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#ffffff", speed: "1", angle: "angle", offsetX: "200", offsetY: "0", radius: "2", growTime: "0.001", keepTime: "2", shrinkTime: "0.001", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#ffffff", speed: "1", angle: "angle", offsetX: "-200", offsetY: "0", radius: "2", growTime: "0.001", keepTime: "2", shrinkTime: "0.001", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#33ffff", speed: "0", angle: "angle", offsetX: "200", offsetY: "0", radius: "30", growTime: "0.3", keepTime: "0.6", shrinkTime: "1", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_trail_resist', bulletType: "trail", color: "#33ffff", speed: "0", angle: "angle", offsetX: "-200", offsetY: "0", radius: "30", growTime: "0.3", keepTime: "0.6", shrinkTime: "1", round: "true", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    let _prev_i_140 = vars['i'];
    for (let _loopIdx_140 = 0, _limit_140 = Math.round(50); _loopIdx_140 < _limit_140; _loopIdx_140++) {
      vars['i'] = _loopIdx_140;
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "50 + spd", angle: "angle", count: "1", offsetX: "200", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#ff3333", speed: "50 + spd", angle: "angle", count: "1", offsetX: "-200", offsetY: "0", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (10);
      vars['spd'] = (vars['spd'] || 0) + (2.5);
    }
    vars['i'] = _prev_i_140;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
};
window.compiledDanmaku['danmaku_67_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['ex0y4opvg']) {
        state.onceMap['ex0y4opvg'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "120 + seedrandom[5](-0,50)", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars['angle'] || 0) + (180);
      }
      if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,70))) {
        vars['hitRadius'] = 5;
        vars['idoukakudo'] = random(-0.3,0.3);
        vars['speed'] = (vars['speed'] || 0) + (random(0,200));
      }
      if ("frame == 80..") {
        vars['angle'] = (vars['angle'] || 0) + ((vars.idoukakudo !== undefined ? vars.idoukakudo : 0));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['7saj9hmzb']) {
        state.onceMap['7saj9hmzb'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['x'] = (vars['x'] || 0) + ((vars.offsetx !== undefined ? vars.offsetx : 0));
        vars['y'] = (vars['y'] || 0) + ((vars.offsety !== undefined ? vars.offsety : 0));
      }
      if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,2))) {
        vars['speed'] = 8000;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#33ffff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['qwmj1lnzj']) {
        state.onceMap['qwmj1lnzj'] = true;
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['x'] = (vars['x'] || 0) + ((vars.offsetx !== undefined ? vars.offsetx : 0));
        vars['y'] = (vars['y'] || 0) + ((vars.offsety !== undefined ? vars.offsety : 0));
      }
      if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,70))) {
        vars['speed'] = 80000;
        vars['hitRadius'] = 20;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_67_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_68'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 448;
    state.waitTimer = Math.max(0.0167, 0.03);
    yield;
    vars['ey'] = 448;
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (10)))) {
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "1", spread: "120", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (4);
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (10) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (20)))) {
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "2", spread: "180", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (2);
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (20) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (30)))) {
      if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "3", spread: "120", offsetX: "0", offsetY: "0", radius: "12", bulletImage: "b_uroko", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (1.5);
    }
    if (!!((vars.n !== undefined ? (vars.cardFrame === 5 * vars.n) : (Math.round(vars.cardFrame / 5) >= 1 && Math.abs(vars.cardFrame - 5 * Math.round(vars.cardFrame / 5)) < 0.017)))) {
      if (_util.executeBlock({ type: 'play_sound', soundName: "shot", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
  }
};
window.compiledDanmaku['danmaku_68_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['hpzvyxn6r']) {
      state.onceMap['hpzvyxn6r'] = true;
      vars['count'] = 0;
      vars['angle'] = (vars['angle'] || 0) + (random(-10,10));
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['y'] = -80000;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) < 0)) {
      if (!!(_util.fuzzyEqual((vars.count !== undefined ? vars.count : 0) ,1))) {
        vars['color'] = "#ffffff";
        vars['y'] = -8000;
      }
      vars['count'] = 1;
      vars['y'] = 886;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['y'] = -80000;
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 896)) {
      if (!!(_util.fuzzyEqual((vars.count !== undefined ? vars.count : 0) ,1))) {
        vars['color'] = "#ffffff";
        vars['y'] = -8000;
      }
      vars['count'] = 1;
      vars['y'] = 10;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['y'] = -80000;
    }
    if (!!((vars.x !== undefined ? vars.x : 0) < 0)) {
      if (!!(_util.fuzzyEqual((vars.count !== undefined ? vars.count : 0) ,1))) {
        vars['color'] = "#ffffff";
        vars['y'] = -8000;
      }
      vars['count'] = 1;
      vars['x'] = 758;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['y'] = -80000;
    }
    if (!!((vars.x !== undefined ? vars.x : 0) > 768)) {
      if (!!(_util.fuzzyEqual((vars.count !== undefined ? vars.count : 0) ,1))) {
        vars['color'] = "#ffffff";
        vars['y'] = -8000;
      }
      vars['count'] = 1;
      vars['x'] = 10;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      vars['y'] = -80000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_68_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_69'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (0) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (5)))) {
      vars['offset'] = 1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "50", angle: "angle", count: "26", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "virus", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = random(0,20);
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (4) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (10)))) {
      vars['offset'] = -1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "50", angle: "angle", count: "26", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "virus", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = random(0,20);
      state.waitTimer = Math.max(0.0167, 0.05);
      yield;
    }
    if (!!((((vars.cardSecond !== undefined ? vars.cardSecond : 0)) >= (10) && ((vars.cardSecond !== undefined ? vars.cardSecond : 0)) <= (18)))) {
      vars['offset'] = 1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "50", angle: "angle", count: "26", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "virus", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = random(0,20);
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
      vars['offset'] = -1;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "50", angle: "angle", count: "26", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "virus", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = random(0,20);
      state.waitTimer = Math.max(0.0167, 0.1);
      yield;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_69_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((((vars.timer !== undefined ? vars.timer : 0)) >= (0) && ((vars.timer !== undefined ? vars.timer : 0)) <= (5)))) {
      vars['angle'] = (vars['angle'] || 0) + (0.2 * (vars.offset !== undefined ? vars.offset : 0));
    }
    vars['speed'] = 300;
    vars['spriteAngle'] = (vars['spriteAngle'] || 0) + (7);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_69_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_70'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spx'] = 300;
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", offsetX: "spx", offsetY: "-160", radius: "15", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle + 180", offsetX: "-spx", offsetY: "850", radius: "15", bulletImage: "light", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.03);
    yield;
    state.waitTimer = Math.max(0.0167, 0.1-(vars.hindo !== undefined ? vars.hindo : 0));
    yield;
    vars['hindo'] = (vars['hindo'] || 0) + (0.001);
  }
};
window.compiledDanmaku['danmaku_70_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['u1w3g4inp']) {
      state.onceMap['u1w3g4inp'] = true;
      vars['y'] = (vars['y'] || 0) - (100);
      vars['speed'] = 400;
      vars['x'] = (vars['x'] || 0) + (random(-200,200));
      vars['y'] = (vars['y'] || 0) + (random(-70,70));
      vars['speed'] = (vars['speed'] || 0) + (random(-70,70));
      vars['angle'] = (vars['angle'] || 0) + (random(-3,3));
    }
    vars['speed'] = (vars['speed'] || 0) + (1);
    if (!!(abs((vars.y !== undefined ? vars.y : 0)  - (vars.ty !== undefined ? vars.ty : 0)) <= 20)) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['upjkajxmx']) {
        state.onceMap['upjkajxmx'] = true;
        vars['speed'] = 0;
        vars['angle'] = 90;
        if (!!((vars.x !== undefined ? vars.x : 0) < (vars.tx !== undefined ? vars.tx : 0))) {
          vars['angle'] = 0;
        }
        if (!!((vars.x !== undefined ? vars.x : 0) > (vars.tx !== undefined ? vars.tx : 0))) {
          vars['angle'] = 180;
        }
        if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "100", angle: "angle", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "knife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['y'] = -80000;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_70_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = (vars['speed'] || 0) + (1);
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['c1x557klo']) {
      state.onceMap['c1x557klo'] = true;
      vars['angle'] = (vars['angle'] || 0) + (random(-6,6));
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_71'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['offset'] = (vars['offset'] || 0) + (0);
    let _prev_i_141 = vars['i'];
    for (let _loopIdx_141 = 0, _limit_141 = Math.round(8); _loopIdx_141 < _limit_141; _loopIdx_141++) {
      vars['i'] = _loopIdx_141;
      vars['offset'] = (vars['offset'] || 0) + (40);
      vars['offset'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "80", angle: "angle", offsetX: "384", offsetY: "600", radius: "15", bulletImage: "kome", coordMode: "absolute", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_141;
    vars['offset'] = 90;
    if (_util.executeBlock({ type: 'spawn_bullet_resist', bulletType: "normal", color: "#ff3333", speed: "320", angle: "angle", offsetX: "384", offsetY: "600", radius: "30", bulletImage: "knife", coordMode: "absolute", hitRadius: "12", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (0.8);
    state.waitTimer = Math.max(0.0167, 0.016);
    yield;
  }
};
window.compiledDanmaku['danmaku_71_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['l6wndrsyv']) {
      state.onceMap['l6wndrsyv'] = true;
      if (_util.executeBlock({ type: 'advance', distance: "200", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + ((vars.offset !== undefined ? vars.offset : 0));
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_71_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_72'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
    let _prev_i_142 = vars['i'];
    for (let _loopIdx_142 = 0, _limit_142 = Math.round(36); _loopIdx_142 < _limit_142; _loopIdx_142++) {
      vars['i'] = _loopIdx_142;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "500", angle: "-angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "dangan", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffdd33", speed: "400", angle: "angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "dangan", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ff88", speed: "300", angle: "-angle", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "dangan", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (5);
    }
    vars['i'] = _prev_i_142;
    vars['angle'] = (vars['angle'] || 0) + (160.5);
  }
};
window.compiledDanmaku['danmaku_72_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};
window.compiledDanmaku['danmaku_72_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_73'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['kaisuu'] = 0;
    let _prev_i_143 = vars['i'];
    for (let _loopIdx_143 = 0, _limit_143 = Math.round(700); _loopIdx_143 < _limit_143; _loopIdx_143++) {
      vars['i'] = _loopIdx_143;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "kunai2", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (_util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0), 0, 360, vars));
      vars['spd'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0), -250, 150, vars);
      vars['kaisuu'] = (vars['kaisuu'] || 0) + (1);
    }
    vars['i'] = _prev_i_143;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    vars['kaisuu'] = 0;
    let _prev_i_144 = vars['i'];
    for (let _loopIdx_144 = 0, _limit_144 = Math.round(700); _loopIdx_144 < _limit_144; _loopIdx_144++) {
      vars['i'] = _loopIdx_144;
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3388ff", speed: "300 + spd", angle: "angle", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "kunai2", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (_util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0), 0, 360, vars));
      vars['spd'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0), -250, 150, vars);
      vars['kaisuu'] = (vars['kaisuu'] || 0) + (1);
    }
    vars['i'] = _prev_i_144;
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
};
window.compiledDanmaku['danmaku_73_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['te15657ku']) {
      state.onceMap['te15657ku'] = true;
      vars['motospd'] = (vars.speed !== undefined ? vars.speed : 0);
      vars['motocolor'] = (vars.color !== undefined ? vars.color : 0);
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (120) && ((vars.frame !== undefined ? vars.frame : 0)) <= (121)))) {
      vars['speed'] = 0;
      vars['radius'] = 0;
      vars['hitRadius'] = 0;
      vars['color'] = "#aaaaaa";
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (200) && ((vars.frame !== undefined ? vars.frame : 0)) <= (241)))) {
      vars['radius'] = 10;
    }
    if (!!((((vars.frame !== undefined ? vars.frame : 0)) >= (240) && ((vars.frame !== undefined ? vars.frame : 0)) <= (241)))) {
      vars['speed'] = 300;
      vars['color'] = "motocolor";
      vars['hitRadius'] = 3;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_73_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_74'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 300;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle", count: "6", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "dangan", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#3388ff", speed: "200", angle: "kasu", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "dangan", coordMode: "relative", hitRadius: "7", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['kasu'] = (vars['kasu'] || 0) + (2);
    state.waitTimer = Math.max(0.0167, 0.0334);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "syutugen", from: "syutugen", to: "500", mode: "seconds", duration: "2", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "syutugen", from: "syutugen", to: "-100", mode: "seconds", duration: "2", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "360", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "-360", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "0", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "-360", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "360", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "720", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "angle", from: "angle", to: "0", mode: "seconds", duration: "4", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 4);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_74_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['9stuccu45']) {
      state.onceMap['9stuccu45'] = true;
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'advance', distance: "syutugen", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'advance', distance: "600", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_74_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_75'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_145 = vars['i'];
    for (let _loopIdx_145 = 0, _limit_145 = Math.round(3); _loopIdx_145 < _limit_145; _loopIdx_145++) {
      vars['i'] = _loopIdx_145;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#dddddd", speed: "200", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "kunai2", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (0.6);
      state.waitTimer = Math.max(0.0167, 0.0835);
      yield;
    }
    vars['i'] = _prev_i_145;
    let _prev_i_146 = vars['i'];
    for (let _loopIdx_146 = 0, _limit_146 = Math.round(3); _loopIdx_146 < _limit_146; _loopIdx_146++) {
      vars['i'] = _loopIdx_146;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#dddddd", speed: "200", angle: "angle", count: "36", offsetX: "0", offsetY: "0", radius: "10", bulletImage: "kunai2", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) - (0.6);
      state.waitTimer = Math.max(0.0167, 0.0835);
      yield;
    }
    vars['i'] = _prev_i_146;
    vars['angle'] = (vars['angle'] || 0) + (5.5555555555);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_75_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!!((vars.n !== undefined ? (vars.frame === 30 * vars.n) : (Math.round(vars.frame / 30) >= 1 && Math.abs(vars.frame - 30 * Math.round(vars.frame / 30)) < 0.017)))) {
      vars['angle'] = (vars['angle'] || 0) + (60);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      state.waitTimer = Math.max(0.0167, 0.5);
      yield;
      vars['angle'] = (vars['angle'] || 0) - (60);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_75_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_76'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spx'] = 0;
    let _prev_i_147 = vars['i'];
    for (let _loopIdx_147 = 0, _limit_147 = Math.round(38); _loopIdx_147 < _limit_147; _loopIdx_147++) {
      vars['i'] = _loopIdx_147;
      vars['kaisuu'] = (vars['kaisuu'] || 0) + (1);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "200", angle: "90", offsetX: "spx", offsetY: "0", radius: "20", bulletImage: "ohuda", coordMode: "absolute", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['spx'] = (vars['spx'] || 0) + (20);
    }
    vars['i'] = _prev_i_147;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
  }
};
window.compiledDanmaku['danmaku_76_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['6qfxqbn8f']) {
      state.onceMap['6qfxqbn8f'] = true;
      vars['ransuu'] = _util.seedrandom(5 + (vars.kaisuu !== undefined ? vars.kaisuu : 0), 0, 1, vars);
      if (!!((((vars.ransuu !== undefined ? vars.ransuu : 0)) >= (0.5) && ((vars.ransuu !== undefined ? vars.ransuu : 0)) <= (1)))) {
        vars['speed'] = 150;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_76_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_77'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    let _prev_i_148 = vars['i'];
    for (let _loopIdx_148 = 0, _limit_148 = Math.round(8); _loopIdx_148 < _limit_148; _loopIdx_148++) {
      vars['i'] = _loopIdx_148;
      vars['spx'] = random(-400,400);
      vars['spy'] = random(-200,200);
      vars['angle'] = random(0,360);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "0", angle: "angle", count: "12", offsetX: "spx", offsetY: "spy", radius: "20", bulletImage: "redsimple", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['i'] = _prev_i_148;
    state.waitTimer = Math.max(0.0167, 1);
    yield;
  }
};
window.compiledDanmaku['danmaku_77_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['odhjicuyn']) {
      state.onceMap['odhjicuyn'] = true;
      if (_util.executeBlock({ type: 'advance', distance: "10", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'tween_var', name: "multf", from: "0", to: "5", mode: "seconds", duration: "0.8", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'tween_var', name: "hitmultf", from: "0", to: "10", mode: "seconds", duration: "0.8", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'tween_var', name: "multlr", from: "0", to: "0.4", mode: "seconds", duration: "0.8", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.4);
      yield;
      vars['speed'] = 400;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_77_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_78'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['kaisuu'] = (vars['kaisuu'] || 0) + (1);
    vars['angle1'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0) + 1, 0, 360, vars);
    vars['angle2'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0) + 2, 0, 360, vars);
    vars['angle3'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0) + 3, 0, 360, vars);
    vars['angle4'] = _util.seedrandom((vars.kaisuu !== undefined ? vars.kaisuu : 0) + 4, 0, 360, vars);
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#fffffe", speed: "0", angle: "angle1", count: "18", offsetX: "200", offsetY: "-50", radius: "20", bulletImage: "whitesimple", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#fffffe", speed: "0", angle: "angle2", count: "18", offsetX: "-200", offsetY: "-50", radius: "20", bulletImage: "whitesimple", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#fffffe", speed: "0", angle: "angle3", count: "18", offsetX: "200", offsetY: "200", radius: "20", bulletImage: "whitesimple", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#fffffe", speed: "0", angle: "angle4", count: "18", offsetX: "-200", offsetY: "200", radius: "20", bulletImage: "whitesimple", coordMode: "relative", hitRadius: "0", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle1", count: "18", offsetX: "200", offsetY: "-50", radius: "20", bulletImage: "redsimple", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle2", count: "18", offsetX: "-200", offsetY: "-50", radius: "20", bulletImage: "redsimple", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle3", count: "18", offsetX: "200", offsetY: "200", radius: "20", bulletImage: "redsimple", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle4", count: "18", offsetX: "-200", offsetY: "200", radius: "20", bulletImage: "redsimple", coordMode: "relative", hitRadius: "10", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    let _prev_i_149 = vars['i'];
    for (let _loopIdx_149 = 0, _limit_149 = Math.round(4); _loopIdx_149 < _limit_149; _loopIdx_149++) {
      vars['i'] = _loopIdx_149;
      vars['kaisuu2'] = (vars['kaisuu2'] || 0) + (1);
      vars['r1'] = _util.seedrandom((vars.kaisuu2 !== undefined ? vars.kaisuu2 : 0), 0, 360, vars);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "r1", count: "36", offsetX: "200", offsetY: "-50", radius: "20", bulletImage: "blueknife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['r1'] = _util.seedrandom((vars.kaisuu2 !== undefined ? vars.kaisuu2 : 0) + 5, 0, 360, vars);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "r1", count: "36", offsetX: "-200", offsetY: "-50", radius: "20", bulletImage: "blueknife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['r1'] = _util.seedrandom((vars.kaisuu2 !== undefined ? vars.kaisuu2 : 0) + 10, 0, 360, vars);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "r1", count: "36", offsetX: "200", offsetY: "200", radius: "20", bulletImage: "blueknife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['r1'] = _util.seedrandom((vars.kaisuu2 !== undefined ? vars.kaisuu2 : 0) + 15, 0, 360, vars);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "300", angle: "r1", count: "36", offsetX: "-200", offsetY: "200", radius: "20", bulletImage: "blueknife", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.167 * 5);
      yield;
    }
    vars['i'] = _prev_i_149;
    state.waitTimer = Math.max(0.0167, 0.1);
    yield;
  }
};
window.compiledDanmaku['danmaku_78_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['fq0omtll5']) {
        state.onceMap['fq0omtll5'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "-100", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['multf'] = 30;
        vars['hitmultf'] = 80;
        vars['hitmultlr'] = 0;
        vars['hitRadius'] = 0;
        if (_util.executeBlock({ type: 'tween_var', name: "multlr", from: "0", to: "1", mode: "seconds", duration: "1", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 1.5);
        yield;
        vars['hitRadius'] = 10;
        if (_util.executeBlock({ type: 'tween_var', name: "hitmultlr", from: "0", to: "1", mode: "seconds", duration: "1", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.5);
        yield;
        if (_util.executeBlock({ type: 'tween_var', name: "hitmultlr", from: "1", to: "0", mode: "seconds", duration: "1", stepVal: "5", easing: "easeIn", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.5);
        yield;
        if (_util.executeBlock({ type: 'tween_var', name: "multlr", from: "1", to: "0", mode: "seconds", duration: "1", stepVal: "5", easing: "easeIn", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 0.5);
        yield;
        vars['hitRadius'] = 0;
        state.waitTimer = Math.max(0.0167, 0.5);
        yield;
        vars['y'] = -9000;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#fffffe").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['qqn30mm5q']) {
        state.onceMap['qqn30mm5q'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "-100", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['multf'] = 30;
        vars['multlr'] = 0.1;
        state.waitTimer = Math.max(0.0167, 0.6);
        yield;
        vars['y'] = -90000;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_78_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_79'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "0 + l", offsetX: "-400", offsetY: "200", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3388ff", speed: "200", angle: "180 - l", offsetX: "400", offsetY: "200", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#33ffff", speed: "200", angle: "80", offsetX: "-400", offsetY: "200", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#3388ff", speed: "200", angle: "180 - 80", offsetX: "400", offsetY: "200", radius: "6", bulletImage: "light", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.0167 * 3);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "l", from: "l", to: "-90", mode: "seconds", duration: "2", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "l", from: "l", to: "60", mode: "seconds", duration: "2", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 2);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_79_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['wjyhq6ccl']) {
      state.onceMap['wjyhq6ccl'] = true;
      vars['yjiku'] = -1 + random(-0.4,0.4);
      vars['speed'] = (vars['speed'] || 0) + (random(-40,40));
      vars['angle'] = (vars['angle'] || 0) + (random(-10,10));
    }
    vars['y'] = (vars['y'] || 0) + ((vars.yjiku !== undefined ? vars.yjiku : 0));
    vars['yjiku'] = (vars['yjiku'] || 0) + (0.02);
    if (!!((((vars.speed !== undefined ? vars.speed : 0)) >= (10) && ((vars.speed !== undefined ? vars.speed : 0)) <= (5000)))) {
      vars['speed'] = (vars['speed'] || 0) - (0.2);
    }
    if (!!((vars.y !== undefined ? vars.y : 0) > 870)) {
      vars['y'] = -890000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_79_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_s2_0'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#33ccff", speed: "180", angle: "angle", count: "6", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "star", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3388", speed: "140", angle: "angle + 30", count: "6", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "star", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 0.18);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_0_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['speed'] = 180;
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#ffdd00").trim().toLowerCase())) {
      if (!!((vars.isBounced !== undefined ? vars.isBounced : 0))) {
        vars['color'] = "#ffdd00";
        if (_util.executeBlock({ type: 'aim_at_target', }, state, b, attacker, target, _util)) {
          yield;
        }
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_0_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_s2_1'] = [
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['oocfwubtc']) {
      state.onceMap['oocfwubtc'] = true;
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#888888", speed: "0", angle: "0", count: "9", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "greenscale", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring_resist', bulletType: "normal", color: "#000000", speed: "0", angle: "20", count: "9", offsetX: "0", offsetY: "0", radius: "20", bulletImage: "yellowscale", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    vars['spangle'] = (vars['spangle'] || 0) + (0.6);
    state.waitTimer = Math.max(0.0167, 0.0167);
    yield;
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    state.waitTimer = Math.max(0.0167, 2.0);
    yield;
    while (true) {
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "spangle", count: "9", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "goldbig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ffaa33", speed: "200", angle: "-spangle + 20", count: "9", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "orangebig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 0.0167 * 3);
      yield;
    }
  }
},
  function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'tween_var', name: "mukukakudo", from: "0", to: "180", mode: "seconds", duration: "3", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "mukukakudo2", from: "180", to: "0", mode: "seconds", duration: "3", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "mukukakudo", from: "180", to: "0", mode: "seconds", duration: "3", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
    if (_util.executeBlock({ type: 'tween_var', name: "mukukakudo2", from: "0", to: "180", mode: "seconds", duration: "3", stepVal: "5", easing: "easeInOut", }, state, b, attacker, target, _util)) {
      yield;
    }
    state.waitTimer = Math.max(0.0167, 1.5);
    yield;
  }
}
];
window.compiledDanmaku['danmaku_s2_1_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#888888").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['h9nabgu0e']) {
        state.onceMap['h9nabgu0e'] = true;
        if (_util.executeBlock({ type: 'tween_var', name: "multf", from: "0", to: "7", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "hitmultf", from: "0", to: "8", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "multlr", from: "0", to: "0.8", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'advance', distance: "150", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (_util.executeBlock({ type: 'advance', distance: "-150", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (0.6);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      if (_util.executeBlock({ type: 'advance', distance: "150", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#000000").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['nae26q9s0']) {
        state.onceMap['nae26q9s0'] = true;
        if (_util.executeBlock({ type: 'tween_var', name: "multf", from: "0", to: "3", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "hitmultf", from: "0", to: "6", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "multlr", from: "0", to: "0.6", mode: "seconds", duration: "0.6", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'advance', distance: "90", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if (_util.executeBlock({ type: 'advance', distance: "-90", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) - (0.6);
      vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
      if (_util.executeBlock({ type: 'advance', distance: "90", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['m4mr406yi']) {
        state.onceMap['m4mr406yi'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "180", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars.mukukakudo !== undefined ? vars.mukukakudo : 0);
        vars['angle'] = (vars['angle'] || 0) + (random(-3,3));
      }
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffaa33").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['27c5taham']) {
        state.onceMap['27c5taham'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "90", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['angle'] = (vars.mukukakudo2 !== undefined ? vars.mukukakudo2 : 0);
        vars['angle'] = (vars['angle'] || 0) + (random(-3,3));
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_1_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_s2_2'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['angle'] = -90;
    vars['kawaru'] = 0;
    vars['kawaru2'] = 0.6012;
    let _prev_i_150 = vars['i'];
    for (let _loopIdx_150 = 0, _limit_150 = Math.round(36); _loopIdx_150 < _limit_150; _loopIdx_150++) {
      vars['i'] = _loopIdx_150;
      vars['spd'] = 0;
      vars['ang'] = 0;
      let _prev_i_151 = vars['i'];
      for (let _loopIdx_151 = 0, _limit_151 = Math.round(10); _loopIdx_151 < _limit_151; _loopIdx_151++) {
        vars['i'] = _loopIdx_151;
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#ff3333", speed: "0 + spd", angle: "angle + ang", count: "1", spread: "15", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "reddiamond", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'spawn_way', bulletType: "normal", color: "#3388ff", speed: "200 + spd", angle: "angle + ang + 7.5", count: "1", spread: "15", offsetX: "0", offsetY: "0", radius: "6", bulletImage: "bluediamond", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
          yield;
        }
        vars['spd'] = (vars['spd'] || 0) + (20);
        vars['ang'] = (vars['ang'] || 0) + (-5);
      }
      vars['i'] = _prev_i_151;
      vars['angle'] = (vars['angle'] || 0) + (10);
      vars['kawaru'] = (vars['kawaru'] || 0) + (0.0167);
      vars['kawaru2'] = (vars['kawaru2'] || 0) - (0.0167);
    }
    vars['i'] = _prev_i_150;
    state.waitTimer = Math.max(0.0167, 1.67);
    yield;
    state.waitTimer = Math.max(0.0167, 1.67);
    yield;
    let _prev_i_152 = vars['i'];
    for (let _loopIdx_152 = 0, _limit_152 = Math.round(10); _loopIdx_152 < _limit_152; _loopIdx_152++) {
      vars['i'] = _loopIdx_152;
      state.waitTimer = Math.max(0.0167, 0.167);
      yield;
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#888888", speed: "300", angle: "angle", count: "12", offsetX: "0", offsetY: "0", radius: "30", bulletImage: "redbig2", coordMode: "relative", hitRadius: "20", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = random(0,360);
    }
    vars['i'] = _prev_i_152;
  }
};
window.compiledDanmaku['danmaku_s2_2_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() !== String("#888888").trim().toLowerCase())) {
      if (!state.onceMap) state.onceMap = {};
      if (!state.onceMap['ropweevkn']) {
        state.onceMap['ropweevkn'] = true;
        if (_util.executeBlock({ type: 'advance', distance: "50", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "speed", to: "1", mode: "seconds", duration: "2.5", stepVal: "5", easing: "easeOut", }, state, b, attacker, target, _util)) {
          yield;
        }
        state.waitTimer = Math.max(0.0167, 2.3);
        yield;
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
          state.waitTimer = Math.max(0.0167, (vars.kawaru !== undefined ? vars.kawaru : 0));
          yield;
        }
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
          state.waitTimer = Math.max(0.0167, (vars.kawaru2 !== undefined ? vars.kawaru2 : 0));
          yield;
        }
        vars['bulletImage'] = "redeye";
        vars['image'] = "redeye";
        if (b) b.bulletImage = "redeye";
        vars['angle'] = (vars['angle'] || 0) + (160);
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
          vars['bulletImage'] = "blueeye";
          vars['image'] = "blueeye";
          if (b) b.bulletImage = "blueeye";
          vars['angle'] = (vars['angle'] || 0) + (40);
        }
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
          state.waitTimer = Math.max(0.0167, 1 - (vars.kawaru !== undefined ? vars.kawaru : 0));
          yield;
        }
        if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#3388ff").trim().toLowerCase())) {
          state.waitTimer = Math.max(0.0167, 1 - (vars.kawaru2 !== undefined ? vars.kawaru2 : 0));
          yield;
        }
        vars['speed'] = 200;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_2_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_s2_3'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['ey'] = 250;
    state.waitTimer = Math.max(0.0167, 0.5);
    yield;
    vars['count'] = 0;
    vars['s'] = 0;
    let _prev_i_153 = vars['i'];
    for (let _loopIdx_153 = 0, _limit_153 = Math.round(5); _loopIdx_153 < _limit_153; _loopIdx_153++) {
      vars['i'] = _loopIdx_153;
      vars['count'] = (vars['count'] || 0) + (1);
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3333", speed: "0", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "0", bulletImage: "redbig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle + 180", offsetX: "0", offsetY: "0", radius: "0", bulletImage: "redbig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#fffff2", speed: "0", angle: "-angle - 180", offsetX: "0", offsetY: "0", radius: "0", bulletImage: "redbig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (72 * 3);
      state.waitTimer = Math.max(0.0167, 0.0167 * 11);
      yield;
      vars['s'] = (vars['s'] || 0) + (11);
    }
    vars['i'] = _prev_i_153;
    state.waitTimer = Math.max(0.0167, 5);
    yield;
    vars['angle'] = (vars['angle'] || 0) + (15);
  }
};
window.compiledDanmaku['danmaku_s2_3_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3333").trim().toLowerCase())) {
      if (_util.executeBlock({ type: 'advance', distance: "200", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (180);
      vars['angle'] = (vars['angle'] || 0) + (72 / 4);
      let _prev_i_154 = vars['i'];
      for (let _loopIdx_154 = 0, _limit_154 = Math.round(10); _loopIdx_154 < _limit_154; _loopIdx_154++) {
        vars['i'] = _loopIdx_154;
        let _prev_i_155 = vars['i'];
        for (let _loopIdx_155 = 0, _limit_155 = Math.round(2); _loopIdx_155 < _limit_155; _loopIdx_155++) {
          vars['i'] = _loopIdx_155;
          vars['s2angle'] = -90;
          let _prev_i_156 = vars['i'];
          for (let _loopIdx_156 = 0, _limit_156 = Math.round(5); _loopIdx_156 < _limit_156; _loopIdx_156++) {
            vars['i'] = _loopIdx_156;
            vars['s2angle'] = (vars['s2angle'] || 0) + (72);
            if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ff3332", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "goldbig", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
              yield;
            }
          }
          vars['i'] = _prev_i_156;
          if (_util.executeBlock({ type: 'advance', distance: "20", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['spangle'] = (vars['spangle'] || 0) - (-9.2);
        }
        vars['i'] = _prev_i_155;
        state.waitTimer = Math.max(0.0167, 0.0167);
        yield;
        vars['s'] = (vars['s'] || 0) + (1);
      }
      vars['i'] = _prev_i_154;
      vars['y'] = -8000;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
      if (_util.executeBlock({ type: 'advance', distance: "100", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (180);
      vars['angle'] = (vars['angle'] || 0) + (72 / 4);
      let _prev_i_157 = vars['i'];
      for (let _loopIdx_157 = 0, _limit_157 = Math.round(10); _loopIdx_157 < _limit_157; _loopIdx_157++) {
        vars['i'] = _loopIdx_157;
        let _prev_i_158 = vars['i'];
        for (let _loopIdx_158 = 0, _limit_158 = Math.round(2); _loopIdx_158 < _limit_158; _loopIdx_158++) {
          vars['i'] = _loopIdx_158;
          vars['s3angle'] = -90 + 60;
          let _prev_i_159 = vars['i'];
          for (let _loopIdx_159 = 0, _limit_159 = Math.round(3); _loopIdx_159 < _limit_159; _loopIdx_159++) {
            vars['i'] = _loopIdx_159;
            vars['s3angle'] = (vars['s3angle'] || 0) + (120);
            if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "bluebig", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
              yield;
            }
          }
          vars['i'] = _prev_i_159;
          if (_util.executeBlock({ type: 'advance', distance: "10", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['spangle'] = (vars['spangle'] || 0) - (-9.2);
        }
        vars['i'] = _prev_i_158;
        state.waitTimer = Math.max(0.0167, 0.0167 * 2);
        yield;
        vars['s'] = (vars['s'] || 0) + (2);
      }
      vars['i'] = _prev_i_157;
      vars['y'] = -8000;
    }
    if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#fffff2").trim().toLowerCase())) {
      if (_util.executeBlock({ type: 'advance', distance: "100", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (180);
      vars['angle'] = (vars['angle'] || 0) + (72 / 4);
      let _prev_i_160 = vars['i'];
      for (let _loopIdx_160 = 0, _limit_160 = Math.round(10); _loopIdx_160 < _limit_160; _loopIdx_160++) {
        vars['i'] = _loopIdx_160;
        let _prev_i_161 = vars['i'];
        for (let _loopIdx_161 = 0, _limit_161 = Math.round(2); _loopIdx_161 < _limit_161; _loopIdx_161++) {
          vars['i'] = _loopIdx_161;
          vars['s3angle'] = 90 + 60;
          let _prev_i_162 = vars['i'];
          for (let _loopIdx_162 = 0, _limit_162 = Math.round(3); _loopIdx_162 < _limit_162; _loopIdx_162++) {
            vars['i'] = _loopIdx_162;
            vars['s3angle'] = (vars['s3angle'] || 0) - (120);
            if (_util.executeBlock({ type: 'spawn_bullet', bulletType: "normal", color: "#ffffff", speed: "0", angle: "angle", offsetX: "0", offsetY: "0", radius: "9", bulletImage: "bluebig", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
              yield;
            }
          }
          vars['i'] = _prev_i_162;
          if (_util.executeBlock({ type: 'advance', distance: "10", }, state, b, attacker, target, _util)) {
            yield;
          }
          vars['spangle'] = (vars['spangle'] || 0) - (9.2);
        }
        vars['i'] = _prev_i_161;
        state.waitTimer = Math.max(0.0167, 0.0167 * 2);
        yield;
        vars['s'] = (vars['s'] || 0) + (2);
      }
      vars['i'] = _prev_i_160;
      vars['y'] = -8000;
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_3_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['2kpy3oeym']) {
      state.onceMap['2kpy3oeym'] = true;
      vars['motoangle'] = (vars.angle !== undefined ? vars.angle : 0);
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        vars['angle'] = (vars.s2angle !== undefined ? vars.s2angle : 0);
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
        vars['angle'] = (vars.s3angle !== undefined ? vars.s3angle : 0);
      }
    }
    if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,65 - (vars.s !== undefined ? vars.s : 0)))) {
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "0", to: "600", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "600", to: "0", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
        if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "0", to: "300", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
        if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "300", to: "0", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
          yield;
        }
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      vars['angle'] = (vars.motoangle !== undefined ? vars.motoangle : 0);
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ff3332").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + ((vars.spangle !== undefined ? vars.spangle : 0));
      }
      if ((String((vars.color !== undefined ? vars.color : "")).trim().toLowerCase() === String("#ffffff").trim().toLowerCase())) {
        vars['angle'] = (vars['angle'] || 0) + ((vars.spangle !== undefined ? vars.spangle : 0) * 0.1);
      }
      if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "0", to: "130", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
    }
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};

window.compiledDanmaku['danmaku_s2_4'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    vars['spd'] = 0;
    vars['wa'] = 0;
    let _prev_i_163 = vars['i'];
    for (let _loopIdx_163 = 0, _limit_163 = Math.round(20); _loopIdx_163 < _limit_163; _loopIdx_163++) {
      vars['i'] = _loopIdx_163;
      vars['spd'] = (vars['spd'] || 0) + (50);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "100 + spd", angle: "angle", count: "18", offsetX: "0", offsetY: "0", radius: "18", bulletImage: "yellowarrow", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (1);
    }
    vars['i'] = _prev_i_163;
    vars['angle'] = (vars['angle'] || 0) + (14);
    state.waitTimer = Math.max(0.0167, 0.0167 * 60);
    yield;
    vars['spd'] = 0;
    vars['wa'] = 0;
    let _prev_i_164 = vars['i'];
    for (let _loopIdx_164 = 0, _limit_164 = Math.round(20); _loopIdx_164 < _limit_164; _loopIdx_164++) {
      vars['i'] = _loopIdx_164;
      vars['spd'] = (vars['spd'] || 0) + (50);
      if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "100 + spd", angle: "angle", count: "18", offsetX: "0", offsetY: "0", radius: "18", bulletImage: "yellowarrow", coordMode: "relative", hitRadius: "6", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) - (1);
    }
    vars['i'] = _prev_i_164;
    vars['angle'] = (vars['angle'] || 0) + (14);
    state.waitTimer = Math.max(0.0167, 0.0167 * 60);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_4_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (!state.onceMap) state.onceMap = {};
    if (!state.onceMap['hoawmvvg4']) {
      state.onceMap['hoawmvvg4'] = true;
      if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "speed", to: "0", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      if (_util.executeBlock({ type: 'advance', distance: "50", }, state, b, attacker, target, _util)) {
        yield;
      }
      state.waitTimer = Math.max(0.0167, 1);
      yield;
      vars['bulletImage'] = "whitearrow";
      vars['image'] = "whitearrow";
      if (b) b.bulletImage = "whitearrow";
    }
    if (!!(_util.fuzzyEqual((vars.frame !== undefined ? vars.frame : 0) ,120))) {
      vars['bulletImage'] = "yellowarrow";
      vars['image'] = "yellowarrow";
      if (b) b.bulletImage = "yellowarrow";
      if (_util.executeBlock({ type: 'tween_var', name: "speed", from: "400", to: "1000", mode: "seconds", duration: "1", stepVal: "5", easing: "linear", }, state, b, attacker, target, _util)) {
        yield;
      }
      vars['angle'] = (vars['angle'] || 0) + (random(-11,11));
    }
    vars['spriteAngle'] = (vars.angle !== undefined ? vars.angle : 0);
    state.waitTimer = Math.max(state.waitTimer || 0, state.dt || 0.0167);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_4_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

window.compiledDanmaku['danmaku_s2_5'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
  while (true) {
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle * 546", count: "8", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "redkunai2", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "angle * 2.2", count: "8", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "redkunai2", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    if (_util.executeBlock({ type: 'spawn_ring', bulletType: "normal", color: "#ff3333", speed: "200", angle: "-angle * 4562.2", count: "8", offsetX: "0", offsetY: "0", radius: "8", bulletImage: "redkunai2", coordMode: "relative", hitRadius: "3", }, state, b, attacker, target, _util)) {
      yield;
    }
    vars['angle'] = (vars['angle'] || 0) + (1.345);
    state.waitTimer = Math.max(0.0167, 0.0167 * 2);
    yield;
  }
};
window.compiledDanmaku['danmaku_s2_5_bullet'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};
window.compiledDanmaku['danmaku_s2_5_magic'] = function*(state, b, attacker, target, _util) {
  let vars = state.variables;
  const random = _util.rand;
  const rand = _util.rand;
  const seedrandom = _util.seedrandom;
};

